# Шаблони листів (doc 40 §B.6, §B.7)

**`demo_url` — заповнювач, і він порожній. Потребує засновника.**

До 2026-08-20 тут стояла окрема демонстрація на `vercel.app` під старою назвою продукту
(точний рядок — у коментарі `scripts/validate-canonical-docs.mjs`:169-179 та в історії git;
тут він не повторюється, бо цей каталог — не архів). Її перевіряли 26.07.2026: `/`, `/pilot`,
`/demo`, `/roadmap`, `/legal` повертали 200, і сторінка сама заявляла «демонстраційний прототип»,
«продукт не має клієнтів і не має підтвердженого попиту», «не надає юридичної сили жодному
типу доказу». **Ця демонстрація видалена** разом з `apps/demo` 2026-08-20, а продукт
перейменовано на GoProceed 2026-08-18, тож посилання мертве двічі.

**`https://goproceed-landing.vercel.app` не є заміною за замовчуванням.** Лендинг живий і
публічний, але не несе жодної з трьох оговорок вище: рядка «демонстраційний прототип» у
`apps/landing/` немає, як і застереження про юридичну силу (воно лишилось тільки в
`app/kitchen-sink/`). Направити туди листи — порушити §B.6 цього ж файлу. Що саме має
стояти в `demo_url`, вирішує засновник; до того часу заповнювач лишається порожнім.

**Жоден лист не надіслано. Жодного чернетки в Gmail не створено.**

---

## Правила, обовʼязкові для кожного шаблону (§B.6)

- Простий текст. Без HTML, зображень, трекінг-пікселя, скорочувача посилань і
  unsubscribe-хедера. Має виглядати як лист однієї людини до іншої.
- **Рівно одне посилання** — на `demo_url`. Друге посилання ламає доставність і розсіює увагу.
- **120–170 слів** у тілі листа (без теми і підпису).
- Тема — з малої літери, без маркетингової пунктуації.
- Один рядок про відмову від листування.
- Прямо сказано, що GoProceed — **демонстраційний прототип**.
- **Ніколи**: клієнти, впровадження, трафік, підтверджений попит, український референс.
- **Ніколи**: показник, якого GoProceed не вимірював.
- **Ніколи**: юридична сила будь-якого типу доказу (doc 00, doc 24).
- Персоналізація — на **конкретному перевіреному факті з вказаним джерелом**.
- **Жодного запиту документів** на щаблях 1–2.
- Підпис — справжнє імʼя і робочий телефон.

## Варіанти

| Файл | Коли застосовується |
|---|---|
| `a1-hiring-signal.md` | Сигнал — відкрита вакансія інженера ПТО/ВТВ |
| `a2-tender-award.md` | Сигнал — публічний award у ProZorro з датою |
| `a3-project-listing.md` | Сигнал — датований проєкт, обʼєкт або профіль на власному сайті |
| `f1-followup.md` | Дотик 2, +4 робочих дні |
| `f2-breakup.md` | Дотик 3, +9 робочих днів після F1 |

Варіант обирається **за типом сигналу, а не випадково** — це не порівнювані плечі
експерименту (§Loop 9).

## ER-8b — щабель 2.5 залишається відкритим

З F1 і F2 прибрано «дзвінок не потрібен». Асинхронність — правильний **перший** запит і
неправильний кінцевий стан: doc 12 Stage 0 вимагає інтервʼю на матеріалі реального пакета,
а абзац у пошті не дозволяє пройти повернутий АВР рядок за рядком. Тридцятихвилинна розмова
лишається доступною, якщо співрозмовник сам її запропонує.

## Заповнювачі

| Заповнювач | Джерело |
|---|---|
| `{{signal}}` | `personalization_signal` з датасету — дослівно |
| `{{signal_context}}` | Одне речення, що саме в цьому сигналі зачепило |
| `{{ім'я}}` | Тільки якщо імʼя опубліковано публічно; інакше «Доброго дня.» |
| `{{ім'я, прізвище}}` | **Потребує засновника** |
| `{{телефон}}` | **Потребує засновника — §B.9 позначає блокуючим для першого надсилання** |
| `{{demo_url}}` | **Потребує засновника — блокуюче для першого надсилання.** Стара демонстрація видалена 2026-08-20; лендинг не несе оговорок §B.6 (див. шапку файлу) |

**`{{` у готовій чернетці — жорсткий провал** (§B.10 Loop 4 крок 4).
