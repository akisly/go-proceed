# DEV-008 — One `@types/react` copy, so CI `typecheck` is deterministic

## Assignment

- **Objective and user-visible outcome:** the `ci` workflow's `verify` job stops failing `pnpm turbo run typecheck` at random on identical code. Run 34856425465 failed attempt 1 and passed attempt 2 on the same commit `001b21a`; after this task the lockfile gives pnpm nothing to choose between, so the outcome no longer depends on the run.
- **State:** done
- **Coordinator:** primary Claude Code session, 2026-09-14.
- **Execution mode:** independent subagents for the required stages, as native `gp-*` agent types.
- **Selected route and why:** bounded bug with an understood cause (`agents/COORDINATION.md`): coordinator implements → `gp-reviewer` (+ `gp-mobile`) → `gp-qa`. `package.json` and `pnpm-lock.yaml` are build configuration under root `AGENTS.md`.
- **Triggered stages and why:**
  - `gp-mobile`: `apps/mobile/package.json` changes, and the lockfile re-resolves every `apps/mobile` package that takes `@types/react` as a peer (`react-native`, `expo`, `expo-router` and their dependents).
  - `gp-security` is not triggered: no workflow permission, action pin, secret or environment variable changes. The temporary diagnostic workflow (Plan step 4) lives only on the unmerged branch `diag/types-react-hoist`, with `permissions: contents: read` and the same SHA pins as `ci.yml`.
  - `gp-ui-reviewer` is not triggered: no file under `apps/landing`, `apps/app/app`, `packages/ui` or `packages/tokens` changes; the errors were in `packages/ui` but its source is untouched.
  - `gp-architect` is not triggered: no schema, contract or catalog.
  - `gp-researcher` is not used: the deciding facts are pnpm 9.12.0's own installed source and the CI logs, read directly (Sources).
- **Owning module and allowed edit paths:** `apps/mobile/package.json` (the `@types/react` devDependency only); `pnpm-lock.yaml` (regenerated by pnpm 9.12.0); this record; `docs/tasks/README.md`; `docs/BACKLOG.md` (one new entry).
- **Read context:** root `AGENTS.md`, `START_HERE.md`, `agents/COORDINATION.md`, `agents/TASK_TEMPLATE.md`, `apps/mobile/AGENTS.md`; `.github/workflows/ci.yml`; `turbo.json`; `apps/landing/tsconfig.json`; `packages/ui/tsconfig.json`; `docs/superpowers/plans/evidence/2026-08-01-b0-gate.md:762-764` (why `apps/mobile` pinned 19.2.4).
- **Linked spec, ADR or earlier task:** none.
- **Baseline:** `7b57784` (main, «Merge pull request #88»). The failing run was on PR #88's head `001b21a`; the files this task changes are identical there and at `7b57784`.
- **Dependencies / constraints / out of scope:**
  - No `pnpm turbo run test`, `pnpm --filter @goproceed/app test`, `pnpm --filter @goproceed/testing test` or `supabase db reset` locally (root `AGENTS.md`); CI runs the suites.
  - pnpm stays at 9.12.0; upgrading pnpm, changing `turbo.json`, the landing `tsconfig.json` or CI steps is out of scope, since none of them is the cause (Progress row 3).
  - `@types/react-dom` is already one version (19.2.4) and is not touched.
- **Required acceptance criteria:** see Acceptance evidence.
- **Skipped stages and rationale:** above.

## Owner decisions

| Date | Decision | Source |
|---|---|---|
| 2026-09-14 | Find the cause of the non-deterministic `typecheck` with evidence from CI logs and safe local reproduction (typecheck only), make it deterministic, and prove it with repeated CI runs | Owner, in conversation (task brief) |
| 2026-09-14 | Criterion 6 stays as written; the task stays blocked until GitHub Actions runs again on the head, rather than accepting the green runs on documentation-only heads | Owner, in conversation (answer to the coordinator's question) |
| 2026-09-14 | Delete the temporary branch `diag/types-react-hoist` | Owner, in conversation |
| 2026-09-14 | GitHub Actions billing is not changed this month; CI stays red until the next billing month (October 2026) | Owner, in conversation |
| 2026-09-14 | Criterion 6 revised: the four green CI runs count («засчитываю 4 прогона, закрывай задачу»). This supersedes the earlier decision to keep criterion 6 as written | Owner, in conversation |

## Plan

1. Read the failing and passing logs; reproduce the exact errors locally by varying one input. Check: identical file, line, column and code.
2. Find in pnpm 9.12.0's installed source why that input varies. Check: the code path cited by line.
3. Align `apps/mobile`'s `@types/react` with the other three packages and regenerate the lockfile with pnpm 9.12.0. Check: one `@types/react` version in the lockfile; the package set differs by that version alone; clean `--frozen-lockfile` install; `pnpm turbo run typecheck --force` 10/10.
4. Temporary diagnostic workflow on an unmerged branch: sample the hoisted copy over clean installs on Linux runners, on the baseline and on the fix. Check: baseline varies or the variance is recorded as not observed; fix is 19.2.18 in every sample.
5. `gp-reviewer` and `gp-mobile` on the diff, `gp-qa`, the PR, repeated CI runs.

## Progress and decisions

| Order | State or role | Decision / result | Evidence / reference | Next action |
|---|---|---|---|---|
| 1 | implementing (coordinator): logs | Attempt 1 of run 34856425465 failed only `@goproceed/landing#typecheck` with 17 TS2322 errors. Each elaboration ends in «Two different types with this name exist, but they are unrelated», and the source type is printed as `import(".../node_modules/.pnpm/@types+react@19.2.18/.../index").Ref` while the target is the unqualified global `React.Ref`: two `@types/react` copies are in one program, and the JSX intrinsic types come from the other. `apps/app#typecheck` in the same job was cut off when landing failed (inconclusive); `app-qa`'s own `apps/app` typecheck on another runner in the same attempt passed. Both jobs switched pnpm from v11.19.0 to v9.12.0 before installing; all three runs used runner image `ubuntu24/20260907.300`, Node 24.20.0, `Packages: +1037` | `gh run view 34856425465 --attempt 1 --log` (verify lines 1246–1450, app-qa 2732–2748); attempt 2; run 34854798473 | Reproduce |
| 2 | implementing (coordinator): reproduction | `tsc --explainFiles` for `apps/landing` shows `next` (its bundled `styled-jsx` types), `lucide-react`, `framer-motion`, `@tanstack/react-table` and `@tanstack/react-store` import `react` from virtual-store directories that hold no `@types/react`, so TypeScript finds it in pnpm's private hoist, `node_modules/.pnpm/node_modules/@types/react`. In a scratch worktree of `001b21a`, pointing that one symlink at 19.2.4 made landing's `tsc --noEmit` print exactly CI's 17 errors (same file, line, column, code and elaboration; `landing-hoist-4.txt` against `fail-a1.log:1246-1433`); at 19.2.18 it exited 0 and printed nothing (the empty `landing-hoist-18.txt`; row 6 records exit codes). `apps/app` fails the same way (17 errors); `packages/ui` alone passes | Scratch worktree; `ln -sfn` on the hoisted link | Find why the hoist varies |
| 3 | implementing (coordinator): cause in pnpm | pnpm 9.12.0's private hoist is first-wins over importer order: `getHoistedDependencies` reduces every importer's direct dependencies keeping the first alias seen, then `hoistGraph` skips aliases already taken. `headlessInstall` takes that order from `selectedProjectDirs`, which the recursive install command fills by pushing each project inside `Promise.all(importers.map(async … => { await memReadLocalConfig(rootDir); … push }))`, where `readLocalConfig` is an async read of the project's `.npmrc`. The order is therefore whichever read finishes first. When `apps/mobile` (19.2.4) precedes `apps/app`, `apps/landing` and `packages/ui` (19.2.18), the hoist takes 19.2.4. Six clean local installs on APFS all hoisted 19.2.18. Ruled out: turbo cache (0 cached); `.tsbuildinfo` (ignored, not tracked; only landing writes one, and none exists on a fresh runner); `.next/types` (no build precedes `typecheck`); the pnpm v10→v11 layout warning (install ran 9.12.0); runner image (identical) | pnpm 9.12.0 `dist/pnpm.cjs` lines in Sources; `git ls-files`; `.gitignore:37` | Fix |
| 4 | implementing (coordinator): fix | `apps/mobile`'s `@types/react` 19.2.4 → 19.2.18, lockfile regenerated. `pnpm` on this machine is a pnpm 11.9.0 shim (`~/Library/pnpm/pnpm`) that switches to the `packageManager` version: `pnpm --version` prints its own «"pnpm" field … no longer read» warning, then `9.12.0`; the regenerate, the installs and the typecheck ran through it. The lockfile diff (321 added, 335 removed lines) holds three kinds of change: (a) peer-suffix renames from `@types/react@19.2.4` to `@types/react@19.2.18` on `apps/mobile`'s packages, plus the one importer specifier; (b) removed entries: the `@types/react@19.2.4` package and the optional `@types/react-dom@19.2.4(@types/react@19.2.4)` snapshot, and `bufferutil` / `utf-8-validate` dropped from the `@react-native/metro-config@0.86.2(@babel/core@7.29.7)` snapshot's `transitivePeerDependencies` (metadata, nothing installed changes); (c) re-sorted snapshot blocks for `@radix-ui/*`, `react-remove-scroll*`, `use-callback-ref` and `use-sidecar`, whose moved lines look like `react` 19.2.3/19.2.8 swaps. With peer suffixes stripped, the package and snapshot key sets differ from `7b57784` by `@types/react@19.2.4` alone, and no integrity hash changes. The existing unmet-peer warning for `react-server-dom-webpack` in `apps/mobile` predates this (`grep -cF 'react-server-dom-webpack@19.2.8(react-dom@19.2.3' pnpm-lock.yaml` gives 28 on `7b57784` and 28 on `5a38091`). Commit `5a38091` | Criteria 2–4 | Diagnostic sampler, review |
| 5 | implementing (coordinator): Linux sampler | Temporary workflow `diag-types-react-hoist` on the unmerged branch `diag/types-react-hoist` (base `7b57784` plus the workflow file only): 12 runners, 6 on `7b57784` and 6 on `claude/types-react-dedupe`, each 5 clean `pnpm install --frozen-lockfile` with the setup `ci.yml` uses. All 60 samples hoisted 19.2.18, and landing `tsc` gave 0 errors on every first sample. The natural variance was not observed in this sample; CI's `verify` failed once in three runs on the baseline code | Run 34869969752 (`gh run view 34869969752 --log \| grep SAMPLE`) | Force the order |
| 6 | implementing (coordinator): forced order (v2 after R1-02) | To show the order dependence without waiting for it, `apps/app/.npmrc`, `apps/landing/.npmrc` and `packages/ui/.npmrc` are made FIFOs whose writer opens only after 3 seconds, so those three async reads finish after `apps/mobile`'s; the install itself is an ordinary `pnpm install --frozen-lockfile`, no link touched by hand. None of the three paths is tracked. Results (pnpm 9.12.0): **`001b21a` forced**: install exit 0, hoisted 19.2.4, landing `tsc` exit 2 with CI's 17 errors, and the landing program lists `@types+react@19.2.18` and `@types+react@19.2.4`; **`001b21a` control** (no FIFOs): hoisted 19.2.18, `tsc` exit 0, one copy. **`5a38091` forced**: hoisted 19.2.18, `tsc` exit 0, one copy (`@types+react@19.2.18`); **control**: the same. Both trees clean afterwards | `forced-order-v2.sh` (below); its `fo2-base.summary`, `fo2-fix.summary` and per-run `.install` / `.tsc` outputs | Commit, review |
| 7 | reviewing (`gp-mobile`, native), round 1 | No effect on installability, offline, capture, signing, store, OTA or device behaviour: types only, no runtime or native version moves, no OTA surface (`app.json` has no `updates`, `expo-updates` absent). `react-native@0.86.2` peers `@types/react ^19.1.1` (optional); `expo-router@57.0.9` builds against `~19.2.0`. Expo's check takes `@types/react` from the SDK versions API, not `bundledNativeModules.json`. M1-01 (info), M1-02 (minor) | gp-mobile report | Fix M1-02 |
| 8 | reviewing (`gp-reviewer`, native), round 1 | **Changes requested**, no blocker. The causal chain is confirmed against `pnpm.cjs` (`:198014-198075` → `:197417` → `:179002`, `:179062-179063` → `:140712`, first-wins `:140718-140723`); the CI reproduction matches; no package version or integrity changes; stage reasoning and BL-083's form correct. R1-01 and R1-02 medium; R1-03 to R1-06 low | gp-reviewer report | Fix R1-01 to R1-06 |
| 9 | rework (coordinator), stated fixes | R1-01 / M1-02: `https://api.expo.dev/v2/versions` read 2026-09-14T16:50Z gives SDK 57 `@types/react` `~19.2.4` (`@types/react-dom` `~19.2.3`, `typescript` `~6.0.3`, `expoVersion` `~57.0.22`, `react-native` `0.86.3`); `CI=1 expo install --check` in `apps/mobile` exits 1 on both `001b21a` and `5a38091` with identical output listing nine outdated Expo and React Native packages and not `@types/react`; B0's exit 0 of 2026-08-01 no longer holds on the baseline because the SDK 57 list moved. R1-02: forced order re-run as v2 with exit codes and a program-file control (row 6). R1-03, R1-04: row 4 and criterion 2. R1-05, R1-06, M1-01: «What is not true», criterion 6, BL-083. Not a rework round: no QA FAIL preceded it | `expo-check-base.txt`, `expo-check-fix.txt`, `expo-versions.json` in the session scratchpad; this diff | `gp-qa` |
| 10 | verifying (`gp-qa`, native), round 1 on `22bb1fe` | Criteria 1–5, 7 and 8 PASS, re-run where runnable: forced order `qa-base` / `qa-fix` reproduced row 6 (baseline hoist 19.2.4, 17 errors identical to CI; fix one copy, exit 0); key sets and integrity compared independently; clean install and `turbo run typecheck --force` 10/10 in the repository worktree; validators rc 0. Criterion 6 NOT RUN (CI in progress). All eight review fixes in place. Q1-01 and Q1-02 (info, record text). Run 34871897666 on `41b9283` failed `verify` at `pnpm validate:canonical-docs`: that commit set the record to `verifying` while the index said `implementing`, and the coordinator's validator call piped through `tail`, hiding its exit code; `d4d8672` fixed the index | QA report; `scratchpad/qa/` | Fix Q1-01, Q1-02; CI |
| 11 | rework (coordinator), stated fixes | Q1-01: the count's command stated in row 4. Q1-02: the script resolves its output directory absolutely, in the record and in `forced-order-v2.sh`. Not a rework round: no QA FAIL | This diff | CI on the final head |
| 12 | CI (coordinator) | On head `3a9ea42`, run 34872695375 attempt 1: `verify` and `app-qa` success. Attempts 2 and 3 (`gh run rerun`): both jobs failed in 2–4 s without starting, each annotated «The job was not started because recent account payments have failed or your spending limit needs to be increased». Earlier heads, each equal to `5a38091` in `apps`, `packages`, `pnpm-lock.yaml`, `package.json`, `turbo.json` and `.github` and differing only in `docs/`: 34870624343 (`953310b`), 34871928171 (`d4d8672`), 34871968417 (`22bb1fe`) all green; 34871897666 (`41b9283`) failed `verify` at `validate:canonical-docs` (row 10). Branch `diag/types-react-hoist` deleted on the owner's word; run 34869969752 stays in Actions | `gh run view 34872695375 --attempt 2` / `--attempt 3` annotations; `git diff --quiet 5a38091 3a9ea42 -- apps packages pnpm-lock.yaml package.json turbo.json .github` rc 0 | `gp-qa` narrow re-check |
| 13 | verifying (`gp-qa`, native), round 2 narrow re-check on `3a9ea42` | Q1-01 and Q1-02 fixes PASS (count 28 on `7b57784`, `5a38091` and head; the script run by a relative path writes into the scratchpad and leaves the tree clean); rows 10–11 faithful; validators rc 0; diff since `22bb1fe` is the record only. Criterion 6 NOT RUN, environmental: one executed pass on the head, two attempts not started for billing. No new findings | QA narrow re-check report; `fo2-qa2-fix.summary` | Owner decision |
| 14 | blocked (coordinator) | The owner kept criterion 6 as written (Owner decisions). Every other required criterion passes on `3a9ea42`. Resume when Actions runs jobs again: re-scope, re-run the head twice, record the attempts, `gp-qa` checks criterion 6 | This record | Owner: restore GitHub Actions billing or spending limit |
| 15 | blocked (coordinator) | Rerun of 34874883597 on the new head `cb9df50` (documentation-only after `3a9ea42`): attempt 2 not started, same billing annotation. The owner leaves billing as is until October 2026 (Owner decisions). Every CI run on this account shows red until then, whatever the code | `gh run view 34874883597 --repo akisly/go-proceed --attempt 2` | Resume in October 2026 |
| 16 | done (coordinator) | The owner revised criterion 6 to the four green runs on heads equal to `5a38091` in every fix-bearing path; `gp-qa`'s narrow re-check had observed those runs and that equality (row 13). The equality still holds at the closing head. Every required criterion passes for the revised scope; merging is the owner's decision | Owner decisions; `git diff --quiet 5a38091 HEAD -- apps packages pnpm-lock.yaml package.json turbo.json .github` rc 0 | None for this task |

**The forced-order script, v2** (row 6; `forced-order-v2.sh <checkout> <label>`, run from a checkout whose dependencies install from the store; it deletes every `node_modules` and writes `fo2-<label>.*` next to itself, by absolute path even when called by a relative one (Q1-02)):

```bash
#!/bin/bash
set -u
D="$1"; L="$2"; O="$(cd "$(dirname "$0")" && pwd)/fo2-$L"; cd "$D" || exit 2
SLOW="apps/app apps/landing packages/ui"
{ echo "head=$(git rev-parse --short HEAD)"; echo "pnpm=$(pnpm --version 2>/dev/null | tail -1) at $(command -v pnpm)"; echo "lockfile @types/react: $(grep -oE '@types/react@19\.[0-9.]+' pnpm-lock.yaml | sort -u | tr '\n' ' ')"; } > "$O.summary"
run_install() {  # $1 = forced|control
  rm -rf node_modules apps/*/node_modules packages/*/node_modules discovery/node_modules
  if [ "$1" = forced ]; then
    for p in $SLOW; do rm -f "$p/.npmrc"; mkfifo "$p/.npmrc"; done
    ( sleep 3; for n in $(seq 1 40); do for p in $SLOW; do ( exec 3>"$p/.npmrc" ) & done; sleep 0.5; done ) &
    FEEDER=$!
  fi
  pnpm install --frozen-lockfile --reporter=silent > "$O.$1.install" 2>&1; local rc=$?
  if [ "$1" = forced ]; then kill $FEEDER 2>/dev/null; pkill -f "exec 3>" 2>/dev/null; sleep 1; for p in $SLOW; do rm -f "$p/.npmrc"; done; fi
  echo "$1: install rc=$rc hoisted=$(readlink node_modules/.pnpm/node_modules/@types/react)" >> "$O.summary"
  ( cd apps/landing && ../../node_modules/.bin/tsc --noEmit --incremental false > "$O.$1.tsc" 2>&1; echo "$1: landing tsc rc=$? errors=$(grep -c 'error TS' "$O.$1.tsc") bytes=$(wc -c < "$O.$1.tsc")" >> "$O.summary" )
  ( cd apps/landing && echo "$1: @types/react copies in landing program: $(../../node_modules/.bin/tsc --listFilesOnly 2>&1 | grep -oE '@types\+react@[0-9.]+/node_modules/@types/react/index\.d\.ts' | sort -u | sed 's#/node_modules.*##' | tr '\n' ' ')" >> "$O.summary" )
}
run_install forced
run_install control
echo "tracked-tree clean: $(git status --porcelain | wc -l | tr -d ' ') changed paths" >> "$O.summary"
cat "$O.summary"
```

## Findings and rework

| Finding ID | Severity | Trigger / location | Expected vs actual | Owner | Resolution and evidence |
|---|---|---|---|---|---|
| R1-01 | medium | `apps/mobile/package.json:29`; Sources; B0 gate `2026-08-01-b0-gate.md:762-764` pinned 19.2.4 as «the version Expo SDK 57 expects», taking `expo install --check` from exit 1 to 0 | Expected: the record cites that decision and shows its gate still passes. Actual: not cited; Sources implied Expo names no version; the check not run | coordinator | Expo SDK 57 expects `~19.2.4`, which 19.2.18 satisfies; `expo install --check` exits 1 identically on base and head without flagging `@types/react` (row 9, criterion 8). B0 cited in Read context and Sources |
| R1-02 | medium | `forced-fix.tsc`, `landing-hoist-18.txt`, `mobile-19.2.18.txt` empty; the recorded script did not produce the saved files | Expected: exit codes and a positive control. Actual: an empty file is ambiguous | coordinator | v2 records install and `tsc` exit codes, output bytes and the `@types/react` copies in the landing program, forced and control, on both commits; the record carries v2 verbatim (row 6, criterion 5) |
| R1-03 | low | Row 4; criterion 2 | Actual: «only peer-suffix renames» and «656 other lines» | coordinator | Row 4 lists the three kinds of change; criterion 2 says the key comparison strips peer suffixes |
| R1-04 | low | `dev008-typecheck-force.txt:1` warns that the `pnpm` field is not read | Actual: pnpm version of local commands unrecorded | coordinator | Row 4: pnpm 11.9.0 shim delegating to 9.12.0; v2 summaries print `pnpm=9.12.0` |
| R1-05 | low | «What is not true»; BL-083 | Actual: the `react` / `react-dom` duplicate (19.2.3 mobile, 19.2.8 web) unnamed; guard limited to `@types/*` | coordinator | Both name it; BL-083's guard covers packages that web and mobile importers both reach |
| R1-06 | low | Criterion 6 | Actual: three green runs read as proof although (2/3)³ ≈ 30% of broken trees would pass | coordinator | Criterion 6 is a CI smoke check; determinism rests on criteria 2 and 5 |
| M1-01 | info | `apps/mobile/package.json` | `expo install --check` flags nine outdated Expo / React Native packages on the baseline already | coordinator | Out of scope; «What is not true» |
| M1-02 | minor | Sources; BL-083 | Actual: `bundledNativeModules.json` cited as Expo's source; `expo install --fix` given as the way versions split | coordinator | Sources cite the versions API and `@expo/cli` 57.0.11; BL-083 names a hand pin or an `expo install @types/react` that writes `~19.2.4` |
| Q1-01 | info (record only) | Progress row 4, `react-server-dom-webpack` count | Actual: «28 matching lockfile entries» with no command; QA's counting methods gave 50, 35, 32, 16, 8 or 5, equal on base and head | coordinator | Row 4 names the `grep -cF` command and its counts |
| Q1-02 | info (record only) | Forced-order v2 script, `O=` assignment | Actual: run by a relative path, outputs scatter into the checkout after `cd` and «tracked-tree clean» counts them | coordinator | `O` resolved with `cd … && pwd`, in the record and the scratchpad script |

Rework count and hypothesis changes: the first hypothesis, that pnpm's hoist is a deterministic function of the lockfile, was dropped after reading `hoistGraph`'s input order (Progress row 3). Review round 1's findings were applied as stated fixes; no rework round used.

## What is not true after this task

- **pnpm 9.12.0's order-dependent private hoist is still there.** Any package that reaches two versions in the lockfile and is reached through the hoist can do the same. Nothing checks that the lockfile keeps one version; BL-083 files a guard.
- **`react` and `react-dom` are still two versions:** 19.2.3 in `apps/mobile`, 19.2.8 in the web apps. The root declares neither, so which `react` the private hoist holds still depends on install order; a web dependency that imports `react` without declaring it could load the other copy at runtime. Not observed; BL-083.
- **Packages still import `@types/react` they do not declare.** `next`, `lucide-react`, `framer-motion` and `@tanstack/*` reach it through the hoist; with one version that is harmless, not removed.
- **No `pnpm.overrides` was added.** A hand pin in `apps/mobile`, or an `expo install @types/react` that writes Expo's `~19.2.4`, can split the version again.
- **`expo install --check` fails on the baseline and on this branch alike** (exit 1): `expo` 57.0.9 against `~57.0.22`, `react-native` 0.86.2 against `0.86.3` and seven more Expo packages. B0's exit 0 of 2026-08-01 no longer holds; not addressed here.
- **Criterion 6 was met only in its revised form.** The owner counted four green runs on documentation-only-different heads; no head got three runs, and every later CI run on this account is red until October 2026 because Actions does not start jobs.
- **The unforced variance was not observed.** 60 Linux samples and 6 local installs all hoisted 19.2.18; the failure was reproduced by forcing the order (criterion 5).
- **Green CI runs do not prove the flake gone** (criterion 6): at the observed rate a still-broken tree would pass three runs about 30% of the time.
- **pnpm is not upgraded,** and the «pnpm v10 installation layout» warning from `pnpm/action-setup` remains; it did not cause this.
- **`apps/landing`'s `incremental: true` and `.next/types/**/*.ts` include are unchanged;** they did not cause this.
- **No test suite ran locally** (root `AGENTS.md`); CI's `verify` runs them. `pnpm --filter @goproceed/mobile qa` (gp-mobile MA-5, optional), expo-doctor and a device or EAS build were not run; the change is types-only for `apps/mobile`.

## Acceptance evidence

| Criterion | Required? | Checked revision | Command or evidence | PASS / FAIL / NOT RUN | Limitation |
|---|---|---|---|---|---|
| 1. The cause is identified with evidence: CI log, an exact local reproduction, and the pnpm code path | yes | `001b21a` (reproduction), pnpm 9.12.0 | Progress rows 1–3, 6; confirmed by `gp-reviewer` (row 8) | PASS | assisted: row 2 flipped the hoisted symlink by hand; row 6 reproduces the failure through an unmodified install by forcing the order with FIFOs |
| 2. The lockfile holds one `@types/react` version, and no other package version changes | yes | `5a38091` | `grep -oE "@types/react@19\.[0-9.]+" pnpm-lock.yaml \| sort \| uniq -c` → `938 @types/react@19.2.18`; package and snapshot key sets, peer suffixes stripped, diffed against `7b57784`: only `@types/react@19.2.4` removed; one specifier change; the other changes are listed in row 4 | PASS | — |
| 3. A clean `pnpm install --frozen-lockfile` succeeds on the committed lockfile | yes | `5a38091` | local, after removing every `node_modules`: exit 0; `node_modules/.pnpm/@types+react@19.2.18` is the only copy; again in `fo2-fix.summary` | PASS | — |
| 4. `pnpm turbo run typecheck` passes for all ten packages without cache | yes | `5a38091` | `pnpm turbo run typecheck --force` (pnpm 9.12.0): `Tasks: 10 successful, 10 total`, `Cached: 0 cached` | PASS | macOS, Node 24.18.0 |
| 5. The failure depends on importer order on the baseline and not on the fix | yes | `001b21a`, `5a38091` | Progress row 6 (v2): forced order → baseline hoists 19.2.4, landing `tsc` exit 2 with CI's 17 errors, two `@types/react` copies in the program; fix hoists 19.2.18, exit 0, one copy; controls one copy on both | PASS | assisted: the order was forced with FIFOs on macOS; the unforced variance was not observed (60 Linux samples, row 5; 6 local installs) |
| 6. CI smoke, as revised by the owner on 2026-09-14: `verify` and `app-qa` pass in at least three CI runs on heads equal to `5a38091` in every fix-bearing path (`apps`, `packages`, `pnpm-lock.yaml`, `package.json`, `turbo.json`, `.github`). Originally: on the PR head, in at least three runs | yes | `953310b`, `d4d8672`, `22bb1fe`, `3a9ea42` | Green runs 34870624343 (`953310b`), 34871928171 (`d4d8672`), 34871968417 (`22bb1fe`), 34872695375 attempt 1 (`3a9ea42`), observed by `gp-qa` (row 13); each head equal to `5a38091` on those paths | PASS | The original wording was not met: GitHub Actions stopped starting jobs (billing, until October 2026), so the head got one executed run of three. A smoke check, not proof of determinism (R1-06) |
| 7. `pnpm validate:canonical-docs` and `pnpm validate:agents` pass | yes | working tree after this record | `node scripts/validate-canonical-docs.mjs` → «canonical documentation: OK»; `python3 scripts/sync-agents.py --check` (Python 3.12.4) → «Verified 16 host profiles from 8 canonical roles» | PASS | — |
| 8. Expo's dependency check does not flag `@types/react` 19.2.18 and does not change between baseline and fix | yes | `001b21a`, `5a38091` | `CI=1 ./node_modules/.bin/expo install --check` in `apps/mobile`: exit 1 on both with identical output, no `@types/react` line; SDK 57 range `~19.2.4` from `https://api.expo.dev/v2/versions` | PASS | known-red baseline: nine outdated Expo and React Native packages (M1-01) |

## Sources

- pnpm 9.12.0, installed through `packageManager` (`~/Library/pnpm/store/v11/links/@/pnpm/9.12.0/…/node_modules/pnpm/dist/pnpm.cjs`), read 2026-09-14: `readLocalConfig` `:22364`; `getHoistedDependencies` `:140710–140745`; `hoistGraph` `:140788–140815`; `filterLockfileByImportersAndEngine` `:140456–140489`; `headlessInstall` `:179002`, `:179062–179063`, hoist call `:179172`; recursive install `mutatedImporters` inside `Promise.all` `:197975`, `:198013–198075`; `findPackages` sorts projects lexically `:50395–50420`.
- TypeScript 5.9.2 (root `devDependencies`), `tsc --listFilesOnly --explainFiles` on `apps/landing`.
- GitHub Actions logs: runs 34856425465 (attempts 1 and 2) and 34854798473, and the diagnostic run 34869969752, repository `akisly/go-proceed`, read 2026-09-14.
- Expo SDK versions API, `https://api.expo.dev/v2/versions`, `sdkVersions["57.0.0"]`, read 2026-09-14T16:50Z (live data, no publication date): `relatedPackages["@types/react"]` `~19.2.4`.
- `@expo/cli` 57.0.11 (installed): `build/src/start/doctor/dependencies/getVersionedPackages.js:44-80` (remote list preferred over the bundled one) and `validateDependenciesVersions.js:266-279` (`semver.satisfies`); per `gp-mobile`, read 2026-09-14.
- `expo@57.0.9` `bundledNativeModules.json`: pins `react` 19.2.3 and `react-native` 0.86.2 and has no `@types/react` entry; it is not where `expo install --check` takes the `@types/react` range from.
- `react-native@0.86.2` `package.json` `peerDependencies` `@types/react ^19.1.1` (optional), read 2026-09-14.
- Expo SDK 57 reference, `https://docs.expo.dev/versions/v57.0.0/` (modified 2026-07-29, per `gp-mobile`), read 2026-09-14: React Native 0.86, React 19.2.3; no `@types/react` version.
- `docs/superpowers/plans/evidence/2026-08-01-b0-gate.md:762-764`: the 19.2.4 pin and its `expo install --check` exit 0.

## Completion / handoff

- Changed / inspected files: `apps/mobile/package.json`, `pnpm-lock.yaml`, this record, `docs/tasks/README.md`, `docs/BACKLOG.md`.
- Review independence: independent — `gp-reviewer` and `gp-mobile` (round 1) and `gp-qa` (round 1 and a narrow re-check) as native subagents.
- Verified scope: criteria 1–5, 7 and 8 on `3a9ea42`; criterion 6 as revised by the owner; every review and QA finding fixed.
- Remaining risks / blocked requirements: none required; «What is not true» above, and BL-083.
- Next bounded action and owner: owner — review and merge PR #89.
- Final state and reason: done — every required criterion passes for the revised scope; the owner revised criterion 6 on 2026-09-14.
