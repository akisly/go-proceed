# DEV-007 — Design sources of truth and the plan's remaining tails

## Assignment

- **Objective and user-visible outcome:** one normative design source, `DESIGN.md` plus `docs/design/`, with nothing in the tree that a skill or a reader can mistake for another. Stale design memory files and duplicates leave the tree, each with its restore command. The reference folders say what is live and what is history. The prospecting work in `outputs/` stays, and is described and reviewed for personal data.
- **State:** done
- **Coordinator:** primary Claude Code session, 2026-09-14.
- **Execution mode:** independent subagents for the required stages, as native `gp-*` agent types.
- **Selected route and why:** agent instructions (`docs/design/02-building-ui.md`), executed validator code, and documentation → coordinator → `gp-reviewer` → `gp-qa` (`agents/COORDINATION.md`), with `gp-researcher` and `gp-security` as the plan orders.
- **Triggered stages and why:**
  - `gp-researcher`: the plan requires it before `.impeccable/` is deleted — whether the installed Impeccable needs a sidecar regenerated from `DESIGN.md`.
  - `gp-security`: the plan requires a read-only personal-data review of `outputs/`, which the security trigger «retention and deletion of personal data» covers. It deletes nothing; its findings become backlog entries.
  - `gp-architect`, `gp-ui-reviewer`, `gp-mobile`: not triggered. No schema, contract, catalog, RLS or auth path, and no path under `apps/landing`, `apps/app/app`, `apps/app/src/components`, `apps/mobile/src`, `packages/ui`, `packages/tokens` or `technical/copy-catalog.csv` is touched.
- **Owning module and allowed edit paths:** the plan's PR-E targets (`.impeccable/`, `.interface-design/system.md`, `sources/`, `output/`, `design-qa.md`, `.gstack/security-reports/`, `design-references/README.md`, `design-references/evidence-atlas/README.md`, `docs/design/2026-08-19-design-system-rewrite-plan.md`, `outputs/README.md`); `docs/reviews/security/`; `.gitignore`; `docs/design/02-building-ui.md` and `docs/design/03-ui-references.md` (pointers to removed files); `scripts/validate-canonical-docs.mjs` (exemptions and comments naming moved or removed paths); `docs/BACKLOG.md`; `docs/STATUS.md`; this record; `docs/tasks/README.md`.
- **Read context:** the approved migration plan (PR-E, owner-approved 2026-09-13, outside the repository); root `AGENTS.md`; `START_HERE.md`; `agents/COORDINATION.md`; `docs/README.md` «Status meanings»; `docs/design/02-building-ui.md`; [DEV-006](DEV-006-freeze-todos.md).
- **Linked spec, ADR or earlier task:** [DEV-006](DEV-006-freeze-todos.md); DEV-001's owner decision to keep `outputs/`.
- **Baseline:** `d8a860a` (main, «Merge pull request #87»).
- **Dependencies / constraints / out of scope:**
  - `outputs/` is kept (owner, 2026-09-13); nothing in it is moved, redacted or deleted.
  - `.gitignore` keeps `.superpowers/` and `.gstack/`.
  - `DESIGN.md`'s own residuals (BL-060) and the visual-directions README (BL-060) are not fixed here.
  - Files under `packages/tokens` (including the generator of `01-tokens.md`) are not edited.
  - No test suite runs.
- **Required acceptance criteria:** see Acceptance evidence.
- **Skipped stages and rationale:** as «Triggered stages».

## Owner decisions

| Date | Decision | Source |
|---|---|---|
| 2026-09-13 | PR-E scope as the approved plan states it; `outputs/` is kept | Owner, in conversation (plan; DEV-001) |
| 2026-09-14 | Start DEV-007 after #87 merged | Owner, in conversation |
| 2026-09-14 | Owner question 1: keep the colours from `design-references/contest-2026-09/`, and make https://parlo-black.vercel.app/ the landing's reference | Owner, in conversation |

## Plan

1. Inventory every PR-E target at the baseline: tracked files, bytes, last commit, and who reads each (docs, scripts, CI, skills). Check: `git ls-files`, `git grep`.
2. Research and review in parallel: `gp-researcher` on `.impeccable/`, `gp-security` on `outputs/`.
3. Move each live ruling of `.interface-design/system.md` to a live home (`02-building-ui.md` or a backlog entry), then remove the file; remove `sources/`; record restore commands.
4. After `gp-security` returns: remove `output/` (and ignore it) and `design-qa.md`; move the 2026-07-30 `/cso` report to `docs/reviews/security/`, and change the validator's exemptions without moving any cited validator line.
5. After `gp-researcher` returns: `.impeccable/` as it recommends.
6. Banners and READMEs: `design-references/README.md`, `evidence-atlas/README.md`, the rewrite plan to Historical with a backlog entry for the ADR, `outputs/README.md`, the STATUS «Outreach» row.
7. Checks, `gp-reviewer`, rework, `gp-qa`, the PR, CI.

## Progress and decisions

| Order | State or role | Decision / result | Evidence / reference | Next action |
|---|---|---|---|---|
| 1 | scoped (coordinator) | #87 merged as `d8a860a` (2026-09-14 11:32 UTC); branch `claude/design-sources` from `origin/main`. **Inventory at the baseline** (tracked files, bytes, last commit): `.impeccable/` 2, 31,890, `bbfc705`; `.interface-design/system.md` 1, 20,773, `db80308`; `sources/` 1, 12,412, `bbfc705`, byte-identical to `docs/discovery/research-ua-demand-2026-08-21.md` (`cmp`); `output/` 64 (62 PNG screenshots and one WebM recording from Playwright, one Impeccable brief; corrected in Progress row 5), 17,221,054, `bbfc705`, read by nothing; `design-qa.md` 1, 2,804, `8de7f8b`, absolute paths into `/tmp` and `~/.codex`; `.gstack/` 1 (the 2026-07-30 `/cso` JSON), 8,042, `4d511c3`, tracked although `.gitignore` ignores `.gstack/`; `design-references/` 31, 17,973,028; `outputs/` 250 in one session directory, 312,542,484, all added in `bbfc705`, a commit titled as a landing table fix. There is no top-level `evidence-atlas/`: the plan's banner target is `design-references/evidence-atlas/README.md`. The repository is private (`gh repo view`) | `git ls-files`; `wc -c`; `git log -1 -- <path>`; `cmp` | Research and review |
| 2 | scoped (coordinator) | **Deviation from the plan: `design-references/` is not all history.** `brand/*.svg` is the live source of every icon (`scripts/generate-brand-icons.mjs:2`, `:11`); `contest-2026-09/daylight/` is the prototype the landing reproduces (`apps/landing/app/layout.tsx:76`) and a motion token cites (`packages/tokens/src/tokens.json:842`); `evidence-atlas/assets/` is read by `apps/landing/qa/grounds.mjs:36`. The new README names each folder's standing instead of one «historical exploration» banner. **`.interface-design/system.md`:** already marked superseded on 2026-09-05; the vendored `interface-design` skill treats the file as decisions made (`.agents/skills/interface-design/SKILL.md:288`). Its live rulings were checked against the rules: the §6 traps and «assert the rendered result» are in `02-building-ui.md` §8; §5 «WorkRegister» is BL-054, which now carries the ruling; the Button 44px floor and base-layer density are enforced in `packages/ui`; the Tooltip ruling described a rail that no longer exists. *(Two of these were wrong, corrected after review round 1, R1-02: the icon band exists, and its two rulings moved to BL-053; `packages/ui/src/base.css` sets no body font size, the density sizes are the token rows `01-tokens.md:268-269`, and the base-layer mechanism named was `apps/demo`'s `.goproceed-app`, gone with `apps/demo`.)* Still to move: §8's «QA hooks are `data-*` attributes; an assertion names the contract», into `02-building-ui.md` §8 after `gp-researcher` has read that file. **Rewrite plan:** to Historical, since D1–D7 were never recorded in an ADR (§12 P0) and Daylight departed from them; BL-078 tracks the ADR | `git grep` per path; `02-building-ui.md` §8; BL-054 | Implement the independent parts |
| 3 | implementing (coordinator) | `gp-researcher` (`.impeccable/`) and `gp-security` (`outputs/`, plus the `/cso` report, `design-qa.md` and `output/`) launched in parallel. Applied the parts neither reads: `sources/` and `.interface-design/system.md` removed; `03-ui-references.md:5` points at `DESIGN.md` with a dated bracket (same line); the validator's comment names the removal (same line); the rewrite plan's Status is Historical, under a banner; `design-references/README.md` added; the evidence-atlas README carries a banner; BL-054, BL-060 and the new BL-078 updated. `output/`, `design-qa.md`, the `/cso` report and `.impeccable/` wait for the two stages | `git status`; validator | Wait for the stages |
| 4 | researching (`gp-researcher`, native) | **Delete both `.impeccable/` files; regenerate nothing.** The installed Impeccable is the vendored v4.0.3 (`.agents/skills/impeccable/SKILL.md:4`). Nothing requires `design.json`: its absence raises no boot or `doctor` finding (`scripts/lib/staleness.mjs:248`), and it is optional extension data a model writes by hand (`reference/document.md:253`, `:383`). Kept, it is harmful to the one use `02-building-ui.md:105-107` allows, a critique pass: critique's detector adds the sidecar's 26 Evidence Atlas colours and their ramps to its allowlist (`detector/design-system.mjs:271-280`, `:432`), hiding hard-coded colours of the superseded palette. The staleness notice compares file times, not content. The critique snapshot is a report except that `polish` reads the latest one for the same file (`reference/polish.md:29-35`). Upstream `main` is v4.3.1; its docs agree on these points, its scripts were not read | Researcher report; Sources | Remove `.impeccable/`; move the `data-*` ruling |
| 5 | reviewing (`gp-security`, native), personal-data review of `outputs/` | **Personal data of natural persons is tracked, against the project's own rule; no credentials; nothing deploys or uploads it.** Majors: S1-01, buyer-side contact persons in the five raw ProZorro search dumps; S1-02, sole traders under personal names with ten-digit identifiers. Mediums: S1-03, A1-N01 outreach routes called corporate while most are free-mail or mobile; S1-04, private customers named in copied tender titles; S1-05, doc 40 §B.5 (live through `.gitignore`) keeps lead data out of git history, with retention and erasure a tracked copy cannot honour; S1-09, nothing stops a repeat. Lows: S1-06, absolute home-directory paths in 34 scripts and `design-qa.md`; S1-07, the `/cso` report is safe to move and must stay exempt from the rename guard; S1-08, `output/` safe to remove. **Coordinator's re-run at `d8a860a`, with its own method:** 6,371 `contactPoint` objects in the five `search_hits` files, each with a name and an email, 2,444 distinct name–email pairs, 4,760 on a fifteen-domain free-mail list (`gmail.com`, `ukr.net`, `i.ua`, `meta.ua`, `yahoo.com`, `outlook.com`, `hotmail.com`, `bigmir.net`, `email.ua`, `rambler.ru`, `mail.ru`, `yandex.ru`, `icloud.com`, `online.ua`, `ua.fm`; the review: 4,699 on its list); 9,788 ten-digit `edrpou` values in 52 files (as reviewed); 101 «Замовник: surname initial.» matches in 22 files, with `Замовник(?:и)?:\s*[А-ЯІЇЄҐ][а-яіїєґ'’]+\s+[А-ЯІЇЄҐ]\.` over the session's text files (the review: 99); 7 A1-N01 recipient addresses, 6 free-mail (as reviewed); 34 files with `/Users/` paths (as reviewed); `output/` is 62 PNG, 1 WebM and 1 Markdown file, so Progress row 1's «63 screenshots» was wrong. **Decisions:** S1-01, S1-02 and S1-05 become BL-079 (P1, the owner's decision on how `outputs/` is kept); S1-03 and S1-04 BL-080 (P2); S1-09 BL-081 (P2). S1-06 is accepted for `outputs/` as a dated record, the rename guard's existing reasoning, and is moot for `design-qa.md`, which leaves the tree. S1-07 and S1-08 are carried out here. The majors are deferred to the owner, not fixed, so there is no fix for `gp-security` to re-check | Security review report; the coordinator's count script | Carry out S1-07 and S1-08; write BL-079 to BL-081, `outputs/README.md`, STATUS |
| 6 | implementing (coordinator) | S1-07 and S1-08 carried out: `output/` removed and ignored (`/output/`); `design-qa.md` removed; the `/cso` report moved to `docs/reviews/security/2026-07-30-cso.json`. The validator's two `.gstack` exemptions went, replaced in place so no line moved (2,167 lines before and after; its lines cited elsewhere, `:169-179` and `:172`, stay put): the exported predicates now exempt the moved report from both the rename and the retired-workflow guards through `docs/reviews/`, and exempt neither the old path nor `.gstack/x.md`. BL-079 to BL-081 written. `outputs/README.md` sorts all 250 session files into five groups; the first grouping left 59 files unplaced, and the script's completeness assert stopped it before it wrote anything. STATUS: the «Agent workflow» row re-observed at `d8a860a` (`git log --merges --oneline -10` ends at #79), the «Outreach» row's last sentence and «Next action» 1–2 rewritten in place (52 lines). Checks: every remaining mention of a removed path is in a record (the frozen files, `docs/superpowers/`, `docs/legacy/`, `migration/`, DEV-005), in a dated note this task wrote, or in the Historical rewrite plan; the relative links of the five edited or new documents resolve; `scripts/validate_package.py` requires only the four concept PNGs, which stay; `git diff --name-status d8a860a -- outputs` shows only the README added | Acceptance evidence | Commit; `gp-reviewer` |
| 7 | reviewing (`gp-reviewer`, native), round 1 on `7245fcf` | **Changes requested**, no blocker. Medium: R1-01, `02-building-ui.md` §3.1, §7.3 and its Companions line still send agents into the rewrite plan, now Historical (its banner says take no value from it); R1-02, Progress row 2 and criterion 2 were wrong that the icon rail is gone: `apps/app/src/components/dash-shell/sidebar.tsx` has a 768–1240 icon band, and two rulings (the accessible name never depends on the tooltip, which mounts only in that band; the active bar is absolutely positioned) now live only in the Historical plan. Low: R1-03, the evidence-atlas banner denies the crop rules `apps/landing/qa/grounds.mjs` obeys, and the assets are that script's source masters; R1-04, «may inform a screen» still lets `interface-design` propose a competing direction; R1-05, a validator comment still points at the moved `/cso` directory «above»; R1-06, BL-079 and BL-080 do not belong in STATUS's «Agent workflow» cell; R1-07, live QA harnesses do hook by class, against the new §8 bullet; R1-08, Impeccable writes `.impeccable/critique/` again on any permitted critique, unignored; R1-09, BL-079 to BL-081 lack «ranked by DEV-007»; R1-10, the live `brand/` icon sources sit inside the rename-guard exemption. Found correct: no live reader of a removed path; the other `system.md` rulings' homes; the `design-references/` deviation; the Historical status and BL-078's facts; the validator's syntax and exemptions; `02-building-ui.md`'s cited lines; `outputs/` handling and STATUS's line count; scope | Review round 1 report | Rework |
| 8 | rework (coordinator), after review round 1 | All ten findings applied as their stated fixes; not a rework round, since no QA FAIL preceded it. Facts checked first: `sidebar.tsx` has the `rail-icons` band, disabled buttons with `aria-label`, no Tooltip and no active bar yet, so BL-053, which tracks those items becoming links, is where the two rulings apply (R1-02); `packages/ui/src/base.css`'s `body` sets no font size (R1-02, reviewer's unconfirmed point); `packages/ui/src/motion/index.ts`'s header counts and names the vocabulary (R1-01); `grounds.mjs:1-2`, «the route cards' photographic grounds» (R1-03); the class hooks at `landing.mjs:151`, `:255-256` and `field.mjs:3353` (R1-07); `git log --merges --oneline -10 origin/main` ends at #79 because it includes the non-PR merge `6349dd2`, so STATUS's `-10` holds (the reviewer's doubt). After the fixes: `02-building-ui.md` changes only lines 21, 74, 107, 309 and 363 against `7245fcf` and keeps 407 lines, lines 193–216 identical to `d8a860a`; STATUS 52 and the validator 2,167 lines; `git check-ignore` matches `.impeccable/critique/`; the validator is green | Findings; criteria 2, 9 | `gp-qa` |
| 9 | verifying (`gp-qa`, native), QA round 1 on `8ed6a7e` | **Pass with two low record findings.** Criteria 1–11 PASS on QA's own runs: every restore command run in a scratch worktree (each staged result identical to `d8a860a`); the three path predicates imported; seven planted negatives (a live file naming a pre-rename role and the report back at its old path fail; BL-079 without Resume or with State `parked` fails; «Run /cso» fails in a live doc and passes under `docs/reviews/security/`; a pre-rename role name under `brand/` passes, as disclosed); Progress row 1's inventory reproduced exactly; row 5's counts reproduced except two whose method the record did not state (QA: 4,766 free-mail on its own domain list; 80 «Замовник» matches in 22 files with its own pattern). R1-01 to R1-10 verified, and `7245fcf..8ed6a7e` holds only those fixes and bookkeeping. Q1-01: the moved report's undo `git mv` fails without the target directory. Q1-02: the `output/` restore re-stages the files, so no `git add -f` is needed. Both fixes were tested by QA | QA round 1 report | Fix Q1-01, Q1-02 and state the two count methods; PR |
| 10 | verifying (`gp-qa`, native), narrow re-check on `6377514` | **Verified.** Q1-01 and Q1-02 read as QA tested them; the stated domain list and regex reproduce 4,760 free-mail entries (every entry, not distinct addresses) and 101 matches in 22 files; `8ed6a7e..6377514` changes only this record; both validators green. Q2-01 (low, record text): criterion 13's Limitation said the later commit changed the task index, which `6377514` did not. PR #88 was opened on `8ed6a7e` before the record commit existed: a failed validator stopped the commit, and the push and `gh pr create` in the same script ran anyway; `6377514` was pushed to it | QA narrow re-check report; `gh pr view 88` | Read CI `verify`; close the record |
| 11 | scoped (coordinator), after the owner's answer to question 1 | **Reading of the decision:** `contest-2026-09/` stays as the source of the landing's colours, which `DESIGN.md`'s Daylight palette already carries; https://parlo-black.vercel.app/ replaces it as the reference for the landing's structure, composition and motion. Observed on 2026-09-14: «Parlo — Autonomous Support Messaging», a dark page whose first screen is a perspective grid under a headline revealed word by word; its colours are not the reference. **Scope kept to documentation:** the `design-references/README.md` row, a backlog entry for the landing slice that rebuilds against the new reference (a UI change on the `02-building-ui.md` route with `gp-ui-reviewer`), STATUS's «Landing» row, and a dated note where a live document names the prototype as the landing's reference. The shipped landing, `apps/landing/app/layout.tsx`'s parity note and the `stately` token ruling describe what ships and are left to that slice. These edits come after QA verified the task, so `gp-reviewer` and `gp-qa` re-check them | Owner decision; Browser pane, 2026-09-14 | Implement |
| 12 | implementing (coordinator) | The owner's decision recorded: `design-references/README.md`'s `contest-2026-09/` row now reads «source of the landing's colours», names the new reference and BL-082; `03-ui-references.md`'s landing Status line carries a dated bracket on the same line; BL-082 (P2, open) files the rebuild; STATUS's «Landing» Open cell and its preamble sentence name the decision in place. STATUS keeps 52 lines and `03-ui-references.md` 162; the validator is green | `git diff 6377514` | `gp-reviewer` narrow re-review |
| 13 | reviewing (`gp-reviewer`, native), narrow re-review of `6377514..52563e6` | **Changes requested**, no blocker. Medium: N1-01, the `03-ui-references.md` bracket and BL-082 credit the owner with «structure, composition and motion», which is DEV-007's reading, not the owner's words; N1-02, that reading silently settles choices only the owner can make and that collide with the approved `DESIGN.md` (a perspective-grid first screen against «don't build a 3D scene»; which ground, dark or paper; typography; how closely to match; the motion rules tied to the parity spec). Low: N1-03, BL-082's «three live files» undercounts the prototype's citations, notably the composition and motion rulings in `tokens.json`; N1-04, `02-building-ui.md:74` still sends a landing builder to the parity spec with no word of the decision; N1-05, BL-082's wording contradicts itself, overgeneralises 03's licence practice, has a circular «Depends on» and an unattributed observation; N1-06, the record's criteria 4, 12, 13, Completion, «What is not true» and Q2-01 are stale for the new state. Found faithful: the Owner decisions row, the answered question, the README standing, STATUS; the boundary of leaving `layout.tsx` and the token files untouched; BL-082's form; line counts | Narrow review report | Fix N1-01 to N1-06 |
| 14 | rework (coordinator), after the narrow re-review | N1-01 to N1-06 applied as their stated fixes; not a rework round, since no QA FAIL preceded it. Facts re-run first: `DESIGN.md:158` (Onest), `:172-174` (scroll-linked compositions per the landing spec), `:381-386` (no 3D scene; «точь-в-точь»); `02-building-ui.md:185-191`, `:197-203`, `:349`; `grep -n -i prototype packages/tokens/src/tokens.json` gives 31 lines, 11 colour rulings (`:26`, `:86`, `:96`, `:106`, `:116`, `:136`, `:156`, `:196`, `:306`, `:406`, `:1339`) and 20 others (type `:592`–`:724`, radius `:750`, width `:812`, durations `:842`, `:846`, springs `:896`, `:900`, shadows `:993`, `:1018`, heights `:1386`, `:1406`), of which only `:842` cites the path. The owner's scope questions (a)–(e) go to BL-082's plan, not to this task | Findings N1-01 to N1-06 | `gp-qa` narrow re-check |
| 15 | verifying (`gp-qa`, native), narrow re-check of `6377514..aa09cdc` | **Pass.** Every statement of the decision gives only the owner's two clauses as the owner's and marks the rest as DEV-007's reading; N1-01 to N1-06 in place and nothing beyond them; BL-082's counts and cited lines reproduced (31 lines, 11 colour at the listed lines, 20 others, only `:842` citing the path) and its form valid; STATUS 52, `03-ui-references.md` 162, `02-building-ui.md` 407 with `:193-216` identical to `d8a860a`; validators green. Q3-01 to Q3-04 (low, record text): Completion's verified scope and independence lines, Q2-01's fix column after N1-06, «What is not true» calling all 20 rulings composition and motion, and the rework note omitting N1. Two notes left unapplied, since they would change documents after this verdict: the README's «measured from it» is exact for most of the 31 rulings but not `:106` or `:592`; BL-082 does not name the second generated mirror, `packages/tokens/src/tokens.dtcg.json` | QA narrow re-check report | Fix Q3-01 to Q3-04 in the record; read CI `verify` on `aa09cdc` |
| 16 | done (coordinator) | Q3-01 to Q3-04 fixed in record text. PR #88's CI green on its head `aa09cdc` (criterion 14): `verify` and `app-qa` succeeded, and the three Vercel checks pass; the earlier head `6377514` was also green (run 34853193247). Every required criterion passes; merging is the owner's decision. Open for the owner: BL-079 (owner question 2) and the plan of BL-082 | GitHub Actions run 34854798473; `gh pr view 88 --json statusCheckRollup` | None for this task |

## Findings and rework

| Finding ID | Severity | Trigger / location | Expected vs actual | Owner | Resolution and evidence |
|---|---|---|---|---|---|
| R1-01 | medium | `02-building-ui.md` §3.1 (`:73-74`), §7.3 (`:309`), Companions (`:21`) | Expected: an Approved procedure sends no reader into a Historical document. Actual: read «the plan's §9 / §10», and write a new primitive's what and why into «the plan's §8.3» | coordinator | `:74` names `DESIGN.md` and the 2026-09-05/06 landing specs, or `03-ui-references.md`, `04-role-pain-map.md` and the app migration spec; `:309` names the primitive's file header and `packages/ui/src/motion/index.ts`; `:21` marks the plan Historical. BL-078 names the generated `01-tokens.md` Companion line still pointing at the plan |
| R1-02 | medium | Progress row 2, criterion 2; the removed `system.md` §5 Tooltip and rail nav item | Expected: a ruling about a component that still exists keeps a live home. Actual: «a rail that no longer exists», while `sidebar.tsx` has the icon band; and «base-layer density enforced», while `base.css` sets no body size | coordinator | Row 2 and criterion 2 corrected; BL-053 carries both rulings for when the items become links; density sizes are the token rows `01-tokens.md:268-269` |
| R1-03 | low | `design-references/evidence-atlas/README.md:3`; `design-references/README.md` | Actual: the banner said nothing below is a current rule, while `grounds.mjs` obeys its crop rules and relies on «synthetic, no customer data»; the assets are that script's source masters | coordinator | Both say the assets are the source masters of the landing's route-card grounds, cut to the README's crop rules |
| R1-04 | low | `02-building-ui.md:107` | Actual: «may inform a screen» still let `interface-design` propose a competing direction | coordinator | Same line: domain exploration inside `DESIGN.md`'s direction only; adopt no palette, type or signature it proposes; decline the save |
| R1-05 | low | `scripts/validate-canonical-docs.mjs:133` | Actual: «the /cso reports directory above» | coordinator | «the dated /cso report below», same line |
| R1-06 | low | `docs/STATUS.md:26` | Actual: outreach decisions in the «Agent workflow» cell | coordinator | Removed; they stay in «Outreach» and «Next action» |
| R1-07 | low | `02-building-ui.md:363` | Actual: «never class names», while three live harness queries hook by class | coordinator | Same line names the existing class hooks; a new hook is `data-*` |
| R1-08 | low | `.gitignore` | Actual: a permitted critique writes `.impeccable/critique/` again, unignored | coordinator | `.impeccable/critique/` ignored |
| R1-09 | low | BL-079 to BL-081 | Actual: «The priority is the review's», where the preamble asks for «ranked by DEV-NNN» | coordinator | «Ranked by DEV-007 from the review's severity» |
| R1-10 | low | `design-references/README.md` | Actual: the live `brand/` sources sit inside the rename-guard exemption, unmentioned | coordinator | The README says so; «What is not true» lists it |
| Q1-01 | low (record only) | «Removed paths», the Moved line | Expected: the undo works. Actual: `git mv` into `.gstack/security-reports/` exits 128, the directory being absent | coordinator | `mkdir -p .gstack/security-reports && ` prefixed, as QA tested |
| Q1-02 | low (record only) | «Removed paths», the `output/` row | Expected: accurate re-tracking advice. Actual: «re-tracking needs `git add -f`», while the checkout re-stages all 64 files | coordinator | QA's wording |
| Q2-01 | low (record only) | Criterion 13's Limitation | Expected: it describes the later commit. Actual: «changes only its own text and the task index», while `6377514` left the index alone | coordinator | Superseded: N1-06 rewrote criterion 13's Limitation (QA Q3-02) |
| N1-01 | medium | `03-ui-references.md:139` bracket; BL-082 «Why» | Expected: the owner's terms, the coordinator's reading marked as a reading. Actual: «(owner, DEV-007): … taken for structure, composition and motion» | coordinator | The bracket quotes the owner's two clauses and says DEV-007 reads them as structure, composition and motion, confirmed in BL-082's plan; BL-082 says «DEV-007 reads the decision as» |
| N1-02 | medium | BL-082 | Expected: owner choices kept apart from facts, and the clash with `DESIGN.md` named. Actual: the reading settled the 3D first screen, the ground, typography, fidelity and the motion rules silently | coordinator | BL-082's «Depends on» lists owner questions (a)–(e); the slice amends `DESIGN.md` and the landing spec through the approval procedure before code |
| N1-03 | low | BL-082 «Why»; `design-references/README.md` | Actual: «three live files cite that prototype»; «one motion token cites it» | coordinator | BL-082 names `DESIGN.md`, `02-building-ui.md`'s lines, `layout.tsx` and the 31 token rulings split 11 colour / 20 other; the README says «cites it by path» and names the 31 |
| N1-04 | low | `02-building-ui.md:74` | Actual: the read order sent a landing builder to the parity spec with no word of the decision | coordinator | Same line: «A rebuild against the landing reference the owner set on 2026-09-14 follows BL-082» |
| N1-05 | low | BL-082 wording | Actual: contradictory «structure only», an overgeneralised licence claim, a circular «Depends on», an unattributed observation | coordinator | Reimplemented in token roles and `@goproceed/ui/motion`, copying nothing, licence to be recorded as 03's rows do; «Depends on» is the owner's answers; «the coordinator's browser observation» |
| N1-06 | low | This record | Actual: criteria 4, 12 and 13, Completion, «What is not true» and Q2-01 stale for the new state | coordinator | All updated; criterion 13 reopened for the new head |
| Q3-01 | low (record only) | Completion: verified scope, review independence | Expected: agreement with criteria 13 and 14. Actual: «criteria 1–13» while 13 was reopened; the narrow re-review and QA re-checks unnamed | coordinator | Both lines rewritten when criterion 13 was recorded |
| Q3-02 | low (record only) | Q2-01's fix column | Actual: it described a Limitation N1-06 had replaced | coordinator | «Superseded: N1-06 rewrote criterion 13's Limitation» |
| Q3-03 | low (record only) | «What is not true», the landing bullet | Actual: «20 composition and motion rulings», while BL-082 lists type, radius, width, motion, shadows and control heights | coordinator | «20 non-colour rulings (type, radius, width, motion, shadows, control heights)» |
| Q3-04 | low (record only) | «Rework count and hypothesis changes» | Actual: it covered R1 and Q1 only | coordinator | One sentence for N1 and Q3 added |

Rework count and hypothesis changes: review round 1's ten findings were applied as stated fixes; no rework round used. R1-02 came from reading a removed ruling against its component's name rather than the code that renders it; the fix read `sidebar.tsx` and `base.css` before writing. Q1-01 and Q1-02 changed record text only, after a QA pass on every criterion, with fixes QA had tested; not a rework round. The owner's landing-reference decision came after QA; the narrow re-review's N1-01 to N1-06 and the narrow QA re-check's Q3-01 to Q3-04 were applied as stated fixes, and no QA FAIL occurred, so no rework round was used.

## Owner questions

1. **`design-references/contest-2026-09/`.** The plan asks whether it is still current. The landing reproduces its Daylight prototype and a motion token cites it, so this task keeps it as a reference for the shipped landing. Is that the standing you want, or should it become history once the landing is the reference? **Answered 2026-09-14: keep its colours; the landing's reference is https://parlo-black.vercel.app/.**

2. **How `outputs/` is kept** (BL-079). It holds personal data of natural persons that the project's own rule keeps out of git history. The options the review lists: keep it with a recorded purpose, lawful basis and retention date; move it to private storage behind a pointer README; or redact the personal fields in place. Moving or redacting leaves the data in commit `bbfc705` unless history is rewritten, which is a further decision (force-push, every clone re-made).

## Removed paths and how to restore them

| Path | Why | Restore |
|---|---|---|
| `sources/research_ua_ptv_foreman_techsupervision_demand_2026-08-21.md` | Byte-identical duplicate of `docs/discovery/research-ua-demand-2026-08-21.md` | `git checkout d8a860a -- sources/` |
| `.interface-design/system.md` | Superseded 2026-09-05; a skill reads it as decisions made; its live rulings moved (Progress row 2) | `git checkout d8a860a -- .interface-design/system.md` |
| `.impeccable/design.json`, `.impeccable/critique/2026-08-26T08-27-14Z__apps-landing-components-visuals-readiness-map-tsx.md` | Evidence Atlas sidecar and a pre-Daylight critique; optional, and harmful to critique's detector (Progress row 4) | `git checkout d8a860a -- .impeccable/` |
| `output/` (64 files) | Playwright screenshots, one recording and an Impeccable brief that nothing reads (Progress row 1; review S1-08) | `git checkout d8a860a -- output/`; the checkout re-stages the files, and the ignore rule only hides new ones (QA Q1-02) |
| `design-qa.md` | The 2026-08-21 mark QA, which points at files in `/tmp` and `~/.codex` that no one else has (review S1-06); an older design QA snapshot stays at `docs/legacy/design-qa.md` | `git checkout d8a860a -- design-qa.md` |

**Moved:** `.gstack/security-reports/2026-07-30-120350.json` → `docs/reviews/security/2026-07-30-cso.json` (review S1-07). To undo: `mkdir -p .gstack/security-reports && git mv docs/reviews/security/2026-07-30-cso.json .gstack/security-reports/2026-07-30-120350.json` (QA Q1-01: `git mv` does not create the directory) and restore the validator's two exemptions from `d8a860a`.

## What is not true after this task

- **`outputs/` still holds personal data, in the tree and in history.** Nothing was redacted, moved or deleted; BL-079 and BL-080 are the owner's decisions. Every session that reads the directory, this task's security review included, sends what it reads to a model provider.
- **Nothing stops a repeat** (BL-081). `.gitignore` does not ignore `outputs/`.
- **`interface-design` can still recreate `.interface-design/system.md`** if a session accepts its save step. One sentence in `02-building-ui.md` §3.2 declines it; no check enforces that.
- **D1–D7 have no ADR** (BL-078). `02-building-ui.md` and the generated `01-tokens.md` still call the rewrite plan's §3 «the rulings», now inside a Historical document; `packages/tokens/scripts/generate-docs.mjs` was not edited.
- **The rewrite plan's banner moved its lines down by two.** No live file cites it by line; frozen `TODOS.md` does (`:225`, `:344`) and cannot be corrected.
- **Mentions of removed paths remain in records** and in the Historical rewrite plan; they name files that are gone, and the table above restores them.
- **`brand/`'s README row** says icons are rasterised from its files; `scripts/generate-brand-icons.mjs` names eight of the nine by file name, not `goproceed-mark.svg`.
- **`brand/` is unguarded.** The rename-guard exemption for `design-references/` covers the live icon sources; narrowing it was not in scope.
- **Existing QA harness hooks by class** (`landing.mjs`, `field.mjs`) stay; only new hooks follow the `data-*` ruling.
- **The runbook's line citations into `02-building-ui.md` (`:193-210` and its neighbours) and migration `0050`'s `validate-canonical-docs.mjs:239-251`** already pointed at other content before this task (reviewer's note); this task moved neither file's cited lines.
- **The vendored Impeccable is 4.0.3; upstream is 4.3.1.** Not upgraded; the researcher did not read the upstream scripts.
- **The landing still follows the contest prototype.** The shipped landing, `DESIGN.md`'s characteristics and Don't list, `02-building-ui.md`'s landing rules, `apps/landing/app/layout.tsx`'s parity note and 20 non-colour rulings in `tokens.json` (type, radius, width, motion, shadows, control heights) still reflect it; BL-082 holds the rebuild and the owner's scope questions (a)–(e).
- **Not re-observed:** every STATUS row except «Agent workflow»; the «Landing» row gained the owner's decision without re-observation; `DESIGN.md`'s residuals and the visual-directions README (BL-060).
- **No test suite, landing QA harness or typecheck ran.** No executed code changed apart from the validator's exemption lists and comments; `apps/landing/qa/grounds.mjs`'s inputs are untouched.
- **S1-06 is accepted** for the scripts in `outputs/` as a dated record.

## Acceptance evidence

| Criterion | Required? | Checked revision | Command or evidence | PASS / FAIL / NOT RUN | Limitation |
|---|---|---|---|---|---|
| 1. Every removed path had no live reader, and each has a restore command | yes | working tree over `d8a860a` | `git grep -F` per path: remaining mentions are records, dated notes of this task, or the Historical rewrite plan (Progress row 6); `scripts/validate_package.py` requires only the four concept PNGs; «Removed paths» table | PASS | — |
| 2. The live rulings of `.interface-design/system.md` have live homes | yes | working tree after review round 1 | §8 `data-*` ruling → `02-building-ui.md` §8; §5 WorkRegister → BL-054; §5 icon-band Tooltip and active bar → BL-053; §6 traps and «assert the rendered result» in `02-building-ui.md` §8; the 44px floor in `packages/ui/src/components/Button.tsx`; density sizes in `01-tokens.md:268-269` (Progress rows 2 and 8) | PASS | The first revision's evidence was wrong on two rulings (R1-02) |
| 3. `.impeccable/` handled as `gp-researcher` recommends, with sources | yes | working tree | Progress row 4; Sources | PASS | Upstream scripts not read |
| 4. `design-references/README.md` gives each folder the standing its readers show; the evidence-atlas README carries a banner | yes | working tree | Readers: `scripts/generate-brand-icons.mjs`, `apps/landing/app/layout.tsx:76`, `packages/tokens/src/tokens.json:842`, `apps/landing/qa/grounds.mjs:36`; relative links resolve (script) | PASS | Owner question 1, answered 2026-09-14; the new standing is Progress rows 11–14 |
| 5. The rewrite plan is Historical with a banner; BL-078 tracks the ADR; BL-060's item is closed | yes | working tree | Status line `Historical`; validator status enum green; BL-060 and BL-078 | PASS | — |
| 6. The `/cso` report is moved and stays exempt; the `.gstack` exemptions are gone; no validator line moved | yes | working tree | Exported predicates: `docs/reviews/security/2026-07-30-cso.json` role-exempt and retired-exempt `true`; the old path and `.gstack/x.md` `false`. `wc -l`: 2,167 before and after | PASS | — |
| 7. `output/` is ignored | yes | working tree | `git check-ignore -v output/playwright/x.png` → `.gitignore:114:/output/` | PASS | — |
| 8. `outputs/` is kept, described and reviewed; the review's findings are backlog entries | yes | working tree | `git diff --name-status d8a860a -- outputs`: only `A outputs/README.md`; 251 tracked files; README groups sum to 250; `gp-security` S1-01…S1-09 → BL-079 to BL-081 or carried out (Progress rows 5–6) | PASS | Owner question 2 |
| 9. Files cited by line keep their cited lines | yes | working tree | `wc -l` against `d8a860a`: STATUS 52, `03-ui-references.md` 162, validator 2,167 unchanged; `02-building-ui.md` 403 → 407, lines 193–216 identical to `d8a860a`; after review round 1 it differs from `7245fcf` only at `:21`, `:74`, `:107`, `:309`, `:363` | PASS | — |
| 10. `pnpm validate:canonical-docs` green | yes | working tree | `node scripts/validate-canonical-docs.mjs` → `canonical documentation: OK` | PASS | `node` run directly |
| 11. `pnpm validate:agents` green | yes | working tree | `python3 scripts/sync-agents.py --check` → 16 profiles verified | PASS | — |
| 12. Independent `gp-reviewer` with no unresolved finding | yes | `7245fcf` (round 1); `6377514..52563e6` (narrow re-review of the owner-decision edits) | Native `gp-reviewer`: round 1 changes requested, R1-01 to R1-10 applied as stated fixes; the narrow re-review changes requested, N1-01 to N1-06 applied as stated fixes (Findings) | PASS | Fixes are verified by `gp-qa`, not re-reviewed, as root `AGENTS.md` prescribes for stated fixes |
| 13. Independent `gp-qa` on the final revision | yes | `aa09cdc` | `gp-qa` round 1 on `8ed6a7e`: criteria 1–11 PASS on its own runs, seven planted negatives, R1-01 to R1-10 verified; narrow re-check on `6377514`: Q1-01 and Q1-02 verified; narrow re-check of `6377514..aa09cdc`: PASS, the owner-decision edits and N1-01 to N1-06 verified, Q3-01 to Q3-04 (record text) raised | PASS | Q3-01 to Q3-04 were fixed afterwards in record text, which QA said needs no further check |
| 14. CI `verify` green | yes | `aa09cdc` (PR #88 head) | GitHub Actions run 34854798473: `verify` SUCCESS, `app-qa` SUCCESS; the three Vercel checks pass | PASS | The later commit that records this result changes only this record and the task index |

## Sources

- Impeccable, vendored v4.0.3: `.agents/skills/impeccable/` in this repository (`SKILL.md:4`; `skills-lock.json` locks it from `pbakaus/impeccable`). Read 2026-09-14.
- [impeccable.style/docs/document](https://impeccable.style/docs/document/), [/critique](https://impeccable.style/docs/critique/), [/doctor](https://impeccable.style/docs/doctor/): no publication date shown; accessed 2026-09-14. They describe upstream `main`, v4.3.1 ([SKILL.md](https://raw.githubusercontent.com/pbakaus/impeccable/main/.agents/skills/impeccable/SKILL.md)). The installed version and the docs disagree on the version only; the points this task relies on match. Upgrading the vendored skill is not in scope; nothing in the product depends on it.

## Completion / handoff

- **Changed files:** see «Owning module and allowed edit paths» and «Removed paths».
- **Review independence:** native `gp-*` subagents: `gp-researcher`; `gp-security`; `gp-reviewer` round 1 on `7245fcf` and a narrow re-review of `6377514..52563e6`; `gp-qa` round 1 on `8ed6a7e` and narrow re-checks on `6377514` and of `6377514..aa09cdc`. The coordinator verified none of its own fixes as independent evidence.
- **Verified scope:** criteria 1–14; criteria 1–11 re-run independently by `gp-qa`.
- **Commits:** `7245fcf` (implementation), `8ed6a7e` (review round 1 fixes), `6377514` (QA round 1 record fixes), `52563e6` (the owner's landing-reference decision), `aa09cdc` (narrow review fixes), plus this bookkeeping commit. PR [#88](https://github.com/akisly/go-proceed/pull/88).
- **Remaining risks / blocked requirements:** «What is not true after this task»; owner question 2 (BL-079); BL-082's scope questions.
- **Next bounded action and owner:** the owner's review and merge of #88; owner question 2 (BL-079) and the go-ahead for BL-082's plan (owner). This task was the last PR of the migration plan.
- **Final state and reason:** done. Every required criterion passes.
