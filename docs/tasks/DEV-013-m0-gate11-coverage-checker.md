# DEV-013 — M0 readiness gate 11: a mechanical tenant-isolation coverage checker (INV-060)

## Assignment

- **Objective and user-visible outcome:** readiness gate 11 («Tenant isolation for every module», `docs/delivery/production-readiness.md` §11), runbook item 11 and §5.14 order 4. Coverage of positive and negative policy tests per exposed tenant relation becomes a check that fails, rather than a claim: an exposed relation with no such tests is a named gap. INV-060 gains the coverage check it names and no test file implements today.
- **State:** implementing
- **Coordinator:** primary Claude Code session, 2026-09-15.
- **Execution mode:** independent subagents for the required stages, as native `gp-*` agent types.
- **Selected route and why:** new behavior touching RLS verification and a catalog: `gp-architect` design → owner decisions → coordinator implements (failing test first) → `gp-reviewer` + `gp-security` → `gp-qa`.
- **Triggered stages and why:** `gp-architect` (a new check over RLS policies and grants, a possible catalog); `gp-security` (the check decides what counts as tenant-isolation evidence). `gp-ui-reviewer` and `gp-mobile` are not triggered.
- **Owning module and allowed edit paths:** `technical/database/rls-coverage.csv` (new); `scripts/validate-canonical-docs.mjs` (registry step and its self-test fixtures); `packages/testing/src/rls-coverage.test.ts` (new, read-only, no `resetDb`); `technical/database/invariant-catalog.csv` INV-060 enforcement and evidence; `technical/test-catalog.csv` (one row); `docs/delivery/test-strategy.md` §4; `docs/architecture/tenancy-and-security.md` «Required security tests and gates» (one sentence); `docs/delivery/production-readiness.md` §11; runbook §5.11 and §5.14 row 4; `docs/STATUS.md`; `docs/BACKLOG.md` (gap entries per module; DA-149's stale grant); this record and the index. No test assertion in an existing suite changes.
- **Read context:** root `AGENTS.md`; `agents/COORDINATION.md`; `production-readiness.md` §11; runbook §5.11 and §5.14; `docs/delivery/test-strategy.md` §4; `docs/architecture/tenancy-and-security.md` «Required security tests and gates»; `technical/database/invariant-catalog.csv` INV-060; `technical/data-access-surface.csv`; `packages/testing/src/*-rls.test.ts`, `privileges.test.ts`, `m5-external-rls.test.ts` §1.
- **Linked spec, ADR or earlier task:** INV-060; [DEV-012](DEV-012-m0-gate12-evidence.md) (the previous gate).
- **Baseline:** `b9dcf6b` (main after PR #93).
- **Dependencies / constraints / out of scope:** GitHub Actions starts no jobs until October 2026; database suites run locally only with the owner's confirmation, never `supabase db reset`; the local database is at `0085`. Out of scope: writing every missing isolation test (gaps become backlog or follow-up tasks unless the owner decides otherwise), the other gates.
- **Required acceptance criteria:**
  1. `technical/database/rls-coverage.csv` holds one row per exposed `(schema, relation, principal)` of the local database at `0085`, plus one `exempt_no_grant` row per unexposed `public`/`app` table, each classified `covered`, `gap` or `exempt_no_grant` with the fields its class requires.
  2. The validator refuses, with a named error and a self-test fixture each: a deployed table missing from the registry; a stale row; a malformed row (class, key, missing test, missing reason, unknown module, gap without an existing backlog id); a cited file or `describe::it` that does not exist or is ambiguous; a cited test or its describe that can skip or be marked (per the installed vitest's modifiers), and `.only` in a cited file; a relation not named in the cited file; a `gap` row for a table first created after `0085`. The self-tests fail first.
  3. `packages/testing/src/rls-coverage.test.ts` fails, first, on in-transaction probes (an unregistered exposed table; a broken exemption) and on in-memory comparisons (a missing row, a stale row), then passes against the local database at `0085`: the exposed set equals the registry both ways, every exemption holds no privilege, and the role-membership graph among the five principals is pinned. It reads catalogs only, rolls its probes back, and never calls `resetDb`.
  4. Every `covered` row's citations are checked by the validator, and each positive and negative was read to match the v0.1 minimum (member plane: an authorised same-workspace read or write, and a cross-workspace read denial; service plane: the declared workspace reaches the row, another or none is refused); a row that does not meet it is a `gap`.
  5. Every `gap` row points to an existing backlog entry, grouped per module.
  6. INV-060, `test-catalog.csv`, `test-strategy.md` §4, `tenancy-and-security.md`, `production-readiness.md` §11 (not closed while any gap remains), runbook §5.11 and §5.14 row 4, and STATUS agree with the checker.
  7. `pnpm validate:canonical-docs`, `pnpm validate:agents`, and `pnpm --filter @goproceed/testing typecheck` pass.
  8. CI `verify` on the PR head (not required: GitHub Actions starts no jobs until October 2026).
- **Skipped stages and rationale:** above.

## Owner decisions

Record each decision on the day it is made. Write it in the owner's terms; never paraphrase it into something stronger.

| Date | Decision | Source |
|---|---|---|
| 2026-09-15 | Next task: the gate 11 coverage checker (runbook §5.14 order 4) | chat, «смержил, давай чекер» |
| 2026-09-15 | Build the registry and both checkers; relations without both tests become explicit `gap` rows with backlog entries per module; a table created after `0085` cannot arrive as a gap; the missing tests are later tasks | chat, answer «Чекер + список gap» |
| 2026-09-15 | Gate 11 closes only at zero `gap` rows | chat, answer «Только при 0 gap» |
| 2026-09-15 | Run only the new `packages/testing/src/rls-coverage.test.ts` against the local database at `0085`; the cited RLS suites are not run (NOT RUN until CI or a separate permission); no `supabase db reset` | chat, answer «Только новый файл» |

## Plan

Numbered steps. For each step, name the files it touches and the check that proves it. A step that adds a contract, refusal or invariant starts with its failing test.

## Progress and decisions

| Order | State or role | Decision / result | Evidence / reference | Next action |
|---|---|---|---|---|
| 1 | designing (`gp-architect`, native) on `b9dcf6b` | **Design returned, read-only.** An explicit registry `technical/database/rls-coverage.csv` (`schema, relation, principal, module, classification, positive_test, negative_test, backlog_id, reason`; classifications `covered`, `gap`, `exempt_no_grant`; test cited as `packages/testing/src/<file>::<describe>::<it>`), because only a registry enumerates every relation against the database and names each gap; a source scan, title tags or runtime instrumentation would sample. **Exposed** is decided by the running database, not `data-access-surface.csv` (which lists 155 tables against 69 built and still shows a grant `0062:621` revoked): a `public`/`app` base table or `api` view with a direct table or column grant to `anon`, `authenticated`, `goproceed_app`, `goproceed_service` or `goproceed_worker`; the role-membership graph is pinned so inheritance cannot hide a principal. **Positive / negative (v0.1 minimum):** member plane — an authorised actor in workspace A reads (or, without SELECT, writes) A's row / an active member of another workspace reads nothing of A; service plane — a transaction declaring A reaches A's row / another or no declared workspace is refused; `api` views — own context / non-member sees nothing. Other matrix rows stay review. **Two checkers:** the validator (no database) checks registry shape, every deployed `public`/`app` table present, every cited test existing and unskippable (`.skip/.todo/.skipIf/.runIf/.fails`, `.only`, `ctx.skip`), the relation named in the cited file, gap rows carrying an existing backlog id, and no gap row for a table created after `0085`; `packages/testing/src/rls-coverage.test.ts` (read-only, no `resetDb`) checks the database's exposed set equals the registry both ways and that every exemption still has no grant, with in-transaction probes as its own failing tests. **Exempt, no grant:** `outbox_dead_letters`, `telegram_inbox_updates`, `telegram_evidence_decision_attempts`, `telegram_evidence_decision_tokens`, `app.telegram_erasures`, `app.telegram_erasure_keys`, `app.retention_policy`; service-only tables are not exempt. **No quarantine ledger exists**; the skip refusals stand in. **Rough gaps:** about 21 relations covered, 22 partial, 22 with no policy test, plus the service rows of five dual-granted tables. Recommends shipping with an explicit gap list rather than failing on gaps. INV-060 enforcement and evidence text, `test-catalog.csv` T-RLS-COVERAGE-001, `test-strategy.md` §4, one sentence in `tenancy-and-security.md`; no ADR. Also found: DA-149 still shows a grant `0062:621` revoked | architect report | Owner decisions |
| 2 | implementing (coordinator): facts | Local database at `0085`, read-only catalog queries: 73 in-scope relations (69 `public` and 3 `app` base tables, 1 `api` view); 66 exposed by a direct table or column grant to one of the five principals — 65 `public` tables and `api.me_context` (granted to `goproceed_app`, not `authenticated`) — 7 of them to both `goproceed_app` and `goproceed_service`, so 73 `(relation, principal)` rows; 7 unexposed (`outbox_dead_letters`, `telegram_inbox_updates`, `telegram_evidence_decision_attempts`, `telegram_evidence_decision_tokens`, `app.telegram_erasures`, `app.telegram_erasure_keys`, `app.retention_policy`). Role graph among the five: only `goproceed_service` is a member of `goproceed_app`. `gp-researcher` dispatched for vitest 3.2.4 skip and `.only` semantics; `gp-implementer` delegated the registry draft (only `technical/database/rls-coverage.csv`) | `scratchpad/dev013-exposed.txt` | Checker tests |
| 3 | implementing (coordinator): the database checker, failing first | `packages/testing/src/rls-coverage.test.ts` written before its module: **red** — the file could not load `./rls-coverage` (0 tests). Module `rls-coverage.ts` added (CSV parser, in-scope and exposed-set SQL, exemption privilege SQL, `compareCoverage`): the five fixture tests and the role-graph pin pass; the four registry tests fail on the missing `rls-coverage.csv` (still red, as intended until the registry exists) | `scratchpad/dev013-red-db.txt`, `dev013-red2-db.txt` | Registry draft; validator step |
| 4 | implementing (coordinator): the validator step, failing first | Twenty-two `selfTest` fixtures for the registry written against a stub `rlsCoverageErrors` that returns nothing: **red** — `validator self-test FAILED` naming every registry fixture except the agreeing one (rc 2). They cover a deployed table missing, a stale row, an unknown classification, principal and module, a duplicate key, a covered row without a negative, a gap without or with an unknown backlog id, an exemption without a reason, a gap for a table created after `0085`, a missing cited file or test, a test under another describe, an ambiguous title, `it.skip`, `describe.skip`, `ctx.skip()` in the body, `.only` in the cited file, a relation the cited file does not name, and a citation outside `packages/testing/src/` | `scratchpad/dev013-red-validator.txt` | Implementation |
| 5 | implementing (coordinator): the validator step | `rlsCoverageErrors`, `deployedRelations` (first migration per `public`/`app` table and `api` view) and a `main` step reading the registry, the migrations, `docs/BACKLOG.md` ids, the entity catalog's modules and each cited file; the registry added to `REQUIRED`. `gp-researcher` then reported that vitest 3.2.4 can keep a test from running in at least nine ways (chain modifiers on the test or any describe, options objects inherited downward, `.only` anywhere in the file, `skip()` in the body, a hook or a fixture, `getCurrentTest()`, name and line filters, `include`/`exclude`, calls not reached at load) and that `.fails`, a missing function or no `expect` pass without proof; so the scanner was changed from refusing known modifiers to **allowing one shape**: a bare `it`/`test` with a literal title and a parameterless function, indented exactly two spaces inside a bare column-0 `describe`/`suite` with a parameterless function, with no chain modifier, no options object, no nested describe, and at least one `expect`; per file, it refuses `.only`/`only:`, `getCurrentTest`, `.extend(`, a rebinding of `it`/`test`/`describe`, and `skip()` or a destructured `skip` outside `it.skip`/`describe.skip`; `vitestRunFilterErrors` refuses `testNamePattern`, `allowOnly`, `retry`, `passWithNoTests` or `exclude` in `packages/testing/vitest.config.ts`, and `-t`, `--testNamePattern`, `--allowOnly`, `--exclude`, `--shard` or a `file:line` in its test script. Thirty-seven registry and filter fixtures in `selfTest`. One false positive during hardening (an options object's `skip:` key read as a destructured `skip`) fixed. The self-test passes; `main` fails only on the registry file, which the draft will add | `scratchpad/dev013-validator-after-impl.txt`, `dev013-validator-hardened.txt` | Registry draft review |
| 6 | implementing (coordinator): first run against the draft registry | `gp-implementer`'s draft `technical/database/rls-coverage.csv` (80 rows: 23 `covered`, 50 `gap`, 7 `exempt_no_grant`). **Validator:** the self-test passes; `main` reports 50 problems, every one a `GAP-<module>` placeholder the coordinator replaces with backlog ids; all 46 citations of the 23 covered rows pass the allow-list scanner. A narrowed rule after the pre-scan: `m5-external-rls.test.ts:50` («exactly as it does») had matched the rebinding pattern; it now matches only declarations and `import { … as it }`, with fixtures for both and for prose. **`packages/testing/src/rls-coverage.test.ts` alone against the local database at `0085`: 10 passed** — the exposed set equals the registry both ways, no principal holds a privilege on any of the seven exemptions, the role graph is pinned, and both in-transaction probes are reported | `scratchpad/dev013-validator-hardened.txt`, `dev013-green-db.txt` | Implementer report; gap entries |
| 7 | implementing (`gp-implementer`, delegated slice) and coordinator decisions | **Registry draft report:** 80 rows — `goproceed_app` 23 covered and 37 gaps, `goproceed_service` 13 gaps and no covered row, 7 exemptions (migration cites checked). Covered rows cite `rls`, `m1-rls-baseline`, `m1-rls-workspace`, `m1-rules-rls`, `m1-project-sourced-schema`, `m2-rls`, `m2-occurrences-rls`, `m3-closure-rls`, `m4-act-rls` and `foundation` (`audit_events`: no `SELECT` for `goproceed_app`, so its positive is an own-workspace insert and its negative a non-member's refused insert). Gaps by module: workspace_access 12, communication 16, contract_baseline 8, execution 3, evidence 3, external_review 3, operational 2, projection 2, requirements 1; `communication_delivery_attempts` and `telegram_requirement_choices` are named in no test. **Coordinator decisions:** the three rows it flagged stay `gap` under the strict rule — `progress_entries` (its positive is an `INSERT … RETURNING`, not a read), `idempotency_records` (the read pair is on a row with no workspace), `telegram_requirement_choice_sessions` service (the declared-workspace case is refused by a foreign key, so no row of A is reached); a reviewer may argue them to `covered`. The cited `rls.test.ts` test calls `resetDb()`, which matters to whoever runs it. Gap placeholders replaced with BL-090 to BL-098, one P1 entry per module; DA-149 corrected in the catalog (no grant since `0062:621`) rather than filed | implementer report; `docs/BACKLOG.md` | Validators, commit, reviews |
| 8 | implementing (coordinator): documents and final local evidence | INV-060 enforcement and evidence re-worded; `test-catalog.csv` T-RLS-COVERAGE-001; `test-strategy.md` §4 «How that check works» and one dated sentence in `tenancy-and-security.md` (both Approved; added text only, dated); `production-readiness.md` §11 «not closed», runbook §5.11 status and §5.14 row 4, STATUS. **Final runs:** `node scripts/validate-canonical-docs.mjs` rc 0 and `python3 scripts/sync-agents.py --check` rc 0; `packages/testing/src/rls-coverage.test.ts` alone, 10 passed, local database at `0085`; `pnpm --filter @goproceed/testing typecheck` rc 0. **Mutation:** `it.skip` on the cited `m2-rls.test.ts` «0016 tenant isolation» › «hides another workspace's assignments» turns the validator red naming both columns of the `work_assignments` row (rc 1); restored byte for byte, rc 0 again. Not run: the cited RLS suites themselves (owner allowed only the new file) | `scratchpad/dev013-validate-docs.txt`, `-agents.txt`, `dev013-final-db.txt`, `dev013-final-typecheck.txt`, `dev013-mutation-skip.txt` | Reviews |

## Findings and rework

| Finding ID | Severity | Trigger / location | Expected vs actual | Owner | Resolution and evidence |
|---|---|---|---|---|---|

Rework count and hypothesis changes:

## What is not true after this task

List what a reader might assume this task achieved but it did not. Examples: untested platforms, targets not yet delivered, NOT RUN criteria, and follow-ups.

## Acceptance evidence

| Criterion | Required? | Checked revision | Command or evidence | PASS / FAIL / NOT RUN | Limitation |
|---|---|---|---|---|---|

A blank cell is not a passed check. A required FAIL or NOT RUN prevents done, unless the task scope is explicitly revised and the original requirement stays recorded. A skipped test suite is NOT RUN. Record its environmental reason and the command that would settle it.

The Limitation column opens with at most one qualifier from this closed set, then its detail:

- **PASS:** `negative` (the command correctly produced nothing, and the absence is the evidence); `assisted:` what had to be arranged by hand first; `owner-reported` (the owner's report, not a session observation).
- **FAIL:** `known-red baseline:` the named set of pre-existing failures, with no case outside it failing.
- **NOT RUN:** `environmental:` the cause and the command that settles it; `not-provable-locally:` what would settle it; or, with no qualifier, the reason: why it was deliberately not attempted, or why a PASS was earned for the wrong reason (`agents/roles/gp-qa.md`).

Gate records written before 2026-09-13 keep their own tokens; `docs/delivery/pilot-execution-runbook.md` §7.4 maps them onto this set.

## Sources

Third-party documentation and primary sources checked for this task. Give each one its URL, the installed version it applies to, its publication date if known (never substitute today's date) and the access date.

## Completion / handoff

- Changed / inspected files:
- Review independence: same-session / independent (name the actual stage roles)
- Verified scope:
- Remaining risks / blocked requirements:
- Next bounded action and owner:
- Final state and reason:
