# DEV-009 — M0 readiness gate 10: the ДБН file, its retrieval record, the registry check and the dated evidence entry

## Assignment

- **Objective and user-visible outcome:** readiness gate 10 («Regulatory content», `docs/delivery/production-readiness.md`), which carries runbook M0 items 9 and 10, closes with recorded evidence rather than prose. After this task: the primary ДБН А.3.1-5:2016 file and a retrieval-record file sit under `technical/requirements/` with a test binding them to `DBN_RETRIEVAL`; a dated record of the check against the Реєстр будівельних норм and a repeatable procedure for the next one sit beside them; and one dated evidence entry for gate 10 is linked from `docs/delivery/version-0.1.md` §«M0 — Fit to hold someone else's data», the first entry in that section (STATUS separately records entries of 2026-09-03 for gates 2 and 4 outside it; not re-observed here).
- **State:** verifying
- **Coordinator:** primary Claude Code session, 2026-09-14.
- **Execution mode:** independent subagents for the required stages, as native `gp-*` agent types.
- **Selected route and why:** a regulatory-source question first (`agents/COORDINATION.md`, «Unknown API, version or capability» → `gp-researcher`), then new behavior inside existing boundaries: coordinator implements → `gp-reviewer` → `gp-qa`. New files under `technical/requirements/` are read by tests, and a unit test changes.
- **Triggered stages and why:**
  - `gp-researcher`: the Реєстр будівельних норм entry for ДБН А.3.1-5:2016, the page's «Версія №1 Статус (архівна)», and whether a copy of the ДБН text may be committed.
  - `gp-architect` is not triggered: no table, constraint, migration, RLS, contract or state/capability/event catalog; `technical/requirements/` is none of those.
  - `gp-security` is not triggered: no auth, storage, secrets, CI permission or personal data.
  - `gp-ui-reviewer` and `gp-mobile` are not triggered: no UI or mobile path.
- **Owning module and allowed edit paths:** `technical/requirements/` (new files only); `apps/app/src/lib/statutory-act-form.test.ts` (the agreement test); `apps/app/src/lib/statutory-act-form.ts` (comments only, if a comment is contradicted by the evidence); `docs/delivery/version-0.1.md` §M0 (the evidence entry); `docs/delivery/pilot-execution-runbook.md` §5.9, §5.10, §5.14 row 1 and §10 Q-7; `docs/product/hidden-works-content-rules.md` «Open items» where the evidence changes it; `docs/STATUS.md` «Next action»; this record; `docs/tasks/README.md`. Added after review round 1: `turbo.json` (`test.inputs`, R1-02), `docs/delivery/production-readiness.md` (dated annotations and one evidence pointer, no checkbox, R1-03), `.gitattributes` (R1-09), `docs/BACKLOG.md`.
- **Read context:** root `AGENTS.md`; `START_HERE.md`; `agents/COORDINATION.md`; `docs/delivery/production-readiness.md` §10 and «Evidence format»; `docs/delivery/version-0.1.md` §M0; `docs/delivery/pilot-execution-runbook.md` §5 intro, §5.9, §5.10, §5.14, §7, §10 Q-7; `docs/product/hidden-works-content-rules.md` «Open items»; `docs/README.md` «ADR lifecycle and approval»; `apps/app/src/lib/statutory-act-form.ts:137-215` and its test `:209-245`.
- **Linked spec, ADR or earlier task:** ADR-005, ADR-006 decision 7; [DEV-008](DEV-008-types-react-dedupe.md) (the green CI run this task cites).
- **Baseline:** `6e4efdf` (main, «Merge pull request #89»).
- **Dependencies / constraints / out of scope:**
  - GitHub Actions starts no jobs until October 2026 (owner, 2026-09-14), so any CI row is NOT RUN, `environmental:`.
  - No DB suite runs locally without the owner's confirmation (root `AGENTS.md`); this task changes no schema and needs none.
  - `version-0.1.md`, `production-readiness.md` and `hidden-works-content-rules.md` are Approved: the task adds the dated evidence those documents ask for and changes none of their decisions.
  - Out of scope: the other thirteen gates; allow-list items 9–10 and 12–15 (no primary texts); the Telegram webhook blockers (BL-024).
- **Required acceptance criteria:** see Acceptance evidence.
- **Skipped stages and rationale:** above.

## Owner decisions

| Date | Decision | Source |
|---|---|---|
| 2026-09-14 | Take runbook §5.14 order 1 next: M0 items 9 and 10 evidence entries | Owner, in conversation (answer to the coordinator's question) |
| 2026-09-14 | Q-7: commit a retrieval-record file **and the ДБН file itself** under `technical/requirements/` | Owner, in conversation |
| 2026-09-14 | Item 10: a dated registry-check record under `technical/requirements/` **plus** a repeatable procedure for the next check | Owner, in conversation |
| 2026-09-14 | `gp-researcher` performs the registry check; the owner confirms its result | Owner, in conversation |
| 2026-09-14 | Re-download the ДБН file to the session scratchpad and compare its SHA-256 with the record | Owner, in conversation |
| 2026-09-14 | Confirms the registry check result: ДБН А.3.1-5:2016 «Діючий», «Редакція від 20.04.2026», no change listed, version 1 archived and pointing to the same PDF, listing row «Діючий», bytes reproduced | Owner, in conversation (answer to the coordinator's question) |
| 2026-09-14 | The act footer's «Реєстр будівельних норм» wording goes to the backlog (BL-084), not into this task | Owner, in conversation |

## Plan

1. Research (`gp-researcher`): the register entry and status of ДБН А.3.1-5:2016, the «архівна» version label, the right to commit the text. Check: sourced report with access dates.
2. Owner confirms the registry-check result.
3. `technical/requirements/`: the ДБН PDF, a retrieval-record file, a registry-check record, and the procedure. Check: a unit test that the committed PDF hashes to `DBN_RETRIEVAL.sha256` and has the recorded byte count, and that the retrieval-record file equals `DBN_RETRIEVAL`; it starts failing.
4. The dated gate 10 evidence entry in `version-0.1.md` §M0; gate 10 in `production-readiness.md`; runbook §5.9, §5.10, §5.14 row 1 and Q-7; STATUS «Next action». Check: `pnpm validate:canonical-docs`.
5. `gp-reviewer`, rework, `gp-qa`, PR.

## Progress and decisions

| Order | State or role | Decision / result | Evidence / reference | Next action |
|---|---|---|---|---|
| 1 | scoped (coordinator) | Item 9's renderer blocker closed: the Telegram assignment card carries the tag and source since `c48a4a5` and `0359bcb` (2026-09-09, both on `main`), so runbook §5.9's «PARTIAL since 2026-09-03» is stale. Item 10's mechanism is built (`statutory_act_versions.registry_checked_on`, the freeze check, the future-date guard, the footer) with no fact behind the date. The last green CI run, 34872695375 attempt 1 (2026-09-14), checked out PR #89's merge ref `d62cf4c` (`3a9ea42` into `7b57784`; `7b57784` is an ancestor of `3a9ea42`, and `3a9ea42`'s code paths equal `6e4efdf`'s) and ran without skips: `packages/testing` `src/m4-act-schema.test.ts` (58 tests, including the future-dated registry check refusal), `apps/app` `src/lib/statutory-act-form.test.ts` (41), `src/lib/telegram/cards.test.ts` (24), `tests/requirement-library-fidelity.int.test.ts` (13), `tests/norm-ref-labels.int.test.ts` (2) | `git merge-base --is-ancestor`; `gh run view 34872695375 --attempt 1 --log` | Research |
| 2 | researching (coordinator): re-fetch | 2026-09-14 (page response `date` 19:23:15 GMT; PDF response `date` 19:24:35 GMT): `https://e-construction.gov.ua/laws_detail/3879707932224390963` answered 200 and lists three `files-token` links: a PNG (QR code), a DOCX (`50dfd39ace36363a883a886c267b230a`) and a PDF (`c7fb685e91deb04c43a13f9a6cf628a1`, the link recorded in `statutory-act-form.ts:175`). The PDF link still resolved, although the code comment calls such links expiring: `application/pdf`, PDF 1.5, 54 pages, **636 603 bytes, SHA-256 `4592edafaa8097d3b9305b7934d080256d649616a2741b6a5537a28606a665e3`**, identical to the record of 2026-08-10. The page also reads «Версія №1 Статус (архівна)» next to the developer's name, sent to research. The file is kept in the session scratchpad only | `curl -sS -L -o c7fb685e.bin …/files-token/c7fb685e91deb04c43a13f9a6cf628a1; shasum -a 256; wc -c; file` | Research report |
| 3 | implementing (coordinator): failing test first | Two tests added to `apps/app/src/lib/statutory-act-form.test.ts`: the committed `technical/requirements/dbn-a31-5-2016.pdf` hashes to `DBN_RETRIEVAL_RECORD.sha256` and starts with `%PDF-`; `technical/requirements/dbn-a31-5-2016.retrieval.json` equals the record (`pageUrl`, `retrievedOn`, `sha256`) and names the file and its byte count. Baseline before the edit: 41 passed. After the edit, before any file: 2 failed with `ENOENT` on the two paths, 41 passed. The retrieval-record JSON is written; the PDF waits for criterion 1 | `pnpm --filter @goproceed/app exec vitest run src/lib/statutory-act-form.test.ts` (unit only, no database) | Research report |
| 4 | researching (`gp-researcher`, native) | ДБН A: registration is an entry in ЄДЕССБ (ЗУ 1704-VI ст. 10); no law names a «Реєстр будівельних норм»; ПКМУ № 681 п. 6 names Мінрозвитку as holder, renamed by ПКМУ № 963 from 18.07.2026 with construction functions unallocated. B: entry «Діючий», «Редакція від 20.04.2026», code BN01:4205-4602-0357-0183, наказ № 115; version 1 «Архівний»; listing «Діючий»; no change, replacement or cancellation found. C: same PDF link on both versions. D: ЗУ 1704-VI ст. 12 ч. 2 «Будівельні норми не є об'єктом авторського права»; ДСТУ differ (ЗУ 1315-VII ст. 25); the portal states CC BY 4.0 with attribution. E: nothing downgrades the rows. The researcher read pages through a summarising fetch tool and asked for re-verification | gp-researcher report | Re-verify from raw pages |
| 5 | researching (coordinator): raw re-verification | From raw HTML fetched 2026-09-14T19:24:35Z and 19:31:12Z: the entry's text has «Статус | Діючий», «Редакція від 20.04.2026», «BN01:4205-4602-0357-0183», «Наказ Мінрегіону України від 05.05.2016 р. № 115», no line containing «змін», and «Дата набрання чинності» 01.01.2017 in the «Примірники» table (first written here as «empty», R1-01); the archived page has «Архівний», «Версія №1», links to the current entry and the same PDF token, and seven «змін» lines that are the norm's own text (п. 5.1.3 onward); the listing row reads «Діючий». `zakon.rada.gov.ua/laws/show/1704-17/print` (edition 09.06.2022) contains ст. 12 ч. 2 and ст. 10 ч. 6 verbatim. The PDF's pages 1–4 (PDFKit) carry «Видання офіційне», «Мінрегіон України, 2016» and no reproduction restriction | `curl` of the three pages and the law; Python text extraction; `swift pdftext.swift` | Owner confirmation |
| 6 | implementing (coordinator) | Owner confirmed the check and sent the footer wording to BL-084. Committed-to-be: `technical/requirements/dbn-a31-5-2016.pdf` (the re-fetched bytes), `dbn-a31-5-2016.retrieval.json`, `dbn-a31-5-2016.registry-checks.json`, `README.md` (what the files are, what «the Реєстр» means here, the procedure). Tests: 43 passed. Mutation: one byte appended to the PDF turned both new tests red (41 passed, 2 failed); the file restored from the fetched copy hashed to `4592edaf…a665e3`; 43 passed again. `statutory-act-form.ts`: one comment sentence, since the «expiring» link resolved to the same bytes 35 days later. `pnpm --filter @goproceed/app typecheck` rc 0 | `vitest run src/lib/statutory-act-form.test.ts`; `shasum -a 256` | Documents |
| 7 | implementing (coordinator): documents | `version-0.1.md` §M0 gains «Gate evidence entries» with gate 10's dated entry, the first in that section. `production-readiness.md` is not edited: it says «No gate below is recorded closed by this document», so no checkbox is ticked. Runbook §5 intro, §5.9, §5.10, §5.14 row 1, Q-7 and §8 question 1, each with the replaced text kept in a dated note. `hidden-works-content-rules.md` Open items: one dated sentence that the file is retained. STATUS «M0 gates» row (gate 10 only, other cells not re-observed) and «Next action» 1 and 3. BL-084 added. Validators rc 0 | `node scripts/validate-canonical-docs.mjs`; `python3 scripts/sync-agents.py --check` | `gp-reviewer` |
| 8 | reviewing (`gp-reviewer`, native), round 1 on `a83f48b` | **Changes requested.** Major R1-01; medium R1-02 to R1-04; low R1-05 to R1-10. Confirmed: every registry claim against the raw HTML except the effective date, the CI counts with no skips, the identifiers, the tests' failure causes, the Approved-document edits as annotations and added evidence, BL-084's form, the stage reasoning, and no rule against the PDF | gp-reviewer report | Fix R1-01 to R1-10 |
| 9 | rework (coordinator), stated fixes | All ten applied (Findings table). Mutations sharpened: one byte flipped at offset 1000, length unchanged → only «commits the ДБН file…» red (42 passed); `retrievedOn` changed to 2026-08-11 → only «commits a retrieval-record file…» red (42 passed); restored → 43 passed. Turbo: `@goproceed/app#test` hash `fc7b6ac55f536c3e` before the input; `e4d946491eb1065d` after; `9efeff0b15186f8e` with one byte appended to the PDF; `e4d946491eb1065d` restored. `production-readiness.md` unticked items: 32. Not a rework round: no QA FAIL preceded it | `dev009-r1-mut-flip.txt`, `dev009-r1-mut-date.txt`, `dev009-r1-green2.txt`; `pnpm turbo run test --filter=@goproceed/app --dry=json` | `gp-qa` |
| 10 | verifying (`gp-qa`, native), round 1 on `08ed618` | Criteria 1–9 and 11 PASS, re-run: PDF bytes equal the fetched copy and the `HEAD` blob; four single-test mutations (PDF byte flip; `retrievedOn`; the check's `file.sha256`; `reproductions[0].sha256`), each `1 failed | 42 passed`, restored to 43; typecheck and validators rc 0; turbo hash follows every file under `technical/requirements/` (its own values, from its own tree); ancestry of `c48a4a5`, `0359bcb`, `bd08da9` and `7b57784` → `3a9ea42`; every registry fact against the raw HTML and ЗУ 1704-VI. Criterion 10 NOT RUN, not required. All ten review fixes in place. Q1-01 to Q1-04 (low, text) | QA report; `scratchpad/qa-dev009-mut.sh`, `qa-vt-*.txt` | Fix Q1-01 to Q1-04 |
| 11 | rework (coordinator), stated fixes | Q1-01: Sources times. Q1-02: «What is not true» narrowed. Q1-03: the entry's mutation sentence. Q1-04: `archivedVersion.version` removed; `urlSource` and `labelNote` added. Not a rework round: no QA FAIL | This diff | `gp-qa` narrow re-check |

## Findings and rework

| Finding ID | Severity | Trigger / location | Expected vs actual | Owner | Resolution and evidence |
|---|---|---|---|---|---|
| R1-01 | major | `dbn-a31-5-2016.registry-checks.json` `effectiveDateShown`; row 5 | Expected: what the page says. Actual: «field is empty», while the «Примірники» table reads 01.01.2017 (`laws_detail.html:622`) | coordinator | The field replaced by `copies` (one row: «Основний текст», «Електронна версія паперового примірника», 01.01.2017, PDF and docx) and a note that the date is an observation, not a printable source; row 5 corrected. The owner's confirmation question did not include the effective-date claim, and the corrected value leaves the confirmed result unchanged |
| R1-02 | medium | `turbo.json` `test.inputs` | Expected: a change to the PDF or JSON re-runs the tests. Actual: `technical/requirements/**` not an input, so a cached PASS replays | coordinator | `"../../technical/requirements/**"` added; hash changes with the PDF and returns on restore (row 9) |
| R1-03 | medium | `version-0.1.md:310-312`; `production-readiness.md:152-154`, `:351-353`; gate 10 without a pointer | Actual: stale «no reviewer can reopen» and «record … owes» beside the new entry; no evidence pointer where gates 2 and 4 have one | coordinator | Dated «Annotated 2026-09-14» notes on all three; an unticked «Evidence, 2026-09-14» sub-bullet under gate 10 |
| R1-04 | medium | `registry-checks.json` `register.holder`; BL-084; README | Actual: ПКМУ facts from a summarised reading stated without qualifier; «no law» broader than one law read raw | coordinator | Qualified as `gp-researcher`'s summarised reading, not re-verified; the name claim narrowed to ЗУ 1704-VI and the ЄДЕССБ pages read on 2026-09-14, in the JSON, README and BL-084 (title and Why) |
| R1-05 | low | `version-0.1.md` entry; row 1 | Actual: no tested revision named | coordinator | `d62cf4c` named, with the ancestry of `7b57784` to `3a9ea42` and `3a9ea42`'s code equal to `6e4efdf` |
| R1-06 | low | `registry-checks.json` `entry.accessedAt` | Actual: 19:24:35Z, the PDF's time; the page's `date` header is 19:23:15 | coordinator | 19:23:15Z with its source; archived page and listing marked `fetchedBefore` 19:31:12Z, headers not kept; README step 1 asks to keep headers |
| R1-07 | low | Mutation evidence; unbound hashes | Actual: one mutation turned both tests red; no JSON mutation; `reproductions[].sha256` and the check's `file.sha256` unbound | coordinator | Two single-test mutations (row 9); test 2 now binds both hashes |
| R1-08 | low | STATUS preamble | Actual: «without re-observing the row's other cells» under-describes two cell edits | coordinator | The sentence itemises the State-cell reading and the Open-cell replacement |
| R1-09 | low | No `.gitattributes` | Actual: nothing stops a line-ending conversion rewriting the PDF | coordinator | `.gitattributes`: `*.pdf binary` |
| R1-10 | low | Entry omits `bd08da9`; unclear comment; README step 5 | Actual as stated | coordinator | `0084` (`bd08da9`) cited; comment reads «No expiry has been observed yet»; step 5 forbids replacing the committed bytes, record or `DBN_RETRIEVAL` |
| Q1-01 | low (record only) | Sources, current entry and listing lines | Actual: page «accessed 19:24:35Z; the PDF … fetched the same second», which R1-06 corrected elsewhere | coordinator | Page 19:23:15Z and PDF 19:24:35Z from their headers; archived page and listing «fetched before 19:31:12Z» |
| Q1-02 | low (record only) | «What is not true», the footer bullet | Actual: «a name no law uses», broader than R1-04's narrowing | coordinator | «a name ЗУ 1704-VI and the ЄДЕССБ pages read on 2026-09-14 do not use» |
| Q1-03 | low | `version-0.1.md` gate 10 entry | Actual: «a one-byte change to the file turns both new tests red»; a same-length change turns one | coordinator | «a one-byte change to the file, or to a hash or date in the retrieval record, turns its own test red» |
| Q1-04 | low | `registry-checks.json` `archivedVersion.version` | Actual: «Версія №1» asserted, while the label is identical text on both pages and the archived URL is not linked from the current entry | coordinator | `version` removed; `urlSource` (gp-researcher's reading, not linked from the current entry) and `labelNote` added |

Rework count and hypothesis changes: review round 1's and QA round 1's findings were applied as stated fixes; no rework round used. R1-01 came from reading the label that follows «Дата набрання чинності» in page text order, which is the next column header, not the cell; the fix read the table row.

## What is not true after this task

- **M0 is not closed.** Readiness gate 10 has an entry; gates 1–9 and 11–14 do not in `version-0.1.md` §M0.
- **The new tests have not run in CI.** GitHub Actions starts no jobs until October 2026; they ran locally (43 passed, macOS). The CI run the entry cites (34872695375 attempt 1) predates the new files and proves the mechanism, not the committed bytes.
- **The database does not know a check happened.** `registry_checked_on` is still only refused in the future; nothing ties a printed date to `dbn-a31-5-2016.registry-checks.json`, and the procedure is followed by hand.
- **The footer still says «Реєстр будівельних норм»**, a name ЗУ 1704-VI and the ЄДЕССБ pages read on 2026-09-14 do not use, and citations still say «Мінрозвитку», renamed on 18.07.2026 (BL-084).
- **One fetch reproduced is not two independent sources.** «незалежність будь-яких додаткових копій не встановлена» stays true of every row.
- **The registry check reads one portal.** No second official source was found; the mininfra «Чинні будівельні норми» list omits the norm and is not treated as a register.
- **Allow-list items 9–10 and 12–15** still have no primary texts or tags.
- **`production-readiness.md`'s checkboxes stay unticked**, by that document's own rule; STATUS's count of 32 open items is unchanged.
- **STATUS's claim of entries for gates 2 and 4 (2026-09-03) was not re-observed.**
- **The PDF's rights notice was read on pages 1–4 only.**
- **The ПКМУ facts in the registry-check record and BL-084 are `gp-researcher`'s summarised reading**, not re-verified from raw text.
- **The CI run cited for item 9 tested `d62cf4c`, a merge ref no longer fetchable;** its code equality with `main` rests on `3a9ea42`'s ancestry and tree comparison, not on `d62cf4c` itself.

## Acceptance evidence

| Criterion | Required? | Checked revision | Command or evidence | PASS / FAIL / NOT RUN | Limitation |
|---|---|---|---|---|---|
| 1. The right to commit a copy of the ДБН text is established from primary sources before the file is committed | yes | working tree over `6e4efdf` | ЗУ 1704-VI ст. 12 ч. 2, read raw from `zakon.rada.gov.ua/laws/show/1704-17/print` (edition 09.06.2022); PDF pages 1–4 carry no restriction (row 5) | PASS | assisted: the legal reading is `gp-researcher`'s, verified verbatim by the coordinator; no lawyer consulted |
| 2. The ДБН PDF is committed under `technical/requirements/`, and a test proves its bytes hash to `DBN_RETRIEVAL.sha256` | yes | working tree after review round 1 | `vitest run src/lib/statutory-act-form.test.ts`: 43 passed; before the file: `ENOENT`; same-length byte flip: only this test red (row 9); restored: 43 passed | PASS | — |
| 3. A retrieval-record file under `technical/requirements/` agrees with `DBN_RETRIEVAL`, enforced by a test | yes | working tree after review round 1 | same run; compared on `pageUrl`, `retrievedOn`, `sha256`, `bytes`, `file`, every `reproductions[].sha256` and the check's `file.sha256`; `retrievedOn` mutation: only this test red (row 9) | PASS | — |
| 4. A dated registry-check record names the register, its URL, the norm, its status and edition, the access time, who checked and the owner's confirmation | yes | working tree over `6e4efdf` | `dbn-a31-5-2016.registry-checks.json`; rows 4–5; Owner decisions 2026-09-14 | PASS | owner-reported: the confirmation is the owner's answer in conversation |
| 5. A procedure for repeating the check, and what a changed result does to the rows, is written where an operator finds it | yes | working tree over `6e4efdf` | `technical/requirements/README.md` «Repeating the registry check»; runbook §5.10 «Next action» points to it | PASS | — |
| 6. One dated evidence entry for gate 10 is linked from `version-0.1.md` §M0 in the form `production-readiness.md` «Evidence format» asks | yes | working tree over `6e4efdf` | `version-0.1.md` §M0 «Gate evidence entries», gate 10, 2026-09-14, naming the test run, the files, the owner's rulings and the limits | PASS | — |
| 7. Runbook §5.9, §5.10, §5.14 row 1, Q-7 and STATUS «Next action» agree with the evidence | yes | working tree over `6e4efdf` | `git diff 6e4efdf -- docs/delivery/pilot-execution-runbook.md docs/STATUS.md` | PASS | — |
| 8. `statutory-act-form.test.ts` passes locally, with the new tests failing first | yes | working tree over `6e4efdf` | row 3 (red), row 6 (green, mutation); `pnpm --filter @goproceed/app typecheck` rc 0 | PASS | macOS, Node 24.18.0 |
| 9. `pnpm validate:canonical-docs` and `pnpm validate:agents` pass | yes | working tree after review round 1 | `node scripts/validate-canonical-docs.mjs` rc 0; `python3 scripts/sync-agents.py --check` rc 0 | PASS | — |
| 11. A change to any file under `technical/requirements/` changes turbo's `@goproceed/app#test` hash | yes | working tree after review round 1 | row 9: `e4d946491eb1065d` → `9efeff0b15186f8e` with the PDF touched → `e4d946491eb1065d` restored | PASS | — |
| 10. CI `verify` on the PR head | no | — | — | NOT RUN | environmental: GitHub Actions starts no jobs until October 2026 (owner, 2026-09-14); settle with `gh run rerun <run id> --repo akisly/go-proceed` then |

## Sources

- `https://e-construction.gov.ua/laws_detail/3879707932224390963` (current entry), accessed 2026-09-14T19:23:15Z (response `date` header); the PDF behind `files-token/c7fb685e91deb04c43a13f9a6cf628a1`, fetched 19:24:35Z (its response `date` header).
- `https://e-construction.gov.ua/laws_detail/3113373519350597353` (version 1, archived) and `https://e-construction.gov.ua/laws/doc_type=2` (listing), fetched before 2026-09-14T19:31:12Z (the shell clock right after the fetch; headers not kept).
- ЗУ «Про будівельні норми» № 1704-VI, edition of 09.06.2022, `https://zakon.rada.gov.ua/laws/show/1704-17/print`, accessed 2026-09-14T19:31:18Z: ст. 10 ч. 6, ст. 12 ч. 2.
- Per `gp-researcher`, accessed 2026-09-14 through a summarising fetch tool: ЗУ «Про авторське право і суміжні права» № 2811-IX ст. 8; ЗУ «Про стандартизацію» № 1315-VII (edition 28.08.2025) ст. 2, ст. 25; ПКМУ № 681 of 23.06.2021 п. 6; ПКМУ № 950 of 15.07.2026; ПКМУ № 963 of 17.07.2026; наказ Мінрегіону № 115 (Rada card `v0115858-16`); `mininfra.gov.ua` «Чинні будівельні норми» (dated 11.01.2025); `decentralization.gov.ua/news/20871` (18.07.2026, semi-official).
- GitHub Actions run 34872695375 attempt 1, repository `akisly/go-proceed`, read 2026-09-14.

## Completion / handoff

- Changed files: `.gitattributes`, `turbo.json`, `docs/delivery/production-readiness.md`; `technical/requirements/dbn-a31-5-2016.pdf`, `dbn-a31-5-2016.retrieval.json`, `dbn-a31-5-2016.registry-checks.json`, `README.md` (new); `apps/app/src/lib/statutory-act-form.test.ts`, `apps/app/src/lib/statutory-act-form.ts` (comment); `docs/delivery/version-0.1.md`, `docs/delivery/pilot-execution-runbook.md`, `docs/product/hidden-works-content-rules.md`, `docs/STATUS.md`, `docs/BACKLOG.md`, this record, `docs/tasks/README.md`.
- Review independence: independent — `gp-researcher`; `gp-reviewer` round 1; `gp-qa` round 1 and a narrow re-check pending.
- Verified scope: criteria 1–9 and 11, by the coordinator.
- Remaining risks / blocked requirements: «What is not true» above.
- Next bounded action and owner: coordinator — `gp-qa` narrow re-check of Q1-01 to Q1-04, then the PR.
- Final state and reason: verifying.
