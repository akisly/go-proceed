# DEV-010 — M0 readiness gate 14: the preflight's HMAC key checks, the secret rotation runbook and the evidence toward the gate

## Assignment

- **Objective and user-visible outcome:** readiness gate 14 («Environment and secrets», `docs/delivery/production-readiness.md`), which carries runbook M0 item 7, gains recorded evidence. After this task: a standalone secret rotation runbook sits at `infra/secret-rotation.md`; the deploy preflight refuses a malformed HMAC key list or an active key id the list does not hold, as the runtime registry already does, and neither path prints key material; the `version-0.0.md` gate 4 box on role passwords is ticked with its 2026-08-19 evidence and stated limits. **The gate stays open** (owner, 2026-09-15): `TELEGRAM_LINK_PEPPER` has no key id and the owner chose to build one (BL-085).
- **State:** rework
- **Coordinator:** primary Claude Code session, 2026-09-15.
- **Execution mode:** independent subagents for the required stages, as native `gp-*` agent types.
- **Selected route and why:** new behavior inside existing boundaries (`agents/COORDINATION.md`): coordinator implements → `gp-reviewer` + `gp-security` → `gp-qa`. `apps/app/scripts/deploy-preflight.mjs` is executed code, and a test is added.
- **Triggered stages and why:**
  - `gp-security`: the change touches application environment variables and secrets (the deploy preflight's secret checks, the rotation runbook for database role passwords, Supabase API keys and the external-link HMAC keys).
  - `gp-architect` is not triggered: no table, migration, RLS, grant, contract or catalog.
  - `gp-ui-reviewer` and `gp-mobile` are not triggered: no UI or mobile path.
  - `gp-researcher` is not used: the Vercel and Supabase facts the runbook depends on were read by the coordinator from the vendors' current documentation (Sources).
- **Owning module and allowed edit paths:** `apps/app/scripts/deploy-preflight.mjs` and a new sibling module for its key checks; a new unit test; `apps/app/src/lib/external-link.ts` (the registry's error messages only, owner 2026-09-15); `infra/secret-rotation.md` (new); `infra/README-staging.md` (pointers to the new runbook only); `docs/delivery/version-0.0.md` gate 4 (the one box); `docs/delivery/version-0.1.md` §M0 «Gate evidence entries»; `docs/delivery/production-readiness.md` gate 14 (an evidence pointer, no tick); `docs/delivery/pilot-execution-runbook.md` §5.7, §5.14 row 2, §5 intro, §8 question 1; `docs/STATUS.md`; `docs/BACKLOG.md` if a follow-up is filed; this record; `docs/tasks/README.md`. The owner's «секреты» scope includes `apps/landing`'s and the Telegram channel's secrets in the runbook's inventory; their code is not touched.
- **Read context:** root `AGENTS.md`; `START_HERE.md`; `agents/COORDINATION.md`; `docs/delivery/production-readiness.md` §14 and «Evidence format»; `docs/delivery/version-0.0.md` §4; `docs/delivery/version-0.1.md` §M0; `docs/delivery/pilot-execution-runbook.md` §5.7, §5.14; `infra/README-staging.md` §3, §4.3, «Status»; `apps/app/.env.example`; `apps/app/src/lib/external-link.ts` and its test; `supabase/seed.sql`; `scripts/set-local-app-password.mjs`; migrations `0003` and `0034`; `docs/tasks/DEV-009-m0-gate10-evidence.md` (the entry form).
- **Linked spec, ADR or earlier task:** [DEV-009](DEV-009-m0-gate10-evidence.md) (the first gate entry and its form).
- **Baseline:** `a5fd136` (main, «Merge pull request #90»).
- **Dependencies / constraints / out of scope:**
  - GitHub Actions starts no jobs until October 2026 (owner, 2026-09-14), so any CI row is NOT RUN, `environmental:`.
  - No hosted project is changed and no secret is read, rotated or printed. The hosted state is taken from `infra/README-staging.md` «Status» (2026-08-19).
  - No DB suite runs locally without the owner's confirmation.
  - Out of scope: a separate production project (runbook §10 Q-9), the other twelve gates, BL-084.
- **Required acceptance criteria:** see Acceptance evidence.
- **Skipped stages and rationale:** above.

## Owner decisions

| Date | Decision | Source |
|---|---|---|
| 2026-09-14 | Next after DEV-009: runbook §5.14 order 2, item 7 (readiness gate 14) and the `version-0.0.md` tick | Owner, in conversation («смержил, давай дальше», after the coordinator named it the next step) |
| 2026-09-15 | Close gate 14 for the environments that exist, recording that no separate production project exists and that the gate reopens for one when it does (Q-9) | Owner, in conversation (answer to the coordinator's question) |
| 2026-09-15 | Tick `version-0.0.md` gate 4's role-password box with the 2026-08-19 evidence and the caveat that one hosted environment exists | Owner, in conversation |
| 2026-09-15 | Fix the preflight gap in this task: refuse a malformed `*_HMAC_KEYS` or an `*_ACTIVE_KEY_ID` the list does not hold | Owner, in conversation |
| 2026-09-15 | Lift the rotation instructions into a standalone `infra/secret-rotation.md` | Owner, in conversation |
| 2026-09-15 | Build a key-id `TELEGRAM_LINK_PEPPER` rather than close gate 14 with the pepper limit; gate 14 stays open until then (BL-085). This supersedes the decision above to close the gate for the environments that exist | Owner, in conversation (answer to the coordinator's question after review R1-01) |
| 2026-09-15 | Fix the key registry's runtime error messages in this task, so they name the entry by position and never echo a key id | Owner, in conversation |
| 2026-09-15 | The landing's pilot form uses its own Telegram bot, separate from the channel's bot | Owner, in conversation |

## Plan

1. A failing test first: the preflight's key checks refuse exactly what `linkKeys()` / `sessionKeys()` refuse, over a table of cases. Check: red before the change.
2. Move the key checks into a module the preflight imports; wire it in. Check: the test green; the preflight exercise refuses case G.
3. `infra/secret-rotation.md`: what to rotate, the order (new credential, update, redeploy, verify, revoke), per secret, and the compromise path; `README-staging.md` points to it. Check: every command and claim traced to the repository or the vendors' docs.
4. The `version-0.0.md` tick; the gate 14 entry; the `production-readiness.md` pointer; runbook and STATUS. Check: validators.
5. `gp-reviewer` and `gp-security`, rework, `gp-qa`, PR.

## Progress and decisions

| Order | State or role | Decision / result | Evidence / reference | Next action |
|---|---|---|---|---|
| 1 | scoped (coordinator) | Hosted reality: one Supabase project, `asrvzhjaueyvrfozxpzo`, serves staging, the pilot and the production Vercel build of `goproceed-app`; Previews are not built (`apps/app/vercel.json` `ignoreCommand` builds Production only). Seed and roles: `supabase/seed.sql` sets no password; `scripts/set-local-app-password.mjs:11-13` refuses a non-loopback host; migrations `0003` and `0034` create both LOGIN roles with no password. `README-staging.md` «Status» records both role passwords set on 2026-08-19 (SCRAM, different) and the production build printing the preflight's `OK`. A scan of tracked files found no committed secret: one placeholder connection string (`[YOUR-PASSWORD]`, README-staging) and the published local Supabase demo secret `sb_secret_N7UND0…`, used only against `127.0.0.1:54321` (`evidence-storage.ts`) | `git grep` scans; files named | Preflight exercise |
| 2 | scoped (coordinator): preflight exercise | With `DEPLOY_PREFLIGHT=1` and fake values, no network: empty environment → exit 1; `app_pw` in `APP_DB_URL` → exit 1; `APP_DB_URL` equal to `SERVICE_DB_URL` → exit 1; a legacy `eyJ…` JWT as `SUPABASE_SECRET_KEY` → exit 1; a complete valid set → exit 0 with `OK`; no `VERCEL`/`DEPLOY_PREFLIGHT` → exit 0, silent. **Case G: `EXTERNAL_LINK_HMAC_KEYS=nokeyid` and `EXTERNAL_SESSION_ACTIVE_KEY_ID=k9` → exit 0.** The runtime registry refuses both (`external-link.test.ts`: no key material, active id not in the list, a key shorter than 32 bytes), so a build passes that fails its first external request | `env -i … DEPLOY_PREFLIGHT=1 node scripts/deploy-preflight.mjs` | Owner decisions |
| 3 | implementing (coordinator): failing test first | `apps/app/scripts/deploy-preflight-keys.test.mjs` runs twelve cases, for both key pairs, through the preflight check and through `linkKeys()` / `sessionKeys()` (`src/lib/external-link.ts` `loadRegistry`), and requires the same verdict; one more test requires that a problem names the variable and never prints the secret. First run in a fresh worktree exited 254, `Command "vitest" not found` (no dependencies installed), which is not a red test; after `pnpm install --frozen-lockfile`, the red run failed on `Cannot find module './deploy-preflight-keys.mjs'` | `pnpm exec vitest run scripts/deploy-preflight-keys.test.mjs` in `apps/app` | Implement |
| 4 | implementing (coordinator) | `apps/app/scripts/deploy-preflight-keys.mjs` restates `loadRegistry`'s rules: both names set, every comma entry `<keyId>:`, a key id that is not empty after trim, a base64 secret of at least 32 bytes, the active id among the ids. `deploy-preflight.mjs` imports it and adds its problems when both names of a pair are set (an unset name is already reported). Parity test: 25 passed. Exercise with fake values: empty 1; `app_pw` 1; identical URLs 1; legacy JWT 1; a valid set 0 with `OK`; not a deploy 0; **G, `nokeyid` and an absent active id, now 1**, naming entry 1 of `EXTERNAL_LINK_HMAC_KEYS` and `EXTERNAL_SESSION_ACTIVE_KEY_ID`; H, a 16-byte key, 1, without printing the key. `pnpm --filter @goproceed/app typecheck` rc 0 (`scripts/` is outside `tsconfig.json`'s `include`) | `scratchpad/preflight2/*.txt` | Rotation runbook |
| 5 | implementing (coordinator): rotation runbook | `infra/secret-rotation.md` written: the three stores (local, CI, the one hosted environment), an inventory of ten secrets from `apps/app/.env.example`, `apps/landing/.env.example` and `apps/mobile/.env.example` (mobile holds public values only), the vendors' order, and a section per secret: database role passwords (one password per role, so no overlap; a quiet window), the Supabase `sb_secret_` key, the HMAC key lists (overlap by key id; retire an id only after the longest grant or session lifetime), the Telegram token, webhook and worker secrets, `TELEGRAM_LINK_PEPPER` (no key id; replacing it invalidates unused link tokens and breaks the erasure registry's match, so rotate only on a leak), the Resend key, and the leak path. Facts checked in the repository: `apps/app/vercel.json` `ignoreCommand` skips non-production builds; `external_access_grants.expires_at` (`0049`) and session `idle_expires_at` / `absolute_expires_at`; `apps/app/app/internal/telegram/jobs/route.ts`; `apps/app/scripts/telegram-erase-identity.mjs`; `config.ts` minimum lengths; `.env.local` ignored (`.gitignore:15`). README-staging §3 and §4.3 point to it | Sources; `git check-ignore -v`; file paths | Documents |
| 6 | implementing (coordinator): documents | `version-0.0.md` gate 4's role-password box ticked with a dated note (2026-08-19 evidence, the 2026-09-15 scan, one hosted environment, Q-9). `version-0.1.md` §M0 gains the gate 14 entry after gate 10's, with its limits. `production-readiness.md` gate 14 gains an «Evidence, 2026-09-15» pointer; unticked items still 32. Runbook §5 intro (two gates closed), §5.7 status line and the disagreement note, §5.14 row 2, §8 question 1, each keeping the replaced text. STATUS «M0 gates» State cell, preamble sentence, «Next action» 1 and 3. Validators rc 0; `scripts/deploy-preflight-keys.test.mjs` (25) and `src/lib/external-link.test.ts` (29) pass; `pnpm --filter @goproceed/app typecheck` rc 0 | `node scripts/validate-canonical-docs.mjs`; `python3 scripts/sync-agents.py --check`; `pnpm exec vitest run scripts/deploy-preflight-keys.test.mjs src/lib/external-link.test.ts` | `gp-reviewer`, `gp-security` |
| 7 | reviewing (`gp-reviewer`, `gp-security`, native), round 1 on `685b6ab` | **Reviewer: changes requested** — R1-01 major (gate 14's key-ID bullet closed while `TELEGRAM_LINK_PEPPER` has no key id); R1-02 to R1-05 medium; R1-06 to R1-10 low; parity between the preflight and `loadRegistry` confirmed. **Security: changes requested** — S1-01 to S1-04 major (role-password leak leaves open sessions; older deployments keep old secrets; `sb_secret_` blast radius and legacy keys; incident steps), S1-05 to S1-09 medium, S1-10 to S1-14 low; no blocker | Review reports | Owner decisions; rework |
| 8 | rework (coordinator) | Owner decisions of 2026-09-15 recorded. Code: preflight and registry messages name entries by position; three cases added (reversed secret, short secret before the separator, duplicate id); a no-secret test over both paths and three orders; three tests that run `deploy-preflight.mjs` itself. 63 passed (34 in the new file, 29 in `external-link.test.ts`); **mutation**: the preflight's wiring loop removed → 2 failed; restored → 63 passed; typecheck rc 0. Exercise re-run with the exit code inside each file (A–I): 1, 1, 1, 1, 0, 0, 1, 1, 1; case I, a secret before the separator, printed nothing of it. History scan: `git log --all -p` over 75 refs and 1,025 commits for non-loopback connection strings with a password, `sb_secret_` values, `<id>:<base64 43+>` lists, JWT literals, Telegram token and `re_` shapes; 9 redacted hits, classified without printing values: two local Supabase demo JWTs (`iss` `supabase-demo`, roles `anon` and `service_role`), the `dev1` keys (decoding to `dev-only-link-key-` / `dev-only-session-k`), `re_authenticate_and_request` (an error code), and `app:…@db.internal` in a third-party agent profile (a non-routable example). Documents: the gate 14 closure entry removed from `version-0.1.md`; runbook §5 intro and §8 q1 back to one closed gate; §5.7 PARTIAL with progress; STATUS reverted with BL-085 in Open; `production-readiness.md` pointer says «not closed»; the `version-0.0.md` note narrowed; `infra/secret-rotation.md` rewritten for the findings; BL-085, BL-086 added; BL-024 depends on BL-085 and on protecting older production deployments | `scratchpad/dev010-r1-tests2.txt`, `dev010-r1-mut-wiring.txt`, `preflight3/*.txt`, `history-all.patch` (not kept in the repository) | `gp-reviewer` and `gp-security` re-check |
| 9 | reviewing (`gp-reviewer` round 2, `gp-security` re-check, native) on `ccd1163` | **Reviewer: changes requested**, no blocker: R1 resolutions in place; gate 14 consistently not closed; R2-01 (Deployment Protection advice could lock out the production alias), R2-02 (the tick's hit list omits the excluded local demo secret; no saved hit list), R2-03 to R2-06 low, O-1 to O-3 optional. **Security: changes requested**, no blocker: S1-01, S1-05, S1-07 to S1-14 fixed; S1-02, S1-03, S1-04, S1-06 partly; new S2-01 to S2-03 medium (protection advice; «compute under both peppers» could write a re-identifiable row; no `postgres` leak steps), S2-04 to S2-07 low | Review reports | Fix R2 and S2 |
| 10 | rework (coordinator), stated fixes | Runbook: delete older production deployments instead of switching protection, with the 2026-08-19 measurement and the unverified mapping; the lookup-first procedure for a repeat erasure after a pepper rotation; `postgres` leak steps (sessions, both login roles, catalogue comparison); legacy keys deactivated at once after a leaked JWT and new Auth users checked; Development scope removed, not updated (Vercel: Sensitive values only in Production and Preview); Instant Rollback sourced; the legacy-JWT claim cited to its real source; role query parameterised, `postgres` privileges and the direct host marked unverified, passwords kept off the command line; bot-token check without shell history. Tick: nine reported hits enumerated plus the rule exclusions. BACKLOG: dated evidence, full paths, BL-024 dependency dated and reworded. Tests: 8-character-window no-secret checks with non-repeating key material; the script test asserts the refusal message. The saved redacted hit list: 9 reported hits; excluded by rule 4 occurrences of the local demo secret key and 2 placeholder passwords (patch built before `ccd1163`: 75 refs, 1,025 commits). 63 passed; typecheck rc 0; validators rc 0. Not a rework round: no QA FAIL | `scratchpad/dev010-r2-tests.txt`, `history-scan-hits.txt` | `gp-qa`; `gp-security` re-check |
| 11 | reviewing (`gp-security`, native), narrow re-check on `2005ecb` | **Changes requested**, no blocker. S1-02, S1-03, S1-06, S2-01, S2-02, S2-04 to S2-07 fixed; S1-04 / S2-03 partly. It confirmed from `0081` that a «found» run with the old pepper writes no registry row and no identifiable data, that the lookup fails safe, that the session query spares the caller and that the catalogue queries are read-only. New: S3-01 medium (the `postgres` leak comparison misses `BYPASSRLS`, memberships, grants, views, triggers, publications, extensions, default privileges, role settings), S3-02 to S3-04 low | Security report | Fix S3 |
| 12 | verifying (`gp-qa`, native) on `2005ecb` | **No result**: the agent stalled and was stopped by the stream watchdog after 600 s. The worktree was clean afterwards (`git status --short` empty, HEAD `2005ecb`, no test process), so no mutation was left behind. Recorded as NOT RUN, not as a pass | `git status`; task notification | Re-dispatch `gp-qa` on the final head |
| 13 | rework (coordinator), stated fixes | S3-01: a schema diff first (`supabase db diff --linked` against a shadow database from the migrations; `supabase db dump --linked --role-only` for roles), then a wider read-only first look (role attributes incl. `rolbypassrls`, memberships, grants to `anon` / `authenticated` / the application roles, policies, definer functions, user and event triggers, jobs, publications, extensions, default privileges, role settings, views in `public`), and «a clean result is not proof». S3-02: `psql … -W` instead of `PGPASSWORD`; the bot token passed to `curl --config -` on stdin. S3-03: every sealed earlier pepper, run with the pepper that matched. S3-04: the HMAC typed at a `\prompt` in `psql`, `~/.psql_history` checked. Prepared as a script while `gp-qa` ran, dry-run on a copy (all seven replacements matched once), applied after | `scratchpad/dev010-s3-patch.py`; this diff | `gp-qa`; `gp-security` re-check of S3-01 |

## Findings and rework

| Finding ID | Severity | Trigger / location | Expected vs actual | Owner | Resolution and evidence |
|---|---|---|---|---|---|
| R1-01 | major | `version-0.1.md` gate 14 entry; readiness pointer; runbook §5.7 | Actual: the key-ID bullet closed while `TELEGRAM_LINK_PEPPER` has none | owner / coordinator | Owner chose to build a key-id pepper; gate 14 not closed; entry removed; §5.7 PARTIAL; BL-085 |
| R1-02 / S1-05 | medium | `deploy-preflight-keys.mjs:39`; `external-link.ts:89` | Actual: `<secret>:k1` printed the secret as a key id | coordinator | Both paths name the entry by position; tests over both paths and three orders; the script test and exercise I print nothing of the secret |
| R1-03 / S1-12 | medium / low | `secret-rotation.md` step 4 | Actual: «never prints its value», while Supabase key checks print a 10–12 character prefix | coordinator | Step 4 states what is printed and warns not to paste another secret there; shell-history advice for the bot token check |
| R1-04 / S1-07 | medium | Inventory and stores | Actual: no `postgres` password, SMTP credential, operator machines, Vercel scopes, operator access | coordinator | Stores table adds the password manager, every built deployment, operator machines and all three scopes; inventory adds `postgres`, legacy JWTs, Auth SMTP, operator access as a named exclusion |
| R1-05 / S1-06 | medium | Pepper section | Actual: an unfollowable audit-trail check; re-identification risk and the second-surrogate effect unstated | coordinator | Section rewritten from `0081`: re-identification, second surrogate, `already_erased` false, guard bypassed; keep the old pepper sealed, compute under both; operators updated together; BL-085 |
| R1-06 | low | Test coverage of the script | Actual: only the function was tested; the red was a missing module | coordinator | Three `spawnSync` tests; wiring mutation turns two red; the behavioural before/after is `preflight/G.txt` → `preflight3/G-…txt` |
| R1-07 / S1-08 | low / medium | Exercise files; the scan; the tick | Actual: no F, no exit codes; HEAD-only scan; «generated» unshown | coordinator | Exercise re-run A–I with exit codes; history scan over all refs; the tick states the scan's scope, that it is a pattern scan, and that generation was not recorded |
| R1-08 | low | Leak path vs bot-token section | Actual: `/token` as invalidation, doubted elsewhere | coordinator | Bot token: revoke via @BotFather (tutorial), confirm refused, then `getWebhookInfo` / `setWebhook` |
| R1-09 / S1-01 / S1-09 | low / major / medium | Role passwords | Actual: open sessions untouched; outage window includes composing; cleartext SQL; Production only | coordinator | Order: edit Vercel first, `\password` over `psql`, `pg_terminate_backend`, redeploy, prove the old password refused; every scope; unverified items marked |
| R1-10 / S1-13 | low | Duplicate key id; retirement wording | Actual: silently accepted; «every link and session signed under it» inaccurate | coordinator | Parity case added; «never reuse a key id»; retirement after 7 days / 12 hours with sources; leak wording corrected; refusal deferred to BL-086 |
| S1-02 | major | Older deployments | Actual: no step for deployments holding old values | coordinator | Stores table, order step 7 (no rollback), leak step 2; Telegram section: Deployment Protection scope before BL-024; BL-024 depends on it |
| S1-03 | major | Supabase keys | Actual: blast radius, legacy keys and Auth follow-up missing | coordinator | Blast radius quoted; deactivate legacy keys; after a leak, logs and JWT signing-key rotation with the documented wait |
| S1-04 | major | Leak path | Actual: store-level leak, access audit, where it travelled, webhook re-point missing | coordinator | Leak path steps 2–6 |
| S1-10 | low | Shared bot | Actual: unknown whether the landing shares the channel's bot | owner | Owner: separate bot; recorded in the stores table |
| S1-11 | low | Generation commands | Actual: none for three Telegram secrets | coordinator | `randomBytes(32).toString('base64url')` for every Telegram secret |
| S1-14 | low | «No Telegram secret set» | Actual: the landing's token may be set | coordinator | Sentence limited to the channel's secrets; the landing's delivery variables are the owner's record |
| R2-01 / S2-01 | medium | Runbook «Telegram secrets», leak step 2; BL-024 | Actual: Standard Protection advised, which on 2026-08-19 protected the `vercel.app` production alias too | coordinator | Delete older production deployments; protection change not advised without a custom domain; measurement cited; mapping unverified; BL-024 dated |
| R2-02 | medium / low | `version-0.0.md` tick; record row 8 | Actual: the enumeration omitted the local demo secret key; no saved hit list | coordinator | Tick enumerates the nine hits and the rule exclusions; `history-scan-hits.txt` saved (row 10) |
| R2-03 / S2-02 | low / medium | Pepper section | Actual: «compute under both peppers» unfollowable, and running the script with the old pepper could write a re-identifiable row | coordinator | Lookup-first procedure, marked unverified; «never run with the old pepper without that lookup»; re-keying noted for BL-085 |
| R2-04 | low | Legacy JWT date; Vercel scopes; Instant Rollback | Actual: unsupported citations | coordinator | Legacy claim cited to `deploy-preflight.mjs` (undated); scopes and rollback cited with dates (Sources) |
| R2-05 | low | BL-085, BL-086 Evidence | Actual: undated; short path | coordinator | «observed 2026-09-15 at `ccd1163`»; full paths |
| R2-06 / S2-04 | low | Role-password steps; bot-token history | Actual: hard-coded role; unverified privileges and host; password on the command line; zsh history advice wrong by default | coordinator | `<the role rotated>`; unverified markers with the Supabase roles page; `read -rs`; `HIST_IGNORE_SPACE` noted |
| S2-03 | medium | `postgres` password | Actual: no leak steps | coordinator | Leak steps: end its sessions, rotate both login roles, compare roles, policies, definer functions, jobs and event triggers with the migrations, treat the JWT secret as possibly exposed |
| S2-05 | low | Legacy JWT after a leak | Actual: deactivation only «once nothing uses them» | coordinator | Deactivate at once after a leaked JWT |
| S2-06 | low | No-secret tests | Actual: whole-string checks only | coordinator | 8-character windows over non-repeating key material |
| S2-07 | low | Development scope | Actual: «edit every scope … marked Sensitive» | coordinator | Remove from Development; Vercel quote |
| O-1 | optional | Script test | Actual: exit code only | coordinator | Asserts `EXTERNAL_LINK_HMAC_KEYS entry 1 is shorter than 32 bytes` |
| O-2 | optional | Criteria revision | Actual: «working tree over `685b6ab`» | coordinator | `gp-qa` checks the final head |
| O-3 | optional | Runbook §5.7 tick note | Actual: narrower than the tick | coordinator | Note lists all three limits |
| S3-01 | medium | Runbook, `postgres` leak step 3 | Actual: login names, policies, definer functions, jobs and event triggers only; `BYPASSRLS`, memberships, grants and views unseen | coordinator | Schema diff and role dump first (unverified against this project), a wider query list, and «not proof» |
| S3-02 | low | Role-password step 6; bot-token check | Actual: `export PGPASSWORD`; token as a `curl` argument | coordinator | `psql -W` (libpq advises against `PGPASSWORD`); `printf … \| curl --config -` |
| S3-03 | low | Repeat-erasure steps | Actual: assumes one earlier pepper | coordinator | Every sealed earlier pepper; run with the one that matched; keep them sealed until BL-085 |
| S3-04 | low | Repeat-erasure lookup | Actual: HMAC typed into SQL text | coordinator | `psql` `\prompt` and `:'h'`; not the SQL Editor; check `~/.psql_history` |

Rework count and hypothesis changes: review round 1's and round 2's findings were applied as stated fixes, and the owner's pepper decision narrowed the task from closing gate 14 to recording evidence toward it; no rework round used, since no QA FAIL preceded it.

## What is not true after this task

- **Readiness gate 14 is not closed.** `TELEGRAM_LINK_PEPPER` has no key id (BL-085), and one hosted environment exists with no separate production project (Q-9).
- **No secret has been rotated.** The runbook has not been exercised on the hosted project; steps marked *(unverified)* rest on no vendor page read on 2026-09-15.
- **The hosted state is the 2026-08-19 record.** No hosted project, dashboard, scope or secret was read by this task. Whether the legacy Supabase keys are still active, which Vercel scopes hold which values, and the project's current Deployment Protection were not observed.
- **Older production deployments may still be public and hold today's values**; the runbook says what to do on a leak, but nothing was changed.
- **The preflight checks shape, not validity**: a wrong password or a revoked key passes the build.
- **The registry still accepts a duplicate key id** and the same secret in both key spaces (BL-086).
- **The history scan is a pattern scan**, not a secret scanner, over the refs this clone holds.
- **How the hosted role passwords were generated is not recorded.**
- **Operator account tokens have no rotation procedure.**
- **The new tests have not run in CI** (GitHub Actions starts no jobs until October 2026).

## Acceptance evidence

| Criterion | Required? | Checked revision | Command or evidence | PASS / FAIL / NOT RUN | Limitation |
|---|---|---|---|---|---|
| 1. The preflight refuses a malformed `*_HMAC_KEYS` and an `*_ACTIVE_KEY_ID` not in its list, agreeing with the runtime registry over a table of cases, and the script applies it | yes | final head (see Completion) | Behavioural red `preflight/G.txt` (exit 0 before); parity table and script tests: 34 passed in the new file; wiring mutation → 2 failed; `preflight3/G-…txt` exit 1 | PASS | — |
| 2. The preflight's existing refusals and its `OK` path are unchanged | yes | final head (see Completion) | `preflight3/` A–F with exit codes: 1, 1, 1, 1, 0 (`OK`), 0 | PASS | assisted: fake values, `DEPLOY_PREFLIGHT=1`, no Vercel build |
| 3. Neither the preflight nor the registry prints key material, whatever the entry order | yes | final head (see Completion) | No-secret test over both paths and three orders; script test; `preflight3/I-reversed-secret.txt` contains 0 occurrences of the secret | PASS | — |
| 4. `infra/secret-rotation.md` covers the deployments' secrets and stores, in the vendors' documented order, with the leak path; README-staging points to it | yes | final head (see Completion) | Row 8; Sources; README-staging §3 and §4.3 links | PASS | not exercised on the hosted project; steps marked unverified |
| 5. `version-0.0.md` gate 4's role-password box is ticked with dated evidence and its limits | yes | final head (see Completion) | `git diff a5fd136 -- docs/delivery/version-0.0.md`; history scan (row 8) | PASS | owner-reported: the hosted fact is the 2026-08-19 record |
| 6. Gate 14 is recorded as not closed everywhere, with BL-085 as its blocker, and gate 10 remains the only closed gate | yes | final head (see Completion) | No gate 14 entry in `version-0.1.md`; readiness pointer «not closed»; runbook §5 intro, §5.7, §5.14 row 2, §8 q1; STATUS; BL-085; `grep -c -- '- \[ \]' docs/delivery/production-readiness.md` → 32 | PASS | — |
| 7. `pnpm --filter @goproceed/app typecheck`, the tests, `pnpm validate:canonical-docs` and `pnpm validate:agents` pass | yes | final head (see Completion) | typecheck rc 0; `vitest run scripts/deploy-preflight-keys.test.mjs src/lib/external-link.test.ts` 63 passed; `node scripts/validate-canonical-docs.mjs` rc 0; `python3 scripts/sync-agents.py --check` rc 0 | PASS | macOS, Node 24.18.0 |
| 8. CI `verify` on the PR head | no | — | — | NOT RUN | environmental: GitHub Actions starts no jobs until October 2026 (owner, 2026-09-14) |

## Sources

- Vercel, «Environment variables», `https://vercel.com/docs/environment-variables` (last updated 2026-08-20), read 2026-09-15: changes apply only to new deployments.
- Vercel, «Rotating environment variables», `https://vercel.com/docs/environment-variables/rotating-secrets` (last updated 2026-07-15), read 2026-09-15: new credential, edit, redeploy, verify, then invalidate the old one.
- Supabase, «API keys», `https://supabase.com/docs/guides/getting-started/api-keys`, and «Migrating to publishable and secret API keys», `https://supabase.com/docs/guides/getting-started/migrating-to-new-api-keys`, read 2026-09-15 through Supabase MCP `search_docs`: `sb_secret_` keys are created and revoked independently; rotate by creating a new key, switching, deleting the old one.
- Telegram, «Bot API» `setWebhook` and «Getting updates», `https://core.telegram.org/bots/api`, read 2026-09-15: `secret_token` 1–256 of `A-Z a-z 0-9 _ -` in `X-Telegram-Bot-Api-Secret-Token`; an unsuccessful request is repeated and given up «after a reasonable amount of attempts».
- Telegram, «Bots: features», `https://core.telegram.org/bots/features`, read 2026-09-15: `/token` generates a new token for a compromised or lost one; nothing said about the old token.
- Resend, «API keys», `https://resend.com/docs/dashboard/api-keys/introduction`, read 2026-09-15 (no date shown): a key's value cannot be viewed after creation; keys are deleted in the dashboard.
- Telegram, «Bot tutorial», `https://core.telegram.org/bots/tutorial`, read 2026-09-15: a token «can also be revoked at any time via @BotFather»; `getWebhookInfo` and `deleteWebhook`, `https://core.telegram.org/bots/api#getwebhookinfo`.
- Vercel, «Deployment Protection», `https://vercel.com/docs/deployment-protection` (last updated 2026-08-28), read 2026-09-15: Standard Protection protects every deployment except production domains; «(Legacy) Pre-Production Deployments» does not protect past production deployments.
- Supabase, «JWT signing keys», `https://supabase.com/docs/guides/auth/signing-keys`, read 2026-09-15: non-expired access tokens remain accepted after rotation; wait at least 1 hour 15 minutes before revoking the legacy JWT secret. «Migrating to publishable and secret API keys»: secret keys bypass RLS; legacy keys are deactivated in Settings → API Keys, reversibly.
- Vercel, «Managing environment variables across environments», `https://vercel.com/docs/environment-variables/manage-across-environments` (last updated 2026-08-20), read 2026-09-15: `vercel env ls <environment>`; sensitive variables «are only available in production and preview environments»; `vercel pull` writes `.vercel/.env.<environment>.local`; `vercel env rm`.
- Vercel, «Instant Rollback», `https://vercel.com/docs/instant-rollback` (last updated 2026-07-07), read 2026-09-15: a rollback does not update environment variables and rolls back to a previous build.
- Vercel, «Vercel Authentication», `https://vercel.com/docs/deployment-protection/methods-to-protect-deployments/vercel-authentication` (last updated 2026-08-28), read 2026-09-15: API values `prod_deployment_urls_and_all_previews` (Standard Protection), `all`, `preview` (Only Preview Deployments).
- Supabase, «Roles, superuser access and unsupported operations», `https://supabase.com/docs/guides/database/postgres/roles-superuser`, read 2026-09-15: «Superuser access is not given»; nothing on `pg_signal_backend`.
- Supabase CLI reference, «supabase db diff», `https://supabase.com/docs/reference/cli/supabase-db-diff`, and «supabase db dump», `https://supabase.com/docs/reference/cli/supabase-db-dump`, read 2026-09-15: the diff runs migra against a shadow database built from `supabase/migrations`, all schemas by default, known to miss publications, storage buckets and `security_invoker` views; the default dump holds no data or custom roles, `--role-only` dumps cluster roles, and `auth`, `storage` and extension schemas are excluded.
- PostgreSQL 18, «Environment variables» (libpq), `https://www.postgresql.org/docs/current/libpq-envars.html`, and «psql», `https://www.postgresql.org/docs/current/app-psql.html`, read 2026-09-15: `PGPASSWORD` «is not recommended for security reasons»; `-W`; `\prompt`; `:'name'`; `HISTFILE` defaults to `~/.psql_history`.
- curl manual, `https://curl.se/docs/manpage.html`, read 2026-09-15: `--config -` reads the config from stdin; `url = "…"` format.
- PostgreSQL 18, «ALTER ROLE», `https://www.postgresql.org/docs/current/sql-alterrole.html`, and «System administration functions», `https://www.postgresql.org/docs/current/functions-admin.html`, read 2026-09-15: a cleartext password may be logged, `\password` avoids it; `pg_terminate_backend` terminates a session.

## Completion / handoff

- Changed files: `apps/app/scripts/deploy-preflight.mjs`, `apps/app/scripts/deploy-preflight-keys.mjs` (new), `apps/app/scripts/deploy-preflight-keys.test.mjs` (new), `apps/app/src/lib/external-link.ts` (messages), `infra/secret-rotation.md` (new), `infra/README-staging.md`, `docs/delivery/version-0.0.md`, `docs/delivery/production-readiness.md`, `docs/delivery/pilot-execution-runbook.md`, `docs/STATUS.md`, `docs/BACKLOG.md`, this record, `docs/tasks/README.md`.
- Review independence: independent — `gp-reviewer` rounds 1 and 2, `gp-security` round 1 and two re-checks; a `gp-security` re-check of S3-01 and `gp-qa` pending (the first `gp-qa` dispatch stalled with no result).
- Verified scope: criteria 1–7, by the coordinator.
- Remaining risks / blocked requirements: «What is not true» above.
- Next bounded action and owner: coordinator — validators, commit, `gp-reviewer` and `gp-security` re-check, `gp-qa`; then BL-085 as its own task.
- Final state and reason: rework.
