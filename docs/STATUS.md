# GoProceed status

The current state of GoProceed, area by area: what is merged, what runs, what is verified and what is open. The rules for changing it are in [docs/README.md](README.md) «Observation layers» and the paragraph below; the precedence ladder in the same file outranks everything here.

Only the coordinator updates this file; specialist roles return handoffs. **Every entry is an observation as of its date.** Re-check git, CI and `supabase/migrations/` before relying on one. A row cites what a reader can re-run or open; where an older document disagrees with git, the row follows git and the disagreement is listed under «Open issues».

`scripts/validate-canonical-docs.mjs` fails when the latest-migration marker below differs from the last file in `supabase/migrations/`. Updating the number means re-observing that row, not only the number.

## Current — 2026-09-13

Observed at `main` `dbd4c36`. Nothing here queried a hosted database or the Vercel API; hosted state comes from the dated records cited.

On 2026-09-14 DEV-005 re-observed the «Agent workflow» row at `5480d2e`. In the other rows it replaced `TODOS.md:<n>` citations with [BACKLOG.md](BACKLOG.md) entries, and added backlog ids to their «Open» cells, without re-observing those rows. Two sentences it added come from the triage, not from a re-observation, and each cites its entry: the PWA row's staging checks (BL-010) and the Outreach row's missing customer document (BL-003). The CI row's evidence for the closed cases cites #69 (`9b9bf65`) in place of the `TODOS.md` entry. DEV-006 re-observed the «Agent workflow» row at `6fd98d0` on 2026-09-14 and rewrote «Next action» items 1 and 2; it re-observed no other row. DEV-007 re-observed the «Agent workflow» row at `d8a860a` on 2026-09-14, rewrote the «Outreach» row's last sentence from its personal-data review, and rewrote «Next action» items 1 and 2. It also added the owner's landing-reference decision of that day to the «Landing» row's «Open» cell, without re-observing that row. DEV-009, on 2026-09-14 at `6e4efdf`, changed the «M0 gates» row in three places: it added readiness gate 10's dated entry to the State cell, added its own reading of why the checklist stays unticked («because closure is recorded in `version-0.1.md`, not by ticking»), and replaced the Open cell's last sentence, about the card half of item 9, with BL-084. It re-observed none of the row's other claims, including the entries for gates 2 and 4, and rewrote «Next action» items 1 and 3. DEV-010, on 2026-09-15 at `a5fd136`, added to the same row's Open cell that gate 14 waits on BL-085, re-observed none of the row's other claims, and rewrote «Next action» items 1 and 3. DEV-011, on 2026-09-15 on its branch, changed the «Database migrations» row (the latest-migration marker, the apply-record count and the local-database sentence), the «M0 gates» row's Open cell, and «Next action» items 1, 3 and 4; it re-observed only `supabase/migrations/` and the local database head. DEV-012, on 2026-09-15 at `48ba14e`, added gate 12's evidence to the «M0 gates» row's Open cell, replaced that cell's DEV-011 clause after #92 merged, rewrote the «Database migrations» row's `0085` sentence, rewrote «Next action» items 1, 2 and 3, marked Q-10 answered in the «M0 gates» row, and annotated the «Outreach» row's BL-079 clause; it re-observed none of the rows' other claims. DEV-013, on 2026-09-16 at `b9dcf6b`, added item 11's checker to the «M0 gates» row's Open cell and rewrote «Next action» items 1 and 3; it re-observed none of the rows' other claims. DEV-014, on 2026-09-17 at `35578e7`, changed the «M0 gates» row's Open cell and «Next action» items 1 and 3; DEV-015, on 2026-09-17 on its branch, changed the «Database migrations» row (the marker, the apply-record count and the local-database sentence, re-observing `supabase/migrations/` and the local database head), the «M0 gates» row's Open cell and «Next action» items 1, 3 and 4. Neither re-observed the rows' other claims. DEV-016, on 2026-09-17 at `191dd79`, changed the «Database migrations» row's `0086` merge clause (from `git log`), the «M0 gates» row's Open cell and «Next action» items 1 and 3; it re-observed none of the rows' other claims. DEV-017, on 2026-09-18 on its branch, changed the «Database migrations» row (the marker, the apply-record count and the local-database sentence, re-observing `supabase/migrations/` and the local database head), the «M0 gates» row's Open cell and «Next action» items 1, 3 and 4. DEV-018, on 2026-09-18 at `1bf5cea`, changed the «M0 gates» row's State and Open cells (item 11 closed) and «Next action» items 1 and 3, re-observing only the registry and its own run. DEV-019, on 2026-09-18 on its branch, changed the «Database migrations» row (the marker, the file and apply-record counts and the local-database sentence, re-observing `supabase/migrations/` and the local database head) and «Next action» items 1, 3 and 4; it re-observed none of the rows' other claims. DEV-020, on 2026-09-18 on its branch, changed the «Database migrations» row (the marker, the file and apply-record counts and the local-database sentence, re-observing `supabase/migrations/`, `git log` for #100 and the local database head) and «Next action» items 1, 3 and 4; it re-observed none of the rows' other claims. DEV-021, on 2026-09-18 on its branch, changed «Next action» items 1 and 3 only; it re-observed nothing else. DEV-022, on 2026-09-19 on its branch, changed «Next action» items 1 and 3 and added item 4's deploy note; it re-observed nothing else. DEV-023, on 2026-09-19 on its branch, changed «Next action» items 1 and 3 only; it re-observed nothing else. DEV-024, on 2026-09-19 on its branch, changed «Next action» items 1 and 3 only; it re-observed nothing else. DEV-025 and DEV-026, on 2026-09-19 on their branch, each added a clause to the «Landing» row's Open cell; neither re-observed that row's other claims. DEV-030, on 2026-09-23 on its branch from `44e05cd`, rewrote the «Outreach» row's `outputs/` sentences after moving the directory, rebuilt this paragraph's DEV-022 to DEV-026 sentences, which a merge had garbled, from the task index and those tasks' records, and rewrote «Next action» items 1 to 3; it re-observed the row's `outputs/` claims and `git log` for #105, nothing else. DEV-031, on the same branch, added itself to «Next action» item 1. DEV-032 and DEV-033, on 2026-09-23 on the «Evidence» branch, added a clause to the «M0 gates» row's Open cell and rewrote «Next action» items 1 and 2 (re-observing `git log` for #108); they re-observed nothing else. DEV-043, on 2026-09-23 on its branch, changed the «Database migrations» row's marker and first sentence, re-observing `supabase/migrations/`, the local database head, PR #115's migration and the hosted migration list; it re-observed nothing else. DEV-044, on the same branch, changed the same row's marker and migration sentences for `0097`; it re-observed nothing else. Both, after review, prefixed «Next action» item 1 with their pull request, and the «v1 API» row with the scope-row and route-file counts of their branch; they did not repeat that row's route-per-row pass.

| Area | State | Evidence | Open |
|---|---|---|---|
| Database migrations | The tree ends at <!-- latest-migration -->`0097` (96 files: `0001`–`0094`, `0096` and `0097`) observed 2026-09-23 on the branch of DEV-043 and DEV-044; `0095` belongs to the unmerged PR #115 (DEV-042) and is on the hosted project already, and `0096` (DEV-043, the grant table's column UPDATE) and `0097` (DEV-044, the end of a responsibility assignment) are unmerged. `0090` (DEV-036, the evidence purge's principal), `0091` (DEV-037, purge claim fencing), `0092` (DEV-038, abandoning an upload after lost access) and `0093` (DEV-040, the evidence bucket's type allow-list) and `0094` (DEV-038's review fix) merged in #112 (`dc24922`, 2026-09-23). `0089` merged with DEV-020 (BL-103). `0088` merged in #100 (`902c214`, 2026-09-18). `0087` merged in #98 (`1bf5cea`, 2026-09-18). `0086` merged in #96 (`191dd79`, 2026-09-17). `0085` merged in #92 (`48ba14e`, 2026-09-15). `0084` was added in `bd08da9` (2026-09-09) and merged in #78 (`a75b0d8`, 2026-09-13). Hosted applies on record: staging 58/58 on 2026-08-19, and `0059`–`0094` on 2026-09-23 ([DEV-041](tasks/DEV-041-staging-migrations-0059-0094.md)), so the hosted head equalled `main`'s; the Supabase connector's migration list read on 2026-09-23 (DEV-043) ends at `0095`, PR #115's, and `0096`–`0097` are not applied there | `ls supabase/migrations`; [infra/README-staging.md](../infra/README-staging.md) §Status | All 94 applied to the hosted project (DEV-041). Who pushes, on what trigger, and the rollback are undecided (runbook §10 Q-9). The local database was at `0085` on 2026-09-15, applied by hand for DEV-011, at `0086` on 2026-09-17 for DEV-015, at `0087` on 2026-09-18 for DEV-017, at `0088` on 2026-09-18 for DEV-019, at `0089` on 2026-09-18 for DEV-020, at `0094` on 2026-09-23 for DEV-036 to DEV-040, and at `0097` on 2026-09-23 for DEV-043 and DEV-044 (without `0095`), each applied by hand |
| v1 API | *[2026-09-23, DEV-043/DEV-044, counted on their branch: 78 v0.1 scope rows (M1 38, M2 9, M3 6, M4 6, M5 6, M6 3, M7 10) and 69 `route.ts` files; the route-per-row pass below was not repeated.]* 75 v0.1 scope rows (M1 35, M2 9, M3 6, M4 6, M5 6, M6 3, M7 10); 66 `route.ts` files under `apps/app/app/v1`. A read-only subagent pass found a route file exporting the method for every scope row, and one route in no scope file (`POST /v1/organizations`) | `technical/openapi/scope-v0.1.csv`; `find apps/app/app/v1 -name route.ts` | No milestone M1–M7 is formally closed, and no closure procedure exists (runbook Q-2). The route check covers method exports, not behaviour |
| PWA field client (`apps/app`) | *[2026-09-23, DEV-035: RETIRED by the owner before the parity gate — `app/(app)/**` removed, the dashboard is `apps/app`'s root; ADR-009 «Amendment, 2026-09-23». Merged as PR #110 (`3141a33`) and observed in production at 09:58 UTC: the PWA pages and manifest are gone from `goproceed-app`; the field client is `apps/mobile` at `goproceed-field` (row «Mobile»).]* Built, and the pilot's working field client. Production at `goproceed-app.vercel.app` was recorded on 2026-08-19 (`/login` 200, one owner sign-in on a laptop). Last page change: #72 (Daylight, 2026-09-05) | `infra/README-staging.md` §Status; `git log -- 'apps/app/app/(app)'` | The parity gate (runbook §8.3, ADR-009) is open: iPhone measurements exist from 2026-08-21, none from Android (BL-001, BL-002). The staging checks §6.1–§6.8 have no recorded run (BL-010) |
| Office dashboard | Plan D slices D0–D3 are merged: #45 (2026-08-22), #46 and #48 (2026-08-23), #54 (2026-08-29); its Daylight pass is in #72. D4, members and access, has no route | Merge commits on `main`; `apps/app/app/(dash)/` *[path corrected 2026-09-23, DEV-035: the tree was `app/dash/`, and it serves `/` now — `/dash/**` redirects]* | D4 waits on the member-identity decision (runbook Q-15; BL-045). The dashboard shows no assurance level (M0 item 6) |
| Landing (`apps/landing`) | Vercel project `goproceed-landing`, first deployed 2026-08-20. Daylight and prototype parity merged in #71–#77 (2026-09-05 to 2026-09-08). `/api/pilot` delivers a request by Telegram or Resend and stores nothing | `infra/README-staging.md:454-487`; `apps/landing/app/api/pilot/route.ts` | No record says production serves the 2026-09-08 tree. The five delivery variables are owner actions (`infra/README-staging.md:468-471`) with no record of being set. On 2026-09-14 the owner made https://parlo-black.vercel.app/ the landing's reference, keeping the prototype's colours; the rebuild is BL-082. DEV-025 (2026-09-19, on its branch, unmerged and undeployed) splits the landing into `/`, `/product`, `/roles` and `/pilot` with the form on `/pilot` only; it took block types from the reference. DEV-026 (2026-09-19, same branch, unmerged and undeployed) then rebuilt the frame, the home page, the route, the application view, the FAQ and the closing block «1 в 1» after the reference in the Daylight colours, on the owner's correction of that day; the blocks the reference has no composition for keep their earlier form, and BL-082 stays open on those and on the token rulings |
| Mobile (`apps/mobile`) | *[2026-09-23, DEV-035: re-observed serving at 08:32 UTC — Ukrainian sign-in page and manifest; a cross-origin read to `goproceed-app` `/v1/projects` passes CORS (401 unauthenticated); deployed commit not observed. With the PWA retired, this is the field client.]* Expo SDK 57 (`expo` 57.0.9), shipped only as a web export at Vercel `goproceed-field` (created 2026-08-21; iPhone Safari sign-in measured). Never built with EAS; `app.json` has no EAS project id; no store listing | `apps/mobile/package.json`, `app.json`, `eas.json`; [apps/mobile/AGENTS.md](../apps/mobile/AGENTS.md) | The parity gate (§8.3). The Daylight pass of `apps/mobile` was deferred by the owner on 2026-09-05 (BL-041) |
| Telegram channel (ADR-011, M7) | Merged and enabled in no environment: #58 (2026-09-03, `0061`–`0081`, including #62's `0080` and #65's identity erasure `0081`, both merged into its branch), #68 and #69 (2026-09-04; #69 added `0082`–`0083`), #78 (the card's tag and source, `0084`, 2026-09-13) | Merge commits; `apps/app/vercel.json` has no crons, so `app/internal/telegram/jobs/route.ts` has no caller; `src/lib/telegram/ingress.ts` has no rate-limit code | Before any environment enables the webhook: the Task 13 edge rate limit, a scheduler for the jobs route (Q-12), and the real-group staging pass (Q-17, blocked by M0's real-data rule); BL-024. Retention durations are NULL (BL-005) |
| M0 gates | Items 9, 10 and 11 are closed, by their dated entries in `version-0.1.md` §M0 (gate 10 on 2026-09-14, [DEV-009](tasks/DEV-009-m0-gate10-evidence.md); gate 11 on 2026-09-18, [DEV-018](tasks/DEV-018-gate11-closure.md)); the other nine are open. The `production-readiness.md` checklist has 32 open items and none checked, because closure is recorded in `version-0.1.md`, not by ticking; dated evidence entries exist for gates 2 and 4 (2026-09-03) | `grep -c -- '- \[ \]' docs/delivery/production-readiness.md`; runbook §5.1–§5.12, measured 2026-09-03 | Owner decisions Q-3 (domain), Q-4 (retention durations), Q-9, Q-10 (malware control; answered for the pilot on 2026-09-15, DEV-012), Q-11 (export), Q-12 (monitoring). BL-084: the act footer's «Реєстр будівельних норм» wording. Item 7 (readiness gate 14) has its rotation runbook and preflight checks ([DEV-010](tasks/DEV-010-m0-gate14-evidence.md)) and key ids on the Telegram HMAC keys ([DEV-011](tasks/DEV-011-telegram-hmac-key-ids.md), merged in #92, `48ba14e`); it waits on Q-9. Item 12 (readiness gate 12) has its built halves exercised and the owner's pilot malware acceptance recorded ([DEV-012](tasks/DEV-012-m0-gate12-evidence.md)); it waits on export (item 3), BL-088 and BL-089; BL-089 and BL-088 are closed on the «Evidence» branch (DEV-032, DEV-033, unmerged), and the decoding cost of an at-limit image (BL-129) and the channels the size check does not read (BL-132) remain for the gate. Item 11 (readiness gate 11) is **CLOSED on 2026-09-18** by the dated entry in `version-0.1.md` §M0 ([DEV-018](tasks/DEV-018-gate11-closure.md)): the coverage checker of [DEV-013](tasks/DEV-013-m0-gate11-coverage-checker.md), 74 covered registry rows with no gap, and the unfiltered package run passing 786 tests in 55 files with none skipped; its *Limits* bullet governs what that means |
| CI | `main` green on every completed run observed: 34771325867 (#81), 34758143582 (#80), 34754767259 (#79). Run 34778658900 (#82, `dbd4c36`) was in progress. The eighteen red `apps/app` cases closed on 2026-09-04. Supabase CLI pin `2.115.0` | `gh run list --workflow ci --branch main`; #69 (`9b9bf65`); `.supabase-cli-version` | The runbook's statements of the eighteen cases (dated 2026-09-03) still read as open. `test-strategy.md` §Baseline (C-16). No quarantine ledger (Q-8), no supply-chain gate (Q-16) |
| Agent workflow (re-observed 2026-09-14 at `d8a860a`) | The `gp-*` roles and task records are the process. DEV-001 to DEV-006 are done: #79 to #83 merged on 2026-09-13; #84 (`5480d2e`), #85 (`bdbf64b`), #86 (`6fd98d0`) and #87 (`d8a860a`) on 2026-09-14. DEV-007 (design sources of truth) is this change. Open and deferred work is in [BACKLOG.md](BACKLOG.md) | [docs/tasks/README.md](tasks/README.md); `git log --merges --oneline -10` | The retired workflow plugin stays enabled at user level, owner-accepted (DEV-002 criterion 8b); `CLAUDE.md` and `docs/ai-workflow.md` still say it is disabled. |
| Outreach | Last evidenced send: 2026-07-28, 21 emails. Four follow-up materials drafted 2026-08-23, unsent. A ten-company pilot outreach pack prepared 2026-08-25, unsent | [outreach-log.md](discovery/outreach-log.md); [2026-08-23-validation-push.md](discovery/2026-08-23-validation-push.md); [outputs/README.md](../outputs/README.md) | The send and the URL it points at are owner decisions (runbook Q-3; BL-003, BL-004). Assumptions A-1 to A-8 are unvalidated, and no real customer document is on hand. The prospecting session's 250 files, which hold personal data of natural persons (DEV-007's review), left the tree on 2026-09-23 (on its branch, unmerged) for private storage outside the repository behind the pointer [outputs/README.md](../outputs/README.md) (BL-079, owner 2026-09-15; [DEV-030](tasks/DEV-030-outputs-private-storage.md)); commit `bbfc705` still holds them, with no history rewrite. The redaction of the outreach routes and customer names stays the owner's (BL-080, deferred) |
| Deployments | Three Vercel projects are documented: `goproceed-app` (Production builds only, 2026-08-19), `goproceed-landing` (2026-08-20), `goproceed-field` (2026-08-21) | `infra/README-staging.md`; for `goproceed-app`, the Vercel API on 2026-09-23 | `goproceed-app` production serves `4434314` (the redeploy `dpl_9LUw1aKx7GEw5w41Q4HkJzCbR8z7`, READY 2026-09-23 12:33 UTC, preflight OK) with `PURGE_DB_URL` and `CRON_SECRET` set ([DEV-041](tasks/DEV-041-staging-migrations-0059-0094.md)); four cron entries registered, and a manual run answered 200 (12:51 UTC). What the other two projects serve was not observed. No custom domain (Q-3) |

## Review independence

- **From 2026-09-13**, root AGENTS.md requires an independent `gp-reviewer` and `gp-qa` on every behavior change. DEV-003 ran them as native `gp-*` roles; DEV-002 ran them as independent subagents through the Markdown fallback; DEV-001 used an independent upstream reviewer persona, with same-session verification of its fixes.
- **Before 2026-09-13**, a slice's review is whatever its gate record under `docs/superpowers/plans/evidence/` records; [docs/superpowers/README.md](superpowers/README.md) lists the specs and their outcomes. The runbook's §4.3 measured that the product, engineering and design review gates last produced a verdict on 2026-07-31.
- Rows above that cite only merge commits (dashboard slices, landing, mobile) carry the review their own PR or gate record shows; this file does not re-grade them.

## Open issues

Places where a live document disagrees with git on 2026-09-13. Each is a correction owed, not made here.

- **The eighteen red cases and the card tag.** Runbook §1.1, §1.3, §1.6, §5.9, §5.11, §5.14 and §5.15, and ADR-011 §"Status against the runtime", describe both as open. #69 closed the first on 2026-09-04 and #78 the second on 2026-09-13.
- **Hosted migrations.** `docs/delivery/version-0.1.md` (last reviewed 2026-08-08) says `0041`–`0050` were never applied; the staging record says 58/58 on 2026-08-19. The runbook counts 23 unapplied migrations; the tree now has 26 after `0058`. The «ten migrations» counts in `version-0.1.md:207` and `production-readiness.md:150` are BL-074.
- **`infra/README-staging.md` contradicts itself** on whether anything has run, on whether `goproceed-landing` has environment variables, and on where `apps/mobile/vercel.json` lives.
- **`TODOS.md`'s Plan D entry** says D1–D4 remain; D1–D3 are merged (runbook C-12). D4 is BL-045. `TODOS.md` is frozen since DEV-006, so the sentence stays uncorrected there; the backlog entry is current.
- **The card's closure date.** `TODOS.md` and ADR-011 say 2026-09-08; the commits are dated 2026-09-09 and the merge 2026-09-13.
- **ADR approval.** Runbook §10 Q-6 and §1.1's «the approval procedure is still undefined in repo» call the procedure undefined; `docs/README.md` «ADR lifecycle and approval» defines it from 2026-09-13. Runbook §1.4's last row cites `docs/README.md:180-188` for a clause DEV-004 removed.

## Next action

1. *[2026-09-23, DEV-043/DEV-044, on their branch:]* the «Отзыв и завершение» pull request (BL-021, BL-015; [ADR-014](decisions/ADR-014-revoke-access-and-end-responsibility.md)) waits for the owner's merge; migrations `0096`–`0097` are then owed to the hosted project, which is at `0095` (PR #115's). The «Evidence» pull request merged in #109 (`b16fc9b`): [DEV-032](tasks/DEV-032-evidence-signed-read-download.md) (BL-089: the stored type must be the detected one; signed reads issued as downloads), [DEV-033](tasks/DEV-033-image-size-limits.md) (BL-088: image size limits read from the header) and [DEV-034](tasks/DEV-034-storage-errors-without-keys.md) (BL-033: storage errors without keys). The «Данные» pull request, DEV-030 and DEV-031, merged in #108 (`27dea79`). DEV-024 merged in #105 (`0514602`); DEV-022's deploy note stays in item 4.
2. The owner makes the backup of `~/GoProceed-private/outputs/` and rules BL-080 when ready (deferred); a history rewrite of `bbfc705` would be a separate decision. For the «Evidence» work the owner decides BL-128 (a blocked upload's quota), supplies BL-131's phone samples, may revise DEV-033's image limits (268,402,689 px; 65,535 px an edge), and weighs BL-129 (the decoding cost of an at-limit image) and BL-126 (hosted Storage unmeasured) before real data.
3. BL-109 closed with #105 and BL-108 with #104; open from this series: BL-110, BL-111, BL-113, BL-114, BL-115; or §5.14 order 5 (item 2's durations, then item 4).
4. Before any environment enables the Telegram webhook: BL-024 (the Task 13 edge rate limit, the scheduler decision Q-12). The migration push decision (Q-9) now covers 31 migrations on `main` (36 with this branch's `0090`–`0094`), with `0087` (merged in #98), which must not reach a hosted project before the application build that declares the intent's workspace, `0088` (DEV-019), which should follow the build that stops storing the invitation token, and `0089` (DEV-020), which either order tolerates. **Deploy note (DEV-022, owner 2026-09-19):** the first build carrying DEV-022 answers a retry of a `/v1` command (other than `organizations.create` and `import_files.add`, which keep their own hashes) first sent before the deploy, with the same `Idempotency-Key`, with 409 `IDEMPOTENCY_CONFLICT` instead of the stored response (stored hashes covered the body only); a rollback does the same for records the new build wrote. Such a 409 is a refusal, not a sign the first request failed: check the register before submitting again (the new-assignment form keeps one key per form). No data changes and no migration.
