# v0.1-M2-B0 gate record

**Date:** 2026-08-03
**Branch:** `claude/m2-b0-foundations`, from `main` @ `7728f47` (10 commits ahead)
**Plan:** [2026-08-01-goproceed-v0.1-m2-b0-foundations.md](../2026-08-01-goproceed-v0.1-m2-b0-foundations.md)
**Spec:** [2026-08-01-goproceed-v0.1-m2-b0-foundations-design.md](../../specs/2026-08-01-goproceed-v0.1-m2-b0-foundations-design.md)
**Task:** [task-5-brief.md](../../../../.superpowers/sdd/2026-08-01-goproceed-v0.1-m2-b0-foundations/task-5-brief.md)

## The three rulings

Step 1 (rendering the candidates in `apps/mobile` for the owner) was already
done before this task started. The owner ruled on 2026-08-03. What follows is
the record of each ruling as written into `packages/tokens/src/tokens.json`.

### `line` = `#D9DBD5`, opaque

> Owner ruled at a render, 2026-08-03: the document's value, #D9DBD5, opaque.
> Borders do not composite. This is the cheaper branch for React Native: one
> value, no per-surface pre-composited variants, and no compositing logic
> anywhere in the native module — a translucent line would have needed a
> hand-authored blend per surface it sits on (paper, white, ink-950), because
> React Native authors border colours opaque and has no custom properties to
> composite through.

The document (`docs/05-design-system.md:29`) and the shipped prototype
(`packages/ui/src/tokens.css:23`, now deleted) disagreed in *kind*, not just
value: `#D9DBD5` opaque versus `rgba(72, 76, 94, .17)`. That is not a
question a measurement or a "rendered beats written" rule can settle — a
17%-alpha grey over `paper`, `white` and `ink-950` produces three different
effective colours, and React Native has no mechanism to composite a
translucent border colour against whatever surface it happens to sit on. The
owner's ruling picks the opaque value, which is also the cheaper one to
implement: `color["line"]` is a single value everywhere, with nothing to
pre-compute per surface and no compositing logic to write, test, or keep in
sync as new surfaces are added.

### `blue-500` = `#3756a1`

> Owner ruled at a render, 2026-08-03: #3756a1, the value that already fills
> the informational/submitted role at apps/demo/src/styles/theme.css:143
> (--color-info-foreground and --color-readiness-review-foreground). Chosen
> over the document's #5278D8, which survives only as a focus outline at
> apps/demo/src/styles.css:527; --blue: #65719a, shipped but never used as a
> status; and --atlas-blue: #18376a.

Four candidates existed for one role. The document's own `#5278D8`
(`docs/05-design-system.md:28`) is real in the repository, but the only place
it actually renders is as a focus-ring outline colour
(`rgba(82, 120, 216, .45)` — `82,120,216` is `#5278D8` — at
`apps/demo/src/styles.css:527`), not as the informational/submitted status it
was documented for. `--blue: #65719a` ships in the old `tokens.css` but is
never consumed as a status anywhere in `apps/demo`. `--atlas-blue: #18376a`
is a third candidate from an unselected design direction. `#3756a1` is the
one value actually doing the informational/submitted job today, at both
`--color-info-foreground` and `--color-readiness-review-foreground` in
`apps/demo/src/styles/theme.css:143,153`.

**Correction to the brief's citation.** The task brief (and the top-level
instructions) cite this evidence at `apps/demo/src/styles/theme.css:157`.
Reading the file directly puts `--color-info-foreground: #3756a1;` at line
**143**, not 157; line 157 is inside the "Evidence" comment block further
down. The ruling text above uses the verified line number. This is noted here
rather than silently corrected, since it's a discrepancy between the
instructions and the repository as it stands.

### `shadow` = `0 8px 30px rgba(21,23,25,.07)`, one token, not a ladder

> Owner ruled at a render, 2026-08-03: the document's single shadow, 0 8px
> 30px rgba(21,23,25,.07) — not the shipped six-step ladder
> (packages/ui/src/tokens.css:33-36,42-43). The document defines one shadow,
> so the source carries one shadow token, named `shadow` to match the
> document's own naming, which has no scale suffix. No further elevation
> steps are invented around it: a token with no consumer is an untested
> assertion, and B0's single screen (apps/mobile/src/screens/token-proof.tsx)
> uses no shadow at all — further steps arrive in B1 when a screen needs
> them. androidElevation is hand-authored at 3, because Android's elevation
> encodes offset, blur and opacity in one scalar and cannot be derived from
> the CSS value: Material's scale puts a bare resting surface at ~1dp, and
> this shadow's 8px offset and 30px blur describe more separation from the
> page than that minimal geometry even at only 7% opacity, so 3 sits one
> deliberate step above resting rather than at it — nowhere near a floating
> dialog's 24dp.

The shipped ladder (`--shadow-xs`, `--shadow-sm`, `--shadow`, `--shadow-lime`,
plus `--atlas-shadow`, `--atlas-folio-shadow`) used a darker ink base
(`rgba(23,23,23,…)`) and ran from a 16px blur up to a 70px blur — the
document's own words call for "shallow", and 70px is not shallow by any
reading. A second implementer independently used the document's ink base at
a shallow geometry — `--shadow-raised: 0 1px 2px rgba(21,23,25,.05), 0 2px
6px rgba(21,23,25,.06);` at `apps/demo/src/styles/theme.css:215`, whose
sibling comment quotes doc 05 directly: "borders carry most structure;
shadows are shallow". `21,23,25` is the same ink base the document's single
shadow uses, and the geometry is shallow in the same spirit — but
`--shadow-raised` is a *different* value from the document's: a two-layer
shadow at 1-2px offsets with .05/.06 opacity, not the document's single
`0 8px 30px rgba(21,23,25,.07)` layer. This is corroboration that a second
implementer read the document's *intent* the same way the owner ruled —
shallow, correct ink base, not the shipped ladder's darker base and 70px
blur — not evidence that anyone had already shipped the document's literal
value. (An earlier draft of this record overstated this as the document's
"exact value"; corrected here.) The source now carries exactly the one
shadow the document defines, named `shadow` with no scale suffix, and
nothing else — inventing a ladder around a single documented value, or
keeping the six shipped steps, would both be assertions with no consumer to
validate them.

**`androidElevation`.** I authored `3`. Reasoning: Android's `elevation`
prop is a single scalar that stands in for offset, blur *and* shadow opacity
all at once — there's no formula from `{offsetY: 8, blurRadius: 30, alpha:
0.07}` to an elevation number, so it has to be a judgment call recorded next
to the value it approximates, not computed by a generator. Material's own
elevation scale puts a bare resting surface at ~1dp (Material 2's baseline;
Level 1 in Material 3) — barely lifted off the page — with ~6-8dp read as an
actively-raised element (FAB, app bar) and ~24dp as a modal floating well
above the page. The document's shadow — an 8px offset and a 30px blur —
describes more separation from the page than Material's minimal ~1dp
reference geometry, even though its opacity is very low (7%). So `3` sits
one deliberate step above bare-resting rather than at it, on the strength of
the geometry rather than the opacity, and stays nowhere near the FAB/dialog
end of the scale. (An earlier draft of this record instead called `3`
Material's "default resting-card elevation" — Material actually puts a
resting card near 1dp, not 3; the reasoning above replaces that claim rather
than changing the number, which stays defensible on the corrected grounds.)

## The `rgba()` helper's first real consumer

`packages/tokens/scripts/generate-css.mjs`'s `rgba()` helper existed since
Task 1 but was unreachable — all ten original colours have `alpha: 1`, so the
`t.alpha === 1 ? t.hex : rgba(t)` branch never took the `rgba()` path. The
shadow's colour (`{ hex: "#151719", alpha: 0.07 }`) is the first value to
actually exercise it. Verified directly rather than assumed:

```
$ node -e '
function rgba({ hex, alpha }) {
  const n = parseInt(hex.slice(1), 16);
  return `rgba(${(n >> 16) & 255}, ${(n >> 8) & 255}, ${n & 255}, ${alpha})`;
}
console.log(rgba({hex:"#151719", alpha:0.07}));
'
rgba(21, 23, 25, 0.07)
```

`21, 23, 25` is exactly the document's `rgba(21,23,25,.07)`. The committed
`packages/ui/src/tokens.generated.css` shows the helper's real output:
`--shadow: 0px 8px 30px 0px rgba(21, 23, 25, 0.07);`.

## Step 3: the replacement diff

`packages/ui/src/tokens.css` is deleted. `packages/ui/package.json`'s export
map now points `"./tokens.css"` at `./src/tokens.generated.css`. Full diff,
old file (as it stood at HEAD before this task) against the new generated
file, verbatim:

```diff
--- packages/ui/src/tokens.css (deleted, as it stood at HEAD before this task)
+++ packages/ui/src/tokens.generated.css (current)
@@ -1,55 +1,18 @@
-/* Evidence Atlas design tokens — sourced verbatim from prototype/src/styles.css
-   (the two `:root` blocks: the base palette/shadow block at the top of the
-   file, and the "Evidence Atlas — selected Product Design direction" block
-   near the bottom). Visual tokens only — no component markup or state is
-   copied from the prototype.
-
-   Fonts: body is Inter Variable, headings/brand are Manrope Variable
-   (@fontsource-variable/inter, @fontsource-variable/manrope — see
-   prototype/src/main.jsx and prototype/package.json for the source
-   versions). Consumers of this package are responsible for importing the
-   font-loader packages themselves; this file only sets the font-family
-   custom properties/selectors, it does not bundle font files. */
-
+/* GENERATED — do not edit. Source: packages/tokens/src/tokens.json
+   Regenerate: node packages/tokens/scripts/generate-css.mjs
+   packages/testing/src/token-fidelity.test.ts fails if this drifts. */
 :root {
-  --ink: #171717;
-  --ink-2: #242424;
-  --paper: #fbfbfb;
-  --white: #fff;
-
-  --signal: #c6ff34;
-  --signal-dark: #667f12;
-  --slate: #484c5e;
-  --line: rgba(72, 76, 94, .17);
+  --ink-950: #171717;
+  --ink-800: #242424;
+  --paper: #FBFBFB;
+  --white: #FFFFFF;
+  --signal-500: #C6FF34;
+  --slate-600: #484C5E;
+  --signal-700: #667F12;
+  --amber-500: #F2B84B;
+  --red-500: #E45C55;
   --muted: #666979;
-  --amber: #f2b84b;
-  --red: #e45c55;
-  --blue: #65719a;
-
-  --glass: rgba(255, 255, 255, .78);
-  --glass-strong: rgba(255, 255, 255, .9);
-  --glass-dark: rgba(23, 23, 23, .78);
-
-  --shadow-xs: 0 5px 16px rgba(23, 23, 23, .055);
-  --shadow-sm: 0 12px 32px rgba(23, 23, 23, .085);
-  --shadow: 0 24px 70px rgba(23, 23, 23, .13);
-  --shadow-lime: 0 12px 28px rgba(198, 255, 52, .2);
-
-  /* Evidence Atlas — selected Product Design direction */
-  --atlas-paper-deep: #f2f0e9;
-  --atlas-rule: rgba(72, 76, 94, .2);
-  --atlas-blue: #18376a;
-  --atlas-shadow: 0 22px 55px rgba(23, 23, 23, .1);
-  --atlas-folio-shadow: 8px 11px 0 rgba(72, 76, 94, .045), 0 24px 70px rgba(23, 23, 23, .11);
-}
-
-body {
-  font-family: "Inter Variable", Inter, sans-serif;
-  color: var(--ink);
-  background: var(--paper);
-  font-synthesis: none;
-}
-
-h1, h2, h3, .brand, strong {
-  font-family: "Manrope Variable", Manrope, sans-serif;
+  --line: #D9DBD5;
+  --blue-500: #3756a1;
+  --shadow: 0px 8px 30px 0px rgba(21, 23, 25, 0.07);
 }
```

## Finding, then fix: retiring the old stylesheet broke `apps/landing` —
## it shipped in serif with black text, and is now fixed

**In plain words: the first version of this task's commit broke
`apps/landing`.** After `packages/ui/src/tokens.css` was deleted, a rebuilt
and served `apps/landing` rendered `body`, `.brand`, `h1` and `p` in the
browser's default serif font (`"Times"` in the environment a reviewer tested
in), and the hero `<main>` text fell back to inherited/initial colour
(effectively black) instead of the intended `#171717`, because
`color: var(--ink)` referenced a custom property that no longer existed. The
task's own suite — 626 tests, typecheck, build — passed over this, because
nothing in it renders `apps/landing` and reads computed styles; `pnpm build`
only checks that the page compiles, not what it looks like. A reviewer
rebuilt the page, served it, and read `getComputedStyle()` to catch it. This
section originally reported the finding without fixing it; that was
necessary but not sufficient, since this task's own commit caused the break.
It has since been fixed, in this same task, before landing. The original
finding is kept below for the record, followed by the fix and its
verification.

The task's ambiguity-resolution instructions say explicitly: if retiring the
old file changes what `apps/landing` renders in any way beyond the three
ruled tokens, stop and report it rather than absorb it silently. It does.

The old `tokens.css` carried three things that were never token values in the
`tokens.json` sense:

1. **A `body { font-family; color; background; font-synthesis }` rule.**
   `apps/landing/app/page.tsx` never sets its own font — it relies on this
   rule (via the old stylesheet) to apply `"Inter Variable", Inter,
   sans-serif` to the page and `font-synthesis: none`.
2. **An `h1, h2, h3, .brand, strong { font-family: "Manrope Variable" … }`
   rule.** `apps/landing/app/page.tsx:18` renders `<span className="brand">`
   specifically to pick this up.
3. **Custom-property names that don't match the source's.** The old file
   named the ink/signal/slate/red/amber/blue tokens `--ink`, `--signal`,
   `--slate`, `--red`, `--amber`, `--blue` (bare); the generated source names
   them `--ink-950`, `--signal-500`, `--slate-600`, `--red-500`, `--amber-500`
   and, per the owner's ruling, `--blue-500`. `apps/landing/app/page.tsx:14`
   sets `color: "var(--ink)"` inline — that custom property no longer exists
   at all in the generated file.

None of `tokens.json`'s design ever modeled "apply this custom property to
this selector" — the generator's whole contract is a flat `:root { --token:
value }` block, nothing more. So this loss was structural to the migration
described across Tasks 1-5, not a mistake introduced by this task's edits.

Confirmed against the real build output, not just by reading source. Before
this task, the compiled CSS for `apps/landing` (`apps/landing/.next` as it
stood at the start of this task) contained:

```css
:root{--ink:#171717;--ink-2:#242424;--paper:#fbfbfb;--white:#fff;--signal:#c6ff34;--signal-dark:#667f12;--slate:#484c5e;--line:#484c5e2b;--muted:#666979;--amber:#f2b84b;--red:#e45c55;--blue:#65719a;--glass:#ffffffc7; …}
body{color:var(--ink);background:var(--paper);font-synthesis:none;font-family:Inter Variable,Inter,sans-serif}
h1,h2,h3,.brand,strong{font-family:Manrope Variable,Manrope,sans-serif}
```

After this task's `pnpm build`, the compiled CSS for `apps/landing`
(`apps/landing/.next/static/chunks/09n7el0h9qqec.css`) is:

```css
:root{--ink-950:#171717;--ink-800:#242424;--paper:#fbfbfb;--white:#fff;--signal-500:#c6ff34;--slate-600:#484c5e;--signal-700:#667f12;--amber-500:#f2b84b;--red-500:#e45c55;--muted:#666979;--line:#d9dbd5;--blue-500:#3756a1;--shadow:0px 8px 30px 0px #15171912}
```

No `body {}` rule, no `.brand`/heading font-family rule, no `--ink` (or
`--signal`, `--slate`, `--red`, `--amber`, `--blue`) at all. `--paper` and
`--muted` are unaffected (same names, same values, so
`background: var(--paper)` and `color: var(--muted)` in `page.tsx` still
resolve exactly as before). `color: var(--ink)` in `page.tsx:14` now
resolves to nothing, so `<main>`'s text colour falls back to the browser's
inherited/initial value instead of `#171717` — visually similar on most
browsers' default black, but not the same mechanism, and not guaranteed.
Both the page body and the `.brand` span lose their font-family assignment
entirely and fall back to the browser's default font stack, even though
`@fontsource-variable/inter` and `@fontsource-variable/manrope` are still
imported in `apps/landing/app/layout.tsx` and still register the `@font-face`
rules — nothing in the new source applies them to anything.

### The fix

No compatibility shim of bare aliases (`--ink`, `--signal`, …). Those bare
names are exactly the prototype-convenience naming the ruling rule gives to
the design document, not to a shipped prototype — reintroducing them as
aliases would rebuild the drift this slice exists to end, just one layer
indirected.

Instead, the two things the old file conflated are now separate:

- **Tokens stay generated, untouched.** `packages/ui/src/tokens.generated.css`
  is exactly what it was — no hand edits, no aliases.
- **Base styles get their own hand-maintained file.**
  `packages/ui/src/base.css` now holds exactly what the old `tokens.css`
  carried beyond token declarations: the `body { font-family; color;
  background; font-synthesis }` rule and the `.brand`/heading `font-family`
  rule. Its header comment says plainly that these are base styles, not
  tokens, that they were previously smuggled into a tokens file, and that
  they reference the generated custom properties by their documented names
  (`var(--ink-950)`, `var(--paper)`).
- `packages/ui/package.json`'s export map now carries both
  `"./tokens.css"` and `"./base.css"`.
- `apps/landing/app/layout.tsx` imports both. `apps/landing/app/page.tsx`'s
  inline `color: "var(--ink)"` is now `color: "var(--ink-950)"` — the
  documented name, not the retired bare alias.

### Verification: rebuilt and read computed styles, not just "the build passed"

`pnpm --filter @aktflow/landing build`, then served the production build
(`next start` on port 3100) and read `getComputedStyle()` in a real browser
tab, the same way the reviewer did:

**Before the fix** (compiled CSS had no `body {}` rule, no `.brand` rule, no
`--ink`; reviewer's own reading, corroborated by the compiled-CSS diff
above):

| | Before |
|---|---|
| `body` computed `font-family` | browser default serif (reviewer: `"Times"`) |
| `main` computed `color` | inherited/initial (effectively black; `var(--ink)` — undefined) |

**After the fix**, read directly from the running server:

```js
{
  "bodyFontFamily": "\"Inter Variable\", Inter, sans-serif",
  "bodyColor": "rgb(23, 23, 23)",
  "bodyBackground": "rgb(251, 251, 251)",
  "brandFontFamily": "\"Manrope Variable\", Manrope, sans-serif",
  "h1FontFamily": "\"Manrope Variable\", Manrope, sans-serif",
  "pFontFamily": "\"Inter Variable\", Inter, sans-serif",
  "mainColor": "rgb(23, 23, 23)",
  "inkCustomProp": "#171717"
}
```

`rgb(23, 23, 23)` is `#171717` — the intended ink colour, now resolved
through the documented `--ink-950` name rather than a retired bare alias.
`body`, `.brand` and `h1` all resolve to the intended font stacks; `p`
correctly inherits `body`'s Inter rather than picking up the Manrope rule
(it was never in the `h1, h2, h3, .brand, strong` selector list, before or
after). Nothing here is inferred from source reading or from a green build —
it is what the browser actually computed for a served page.

## Evidence

| Gate | Result |
|---|---|
| `pnpm --filter @aktflow/tokens generate` | ran clean; `tokens.generated.css` and `tokens.generated.ts` both updated |
| `pnpm --filter @aktflow/mobile generate` | ran clean; "wrote 6 labels", no diff (already current) |
| Serialized suite | `pnpm turbo run test --concurrency=1 --force` — 6/6 tasks, **626 tests**: app 246, testing 135, demo 134, domain 98, database 7, contracts 6 |
| Typecheck | `pnpm typecheck` — 10/10 |
| Build | `pnpm build` — 3/3 (app, demo, landing — the only packages with a `build` script; unchanged) |
| Canonical docs | `node scripts/validate-canonical-docs.mjs` — `canonical documentation: OK` |
| `apps/landing` computed styles | rebuilt, served, `getComputedStyle()` read in a browser — see "Finding, then fix" above |
| Fidelity-guard mutation checks | shadow-ruling deletion and `contested` refill, both caught — see "Finding, then fix" above and the guard section below |

Baseline before B0 was 619; this slice added 7 (619 + 7 = 626), matching the
task brief's prediction exactly. Per-package breakdown, from the actual
forced run: `@aktflow/app` 246, `@aktflow/testing` 135, `@aktflow/demo` 134,
`@aktflow/domain` 98, `@aktflow/database` 7, `@aktflow/contracts` 6.
`@aktflow/mobile`, `@aktflow/landing`, `@aktflow/ui` and `@aktflow/tokens`
declare no `test` script, so turbo does not generate a task for them — hence
"6/6 tasks" rather than a count against all 10 workspace packages.

**One correction to the brief's own prediction.** The task brief says to
expect "typecheck 9/9". The real, observed number is **10/10** — a tenth
package (`@aktflow/landing`, or `@aktflow/mobile`, depending which was
uncounted when that prediction was written) now exists with its own
`typecheck` script. Reported as observed, not as predicted.

**The `packages/database` test count moved from 4 (M2-A gate) to 7 here.**
That is not a regression or something this task introduced — it reflects
tests added between M2-A and B0 (the M2 service-principal work). All 7 pass
once `APP_DB_URL` and `SERVICE_DB_URL` are set to the documented local dev
credentials (`apps/app/.env.example`); they are not set by default in a bare
shell, which is why the first, unconfigured run of the serialized suite
failed 6 of `packages/database`'s tests with `APP_DB_URL is not set` before
the credentials were exported for the run recorded above.

## The fidelity guard now covers the shadow's ruling and `contested`'s emptiness

A review found two global constraints of this task that held today but were
unguarded: deleting the shadow's `ruling` string still left
`token-fidelity.test.ts` at 4/4, because "carries a ruling for every token"
iterated only `src.color`; and refilling `contested` with
`["blue-500", "line"]` also left it at 4/4, because nothing asserted
`contested` stays empty. Both are now covered: the ruling check also walks
`src.shadow`'s named tokens (filtered from its block-level metadata the same
way the generators do — an object carrying a `layers` array), and a new test
asserts `src.contested` is `[]`.

Both additions were mutation-checked directly, not assumed correct because
they read correctly:

```
$ node -e '/* delete src.shadow.shadow.ruling, rewrite tokens.json */'
$ npx vitest run src/token-fidelity.test.ts
 ❯ src/token-fidelity.test.ts (5 tests | 1 failed)
   × the source accounts for every documented colour > carries a ruling for every token, so no value is unexplained
     → expected [ 'shadow.shadow' ] to deeply equal []
 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)
```

```
$ node -e '/* set src.contested = ["blue-500", "line"], rewrite tokens.json */'
$ npx vitest run src/token-fidelity.test.ts
 ❯ src/token-fidelity.test.ts (5 tests | 1 failed)
   × the source accounts for every documented colour > the contested list is empty, so every B0 render has been ruled
     → expected [ 'blue-500', 'line' ] to deeply equal []
 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)
```

`tokens.json` was restored from a pre-mutation copy after each check;
`git diff --stat` shows only the intended one-line `androidElevation`-ruling
correction against the pre-mutation state, confirming no mutation leaked into
the commit. With both mutations reverted, the suite is back to 5/5.

## What this slice does not close

**One item that was open mid-task and is closed as of this commit.** This
task's own Step 3 broke `apps/landing`'s rendering — the retirement of the
old stylesheet dropped the only rule that gave the page a font and dropped
the only custom property the page's inline `color` referenced, so a rebuilt,
served `apps/landing` showed the marketing page in browser-default serif
with black hero text instead of the intended Inter/Manrope and `#171717`.
626 passing tests, a clean typecheck and a green build all missed it, because
none of them render the page and read computed styles. It is fixed: the base
styles the old file smuggled in now live in `packages/ui/src/base.css`,
hand-maintained and separate from the generated tokens; `apps/landing`
imports both `@aktflow/ui/tokens.css` and `@aktflow/ui/base.css` and uses the
documented token names (`var(--ink-950)`, not the retired `var(--ink)`).
Verified by rebuilding, serving the page, and reading `getComputedStyle()` —
see "Finding, then fix" above for the before/after values. As of this commit,
nothing about `apps/landing`'s rendering is known to remain open.

> The device-install step is NOT DONE. It is blocked on three accounts and two
> devices, and no code closes it.

Recorded in full in
[2026-08-01-b0-procurement.md](2026-08-01-b0-procurement.md): an Apple
Developer Program membership (which itself needs a D-U-N-S number from Dun &
Bradstreet first), a Google Play Console account, and a funded Expo/EAS
account, plus a physical supported iPhone and a physical lower-resource
Android device. `apps/mobile/eas.json` carries build profiles that have never
been resolved by any EAS command in this environment.

> The fidelity guard does not catch a wrong-but-consistent value. Once a value
> is in the source, generation makes it consistent everywhere — which is exactly
> how `muted` drifted with its name intact. Only the visual gate catches that
> class.

`packages/testing/src/token-fidelity.test.ts` asserts that generated output
matches `tokens.json` and that every documented token is accounted for. It
does not and cannot assert that a value is the *right* value — the three
rulings this task recorded were themselves not something any test could have
produced; they required an owner looking at a render.

> Five of seven token categories remain untokenised, by decision. They are
> tokenised in B1 when a screen consumes them.

Only colour and shadow are in `packages/tokens/src/tokens.json`. Typography,
spacing, radius, border-width and motion are not — the stated principle,
repeated throughout this slice's specs and this task's own instructions, is
that a token with no consumer is an untested assertion (the same principle
that kept this task from inventing an elevation ladder around the single
ruled shadow). They arrive in B1 when a screen actually needs them.

## Commit

This gate closed across two commits on `claude/m2-b0-foundations`. The first
(`809899f`) recorded the three rulings, extended the generators, and retired
`packages/ui/src/tokens.css` — and, unreviewed at that point, broke
`apps/landing`. The second, on top of it, is round 1 of review fixes: adds
`packages/ui/src/base.css`, repoints `packages/ui/package.json`'s export map
and `apps/landing`'s imports and token references, corrects this record's
`theme.css:215` and Material-elevation claims (and the matching claim in
`tokens.json`'s shadow ruling), extends
`packages/testing/src/token-fidelity.test.ts` to cover the shadow's ruling
and `contested`'s emptiness, and updates this record's "What this slice does
not close" section to say what happened. See the commits for the exact
diffs.
